# Bản đồ Mã nguồn dự án QLNS

Tra cứu nhanh "tính năng nằm ở file nào" — theo yêu cầu [`docs/YEU_CAU_DU_AN_QLNS.html`](docs/YEU_CAU_DU_AN_QLNS.html) mục 6.1 (bắt buộc trình bày dạng **bảng chỉ mục tính năng**) và [`docs/CODING_STANDARDS.md`](docs/CODING_STANDARDS.md). Cập nhật ngay khi thêm/sửa 1 chức năng, không dồn lại.

## Quy trình thêm 1 chức năng mới

Theo khuôn các module đã có (`Position` gọn nhất, `Employee` đầy đủ nhất).

**Backend:**

| Bước | Làm gì | Bỏ qua khi nào | Ví dụ |
| --- | --- | --- | --- |
| 1. Migration | Tạo bảng mới | Dùng lại bảng có sẵn | [`create_provinces_table.php`](database/migrations/2026_09_07_000001_create_provinces_table.php) |
| 2. Model | `$fillable`, quan hệ | Không bỏ qua nếu có bảng mới | [`Position.php`](app/Models/Position.php) |
| 3. Request | Validate input | Chức năng chỉ đọc | [`StorePositionRequest.php`](app/Http/Requests/Position/StorePositionRequest.php) |
| 4. Repository | Hàm truy vấn DB | Hiếm khi bỏ qua | [`PositionRepository.php`](app/Repositories/PositionRepository.php) |
| 5. Service | Logic nghiệp vụ | Vẫn nên có bản mỏng cho nhất quán | [`PositionService.php`](app/Services/PositionService.php) |
| 6. Controller | Nhận request → gọi Service → trả response | Không bỏ qua | [`PositionController.php`](app/Http/Controllers/Api/V1/PositionController.php) |
| 7. Route | Đăng ký URL + `permission:xxx` | Không bỏ qua | [`routes/api/v1/positions.php`](routes/api/v1/positions.php) |
| 8. Resource | Định dạng JSON / ẩn field nhạy cảm | Không có field nhạy cảm | [`EmployeeResource.php`](app/Http/Resources/EmployeeResource.php) |
| 9. Test | Quyền hạn, validate, ca chính | Không nên bỏ qua | [`PositionTest.php`](tests/Feature/Position/PositionTest.php) |

**Frontend:**

| Bước | Làm gì | Ví dụ |
| --- | --- | --- |
| 1. Service (`xxxService.js`) | Hàm gọi API | [`positionService.js`](resources/js/services/positionService.js) |
| 2. Store (Pinia) | Chỉ khi nhiều trang cùng cần | [`useEmployeeStore.js`](resources/js/stores/useEmployeeStore.js) |
| 3. View danh sách | Dùng lại [`DataTable.vue`](resources/js/components/common/DataTable.vue) | [`Employees.vue`](resources/js/views/Employee/Employees.vue) |
| 4. Form Thêm/Sửa | Dùng lại `FormDialog`/`FormSection`/`SearchSelect`/`InputDate`/`InputMoney` (mục 10) | [`EmployeeForm.vue`](resources/js/views/Employee/EmployeeForm.vue) |
| 5. Router | Route mới + `meta.title` | [`router/index.js`](resources/js/router/index.js) |
| 6. Cập nhật file này | 1 mục mới đúng khuôn | — |

### Ví dụ 1 luồng chạy thật: "Thêm nhân viên"

```
Frontend (gửi)   Employees.vue → EmployeeForm.vue → useEmployeeStore.js → employeeService.js
Backend (xử lý)  routes → auth:api → permission:employee.create → StoreEmployeeRequest
                 → EmployeeController → EmployeeService → EmployeeRepository → Employee
                 → EmployeeResource → JSON
Frontend (nhận)  store nhận response → toast + đóng modal → Employees.vue gọi lại GET, làm mới bảng
```

**4 chỗ có thể dừng sớm** (sai thì dừng ngay, không đi tiếp): (1) validate phía client (`formRef.value.validate()`) — sai thì 0 request gửi lên; (2) middleware `auth:api` — token sai → 401; (3) middleware `permission:employee.create` — thiếu quyền → 403; (4) `StoreEmployeeRequest` — data sai → 422. Càng dừng sớm càng đỡ tốn (mạng, Service/Repository, công viết lại validate ở Controller).

---

## 1. Xác thực (JWT Authentication)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`Login.vue`](resources/js/views/Login.vue), [`ForgotPassword.vue`](resources/js/views/ForgotPassword.vue), [`ResetPassword.vue`](resources/js/views/ResetPassword.vue) (đọc `token`/`email` từ query string), [`authStore.js`](resources/js/stores/authStore.js), [`authService.js`](resources/js/services/authService.js) |
| Xử lý Backend | [`AuthController.php`](app/Http/Controllers/Api/V1/AuthController.php), [`PasswordResetController.php`](app/Http/Controllers/Api/V1/PasswordResetController.php), [`AuthService.php`](app/Services/AuthService.php), [`PasswordResetService.php`](app/Services/PasswordResetService.php), [`Jwt/JwtService.php`](app/Services/Jwt/JwtService.php), [`Auth/JwtGuard.php`](app/Auth/JwtGuard.php), [`UserRepository.php`](app/Repositories/UserRepository.php), [`RefreshTokenRepository.php`](app/Repositories/RefreshTokenRepository.php), [`PasswordResetRepository.php`](app/Repositories/PasswordResetRepository.php) |
| Mail | [`PasswordResetMail.php`](app/Mail/PasswordResetMail.php) — gửi **đồng bộ** (chưa có queue worker lúc đó, xem mục 21) |
| Database & API | `users`, `refresh_tokens`, `password_reset_tokens`, `password_histories`; `POST /auth/login`, `/refresh`, `GET /auth/me`, `POST /auth/logout`, `/forgot-password`, `/reset-password` ([`routes/api/v1/auth.php`](routes/api/v1/auth.php)) — 2 route quên/đặt lại mật khẩu không cần `auth:api` |
| Test | [`AuthTest.php`](tests/Feature/Auth/AuthTest.php), [`LoginValidationTest.php`](tests/Feature/Auth/LoginValidationTest.php), [`PasswordResetTest.php`](tests/Feature/Auth/PasswordResetTest.php) |
| Ghi chú | Quên mật khẩu luôn trả thành công dù email không tồn tại (chống dò email — OWASP). Đặt lại mật khẩu xong thu hồi **mọi** refresh token của user. `JwtGuard` cache theo **token đã giải mã**, không phải cờ `bool` — cần thiết để `actingAs()`/đổi user giữa 2 lệnh gọi HTTP trong cùng 1 test hoạt động đúng. **Access token hết hạn giữa phiên KHÔNG phải bug** — `bootstrap.js` tự bắt 401 → gọi `/auth/refresh` → gửi lại request gốc; dòng "401 (Unauthorized)" đỏ trên console chỉ là trình duyệt tự log request thất bại, kể cả khi code đã retry thành công ngay sau đó. **Bug thật đã sửa (Ngày 44)**: `JwtService::decodeAccessToken()` trước đó chỉ bắt `ExpiredException`/`SignatureInvalidException`/`UnexpectedValueException` — token hỏng dạng khác (payload giải mã base64 ra không phải JSON hợp lệ) khiến `firebase/php-jwt` ném `DomainException`, không nằm trong danh sách bắt cũ nên vỡ thành 500 thay vì 401 sạch. Đã thêm bắt cả `DomainException`/`InvalidArgumentException`, kèm test `test_access_token_with_invalid_payload_encoding_is_rejected` |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 2. Phân quyền (RBAC)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`AppSidebar.vue`](resources/js/components/layout/AppSidebar.vue) ẩn menu qua `can(code)`; [`router/index.js`](resources/js/router/index.js) mỗi route khai `meta.permission`, 1 `beforeEach` guard đá về Dashboard nếu thiếu quyền — **chỉ là lớp UX phụ**, quyền thật luôn do backend middleware `permission:xxx` enforce |
| Xử lý Backend | [`EnsurePermission.php`](app/Http/Middleware/EnsurePermission.php) (alias `permission`), [`RoleController.php`](app/Http/Controllers/Api/V1/RoleController.php) (route `GET /roles` gốc chỉ đọc, gate `employee.update` — dùng cho dropdown tạo tài khoản, xem mục 11) |
| Model | [`Role.php`](app/Models/Role.php), [`Permission.php`](app/Models/Permission.php) |
| Database & API | `roles`, `permissions`, `role_permissions`, `user_roles`; [`PermissionSeeder.php`](database/seeders/PermissionSeeder.php), [`RolePermissionSeeder.php`](database/seeders/RolePermissionSeeder.php) |
| Test | [`PermissionMiddlewareTest.php`](tests/Feature/PermissionMiddlewareTest.php) |
| Ghi chú | Role Employee **không có** `employee.view` (chỉ xem hồ sơ chính mình). `RolePermissionSeeder` tự dọn quyền thừa mỗi lần chạy lại (đồng bộ thật, không cộng dồn), bọc `DB::transaction()` |
| **Màn hình quản lý Role/Permission đầy đủ** (2026-09-24, theo yêu cầu người dùng — 2 tài liệu dự án KHÔNG có "Ngày" nào dành riêng cho màn hình này, chỉ nói chung "Permission linh hoạt" ở mục 3.1 YEU_CAU; đã hỏi lại và người dùng CHỐT: cho tạo Permission mới tự do qua UI, giới hạn quyền `rbac.manage`) | Gate riêng biệt **`rbac.manage`** (đã seed sẵn, hiện chỉ Admin có) — KHÔNG đụng route `GET /roles` cũ. Backend: [`PermissionController.php`](app/Http/Controllers/Api/V1/PermissionController.php)/[`PermissionService.php`](app/Services/PermissionService.php)/[`PermissionRepository.php`](app/Repositories/PermissionRepository.php) (CRUD đầy đủ, chặn xóa quyền đang gán cho Role nào — 422); mở rộng [`RoleController.php`](app/Http/Controllers/Api/V1/RoleController.php) thêm `store/show/update/destroy/updatePermissions` qua [`RoleService.php`](app/Services/RoleService.php) (route MỚI, cùng file [`routes/api/v1/roles.php`](routes/api/v1/roles.php), nhóm middleware riêng `rbac.manage`). `RoleService::syncPermissions()` là **sync thật** (gỡ quyền không còn trong payload, không chỉ cộng dồn), ghi `granted_by`/`granted_at`, sau đó `Cache::forget("permission:{id}")` cho MỌI user đang giữ Role đó (nếu không, quyền vừa đổi phải chờ tới 60s cache cũ hết hạn mới có hiệu lực — xem `EnsurePermission`). **Chặn tự khóa hệ thống**: không cho gỡ `rbac.manage` khỏi 1 Role nếu đó là Role DUY NHẤT còn giữ quyền này (sẽ không còn ai vào được màn hình quản lý quyền nữa). Xóa Role KHÔNG chặn cứng dù đang có user (xóa mềm, khôi phục được) — Frontend tự cảnh báo trước khi xác nhận. Frontend: [`Roles.vue`](resources/js/views/Role/Roles.vue) (danh sách + Sửa/Xóa), [`RoleForm.vue`](resources/js/views/Role/RoleForm.vue) (tạo/sửa tên+mô tả), [`RolePermissionsDialog.vue`](resources/js/views/Role/RolePermissionsDialog.vue) (ma trận checkbox nhóm theo tiền tố mã quyền + form nhỏ tạo quyền mới kèm `v-alert` cảnh báo **quyền mới tạo không tự khóa được API nào** — cần dev tự gắn `permission:<code>` vào route liên quan). Route `/roles` (`meta.permission: "rbac.manage"`) + mục sidebar "Vai trò & Phân quyền" (trước đó `disabled`, giờ bật `v-if="can('rbac.manage')"`). |
| Test (màn hình quản lý) | [`PermissionCrudTest.php`](tests/Feature/Permission/PermissionCrudTest.php), [`RoleCrudTest.php`](tests/Feature/Role/RoleCrudTest.php) (có test regression: route `GET /roles` cũ không bị đụng), [`RolePermissionSyncTest.php`](tests/Feature/Role/RolePermissionSyncTest.php) (sync thật không cộng dồn, cache invalidate có hiệu lực NGAY không phải chờ 60s, chặn tự khóa `rbac.manage`). |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 3. Audit Log

| Hạng mục | Chi tiết |
| --- | --- |
| Xử lý Backend | [`AuditLog.php`](app/Models/AuditLog.php), [`AuditObserver.php`](app/Observers/AuditObserver.php), [`Concerns/Auditable.php`](app/Models/Concerns/Auditable.php) |
| Database | `audit_logs` |
| Danh sách tệp cần sửa khi bảo trì | Muốn 1 Model tự ghi log created/updated/deleted: thêm `use Auditable;` |

## 4. Layout chính & Điều hướng

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`App.vue`](resources/js/App.vue) (chọn layout theo `route.meta.layout`), [`AppLayout.vue`](resources/js/components/layout/AppLayout.vue), [`AppHeader.vue`](resources/js/components/layout/AppHeader.vue), [`AppSidebar.vue`](resources/js/components/layout/AppSidebar.vue), [`AppBreadcrumbs.vue`](resources/js/components/layout/AppBreadcrumbs.vue) |
| Theme | [`plugins/vuetify.js`](resources/js/plugins/vuetify.js) (`qlnsDark`/`qlnsLight`), đổi qua `useTheme()` ở `AppHeader.vue` |
| Router | Route không dùng layout chính khai `meta.layout: 'blank'`; mỗi route khai `meta.title` (tiếng Việt, cho breadcrumb) và `meta.parent` nếu cần chèn cấp giữa |
| Ghi chú | Responsive mobile (sidebar overlay, breakpoint) — xem mục 26 |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 5. Dashboard — nối dữ liệu thật + biểu đồ (2026-09-24)

| Hạng mục | Chi tiết |
| --- | --- |
| Bối cảnh (theo yêu cầu người dùng) | Ban đầu Dashboard chỉ hiện SỐ GIẢ HARDCODE (`'128'`, `'6'`...) kèm chú thích "dữ liệu minh họa". Người dùng yêu cầu nâng cấp UX tổng thể, tôi đề xuất Dashboard là điểm bắt đầu hợp lý nhất (trang đầu tiên sau đăng nhập, đã có ghi chú "chưa làm" sẵn) — được đồng ý. Yêu cầu thêm "màu sắc hiện đại, tối giản công nghệ" — đã thử vẽ mockup qua Artifact trước (không thành công do lỗi đồng bộ phiên bản công cụ), người dùng gửi ảnh tham khảo 1 dashboard dark-theme có widget gauge tròn + biểu đồ đường mượt, rồi yêu cầu làm thẳng vào code thật. **Đối chiếu tài liệu**: YEU_CAU_DU_AN mục 3.2 dòng 1 ghi rõ "Dashboard: Biểu đồ tương tác ApexCharts/Chart.js, widget tổng quan nhân sự, thông báo nhanh, micro-animations" — đã chọn đúng ApexCharts (nêu tên đầu tiên trong tài liệu). |
| **Quyết định thiết kế — nội dung tự đổi theo quyền, KHÔNG phải 1 dashboard chung** | `DashboardService::forUser()` kiểm tra `employee.view` (đúng quyền đã gate trang "Nhân viên", mục 8): CÓ (HR/Manager/Admin) → dashboard "company" (số liệu toàn công ty — tổng nhân viên, tỷ lệ đi làm, phân bổ phòng ban...); KHÔNG (Employee thường) → dashboard "personal" (chỉ số liệu CỦA CHÍNH HỌ — ngày phép còn lại, trạng thái chấm công hôm nay, đơn đang chờ). Tránh lộ số liệu công ty cho người không có quyền xem, cùng tinh thần các chỗ ẩn dữ liệu nhạy cảm khác trong dự án. |
| Backend | [`DashboardService.php`](app/Services/DashboardService.php) — gom dữ liệu từ các Service/Model đã có (không tạo bảng mới): `employee_stats` (tổng/đang làm/mới tháng này), `attendance_today` (gọi lại `AttendanceService::dailyOverview()` sẵn có, mục 16), `leave_pending` (tách đúng "chờ quản lý"/"chờ HR" theo ĐÚNG logic `LeaveApprovalService::decide()` — nhân viên không có quản lý thì tính thẳng vào "chờ HR", không suy đoán riêng), `attendance_trend` (tỷ lệ đi làm 5 ngày làm việc gần nhất, mẫu số là tổng nhân viên active/probation HIỆN TẠI — chấp nhận xấp xỉ, không gọi `dailyOverview()` 5 lần vì quá nặng cho 1 widget tổng quan), `department_distribution` (đếm nhân viên theo `department_id`). [`DashboardController.php`](app/Http/Controllers/Api/V1/DashboardController.php), route `GET /api/v1/dashboard` (chỉ `auth:api`, không cần permission riêng — nội dung tự đổi theo quyền BÊN TRONG Service). |
| Frontend | Cài `apexcharts`+`vue3-apexcharts` (mới, chưa từng có thư viện biểu đồ nào trong dự án). 3 component dùng lại được: [`GaugeChart.vue`](resources/js/components/dashboard/GaugeChart.vue) (radialBar), [`DonutChart.vue`](resources/js/components/dashboard/DonutChart.vue), [`AreaTrendChart.vue`](resources/js/components/dashboard/AreaTrendChart.vue) — cả 3 đọc màu qua `useTheme()` (`theme.global.current.value.colors.primary`...), TỰ đổi màu/chế độ sáng-tối đúng theo theme thật của app (`qlnsDark`/`qlnsLight`, xem `plugins/vuetify.js`), không hardcode màu riêng như 1 bản mockup tĩnh. [`Dashboard.vue`](resources/js/views/Dashboard.vue) — nhánh "company" dùng [`StatCards.vue`](resources/js/components/dashboard/StatCards.vue) (4 thẻ, tái dùng nguyên component đã có ở Employees.vue/AttendanceOverview.vue) + gauge/donut/area chart + panel "Thông báo gần đây" (đọc thẳng `useNotificationStore()` đã kết nối realtime sẵn app-wide, mục 33 — không gọi API riêng); nhánh "personal" đơn giản hơn (3 thẻ + panel thông báo). |
| Kiểm thử đã làm | [`DashboardTest.php`](tests/Feature/DashboardTest.php) — 401 chưa đăng nhập, HR/Manager thấy `scope: company` đúng cấu trúc, Employee thấy `scope: personal`, và 1 test dựng dữ liệu thật (có/không quản lý, đơn ở cả 2 trạng thái) xác nhận số đếm "chờ quản lý"/"chờ HR" khớp CHÍNH XÁC logic duyệt đơn thật — không phải đoán đại 1 công thức riêng rồi hy vọng đúng. Verify thêm qua curl thật (Admin thấy company, Employee thấy personal, số liệu khớp DB dev). KHÔNG verify được bằng mắt qua trình duyệt (không có Playwright trong phiên này) — người dùng tự xem qua Vite dev server đang chạy sẵn (cổng 5173, HMR). |
| Chưa làm / giới hạn đã biết | `attendance_trend` dùng mẫu số CỐ ĐỊNH (tổng nhân viên hiện tại) cho cả 5 ngày thay vì đúng số nhân viên được phân ca từng ngày đó — xấp xỉ, đủ cho biểu đồ xu hướng tham khảo, không dùng để báo cáo chính thức (báo cáo chính thức đã có mục riêng, xem mục báo cáo "Ngày 51-60"). Bundle ApexCharts khá nặng (~290KB gzip riêng trang Dashboard) — chấp nhận được vì tách lazy-load riêng route, không ảnh hưởng các trang khác. |
| **"Cần bạn xử lý" — demo (2026-09-25, theo yêu cầu người dùng)** | Người dùng hỏi ý kiến cải thiện UX chung, tôi đề xuất gộp mọi việc CHỜ DUYỆT (chấm công/nghỉ phép/điều chỉnh công — trước đó nằm rải rác ở chuông + 3 trang riêng) vào 1 khối trên Dashboard — khớp Ke-hoach Ngày 59 "Tích hợp thông báo nhanh trên Dashboard... đơn phép chờ duyệt" (mở rộng thêm 2 loại "chờ duyệt" khác vốn đã có sẵn trong dự án, tài liệu chỉ nêu tên "đơn phép" nhưng không cấm mở rộng). `DashboardService::actionItems(User $user)` — MỖI mục lọc theo ĐÚNG quyền tương ứng (`attendance.approve` → số chấm công `approval_status=pending` đã có check-in; `leave.approve_manager`/`leave.approve_hr` → tái dùng `leavePendingCounts()`, cộng đúng phần theo cấp user có quyền; `attendance.adjust` → số `AttendanceAdjustment` `status=pending`) — count=0 thì ẩn hẳn mục đó, không hiện "0 việc cần làm". Frontend: [`Dashboard.vue`](resources/js/views/Dashboard.vue) thêm khối ngay dưới `StatCards`, mỗi mục là 1 thẻ bấm được (`router.push` sang đúng trang duyệt tương ứng). Tự làm mới realtime bằng cách TÁI DÙNG 3 tín hiệu đã có ở mục 33/34 (không thêm Event mới nào): `attendanceFeed.approvalSignal`, `leaveFeed.signal` (đã kết nối sẵn theo phiên đăng nhập), và `resourceSync.signals.attendance_adjustments` (store page-scoped — Dashboard.vue tự `connect()`/`disconnect()` theo `onMounted`/`onUnmounted`, chỉ khi user có `attendance.adjust`). Test: [`DashboardTest.php`](tests/Feature/DashboardTest.php) thêm 3 test (rỗng khi không có gì chờ, HR thấy đủ cả 3 mục, Manager chỉ thấy đúng 1 mục nghỉ phép — không thấy 2 mục chấm công vì thiếu quyền). |
| **Sửa bug thật (cùng ngày)** — bấm "Chấm công chờ duyệt" không tìm thấy bản ghi cần xử lý | Người dùng test demo phát hiện: Dashboard đếm "1 chờ duyệt" nhưng vào "Tổng hợp chấm công" lại thấy "1 Vắng", không có "Chờ duyệt" nào. Nguyên nhân xác nhận qua tinker trên DB dev thật: đếm ở Dashboard KHÔNG giới hạn theo ngày (toàn bộ lịch sử), còn [`AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue) chỉ xem được ĐÚNG 1 NGÀY tại 1 thời điểm (mặc định hôm nay) — bản ghi chờ duyệt thật sự từ 2 NGÀY TRƯỚC, hôm nay nhân viên đó không có bản ghi nào nên hiện "Vắng". Sửa: `DashboardService::actionItems()` kèm thêm `route_query.date` = ngày SỚM NHẤT còn tồn đọng (`(clone $pendingQuery)->orderBy('attendance_date')->value('attendance_date')?->toDateString()` — **BẪY THẬT đã gặp lúc sửa**: `value()` vẫn đi qua cast `date:Y-m-d` của Attendance nên trả về `Carbon`, không phải chuỗi — thiếu `?->toDateString()` thì JSON serialize mặc định của Carbon đổi sang UTC rồi LỆCH SANG NGÀY HÔM TRƯỚC, đã tự phát hiện qua tinker trước khi lỗi này lọt ra thật). [`Dashboard.vue`](resources/js/views/Dashboard.vue) truyền `query: item.route_query` khi `router.push()`; [`AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue) đọc `route.query.date` lúc khởi tạo `date` ref, không có thì vẫn mặc định hôm nay như cũ. Test: `DashboardTest.php::test_attendance_approval_item_points_to_the_date_with_the_oldest_pending_record`. **Chỉ áp dụng cho `attendance_approval`** — 2 mục còn lại (`leave_approval`/`attendance_adjustment`) không bị vấn đề này vì 2 trang tương ứng vốn đã hiện TẤT CẢ ngày cùng lúc (chỉ lọc theo `status`, không lọc theo ngày). |
| **Sửa bug thật SÂU HƠN (cùng ngày)** — bản ghi chờ duyệt VẪN biến mất dù đã xem ĐÚNG ngày | Người dùng báo lại "vẫn còn lỗi" sau fix ở dòng trên — dùng tinker gọi thẳng `AttendanceService::dailyOverview('2026-09-23', ...)` để xác nhận: đúng ngày rồi nhưng nhân viên đó vẫn không hiện "Chờ duyệt", chỉ hiện "Vắng". Nguyên nhân gốc: nhân viên chấm công lúc 13:33 dưới 1 Ca, 10 PHÚT SAU (13:43, CÙNG NGÀY) HR đổi ca cho họ — bản gán ca CŨ bị xóa mềm, rồi chính Ca làm việc CŨ cũng bị xóa mềm. `dailyOverview()` dựng danh sách dòng từ `listAssignmentsForDate()` (CHỈ thấy bản gán CÒN HIỆU LỰC — xóa mềm là global scope, loại khỏi MỌI truy vấn kể cả xem lại NGÀY TRONG QUÁ KHỨ lúc nó còn hợp lệ), nên bản ghi chấm công (vẫn nguyên trong bảng `attendances`) không khớp được với dòng nào → "biến mất" hoàn toàn khỏi màn hình, không phải chỉ lệch ngày. Sửa 2 lớp ở [`AttendanceService::dailyOverview()`](app/Services/AttendanceService.php): (1) thêm bước "lưới an toàn" — sau khi dựng dòng từ assignment, DUYỆT LẠI `$attendancesByKey` tìm bản ghi chấm công nào KHÔNG khớp dòng nào đã có (theo key `employee_id\|work_shift_id`), tự thêm thành dòng riêng (vẫn tôn trọng filter `$departmentId`/`$workShiftId` truyền vào); (2) [`AttendanceRepository::listAttendancesForDate()`](app/Repositories/AttendanceRepository.php) nạp quan hệ `workShift` kèm `->withTrashed()` — vì CHÍNH Ca làm việc cũng có thể đã bị xóa mềm (đúng kịch bản thật), không `withTrashed()` thì quan hệ trả NULL, bản ghi lại bị lọc mất LẦN NỮA ở bước "lưới an toàn" vừa thêm. Test tái hiện đúng kịch bản: `AttendanceOverviewTest.php::test_overview_still_shows_attendance_after_same_day_reassignment_and_shift_deletion` — xác nhận KHÔNG phá `test_overview_ignores_orphaned_assignment_pointing_to_a_deleted_work_shift` đã có từ trước (case đó nhân viên hoàn toàn KHÔNG có bản ghi chấm công, nên "lưới an toàn" không có gì để cứu, vẫn đúng hành vi cũ: không hiện). |
| **Bộ lọc ngày cho 3 widget số liệu** (2026-09-25, theo yêu cầu người dùng: "dashboard nên có lọc theo ngày nữa" — đã hỏi lại phạm vi qua AskUserQuestion) | Người dùng chọn: CHỈ 3 widget "Tỷ lệ đi làm"/"Xu hướng chấm công"/"Phân bổ phòng ban" lọc theo ngày — **CỐ TÌNH không đụng** `employee_stats`/`leave_pending`/`action_items` (luôn tính theo hiện tại, đặc biệt "Cần bạn xử lý" đúng bản chất là việc TỒN ĐỌNG, giới hạn theo 1 ngày sẽ gây hiểu lầm "không còn gì chờ" nếu quên đổi bộ lọc). Backend: `GET /api/v1/dashboard?date=YYYY-MM-DD` (tùy chọn, mặc định hôm nay — không validate định dạng riêng, cùng cách `AttendanceController::overview()` đã làm). `DashboardService::forUser(User $user, ?string $date = null)` truyền `$date` xuống `companyWide()` → `attendanceTrend(string $endDate)` (đổi mốc TÍNH TỪ `now()` sang `Carbon::parse($endDate)`, mẫu số vẫn xấp xỉ CỐ ĐỊNH như cũ — chấp nhận, không đào sâu thêm) và `departmentDistribution(string $date)` (lọc thêm `hire_date <= $date` VÀ `termination_date` null-hoặc->=`$date` — snapshot "as of" hợp lý, KHÔNG dựng lại lịch sử điều chuyển phòng ban thật qua `EmployeeTransfer` vì quá phức tạp cho 1 widget tổng quan, vẫn dùng `department_id` HIỆN TẠI). Frontend: [`Dashboard.vue`](resources/js/views/Dashboard.vue) thêm `InputDate v-model="statsDate"` (mặc định hôm nay, `max` chặn chọn tương lai) ngay phía trên khối 3 widget kèm chú thích rõ phạm vi ảnh hưởng, `watch(statsDate, loadDashboard)` tự tải lại; [`dashboardService.js`](resources/js/services/dashboardService.js)`::get(date)` gửi kèm query. Test: `DashboardTest.php` thêm `test_date_param_scopes_attendance_today_but_not_action_items_or_leave_pending` (chứng minh 2 lần gọi khác `date` vẫn ra `action_items` GIỐNG HỆT nhau) và `test_department_distribution_excludes_employees_not_yet_hired_on_selected_date`. |
| Danh sách tệp cần sửa khi bảo trì | [`app/Services/DashboardService.php`](app/Services/DashboardService.php), [`app/Http/Controllers/Api/V1/DashboardController.php`](app/Http/Controllers/Api/V1/DashboardController.php), [`routes/api/v1/dashboard.php`](routes/api/v1/dashboard.php), [`resources/js/services/dashboardService.js`](resources/js/services/dashboardService.js), [`resources/js/views/Dashboard.vue`](resources/js/views/Dashboard.vue), [`resources/js/components/dashboard/GaugeChart.vue`](resources/js/components/dashboard/GaugeChart.vue), [`resources/js/components/dashboard/DonutChart.vue`](resources/js/components/dashboard/DonutChart.vue), [`resources/js/components/dashboard/AreaTrendChart.vue`](resources/js/components/dashboard/AreaTrendChart.vue), [`resources/js/components/dashboard/StatCards.vue`](resources/js/components/dashboard/StatCards.vue) |

## 6. Phòng ban (Departments)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`Departments.vue`](resources/js/views/Department/Departments.vue) (cây thụt lề, tìm kiếm, lọc trạng thái, cột Trưởng phòng), [`DepartmentForm.vue`](resources/js/views/Department/DepartmentForm.vue), [`useDepartmentStore.js`](resources/js/stores/useDepartmentStore.js), [`departmentService.js`](resources/js/services/departmentService.js) |
| Xử lý Backend | [`DepartmentController.php`](app/Http/Controllers/Api/V1/DepartmentController.php), [`DepartmentService.php`](app/Services/DepartmentService.php) (`generateCode()`, `syncHeadPosition()`), [`DepartmentRepository.php`](app/Repositories/DepartmentRepository.php) (`tree()`, `wouldCreateCycle()`) |
| Model | [`Department.php`](app/Models/Department.php) — tự tham chiếu `parent()`/`children()`, `manager()` → Employee |
| Validation | [`StoreDepartmentRequest.php`](app/Http/Requests/Department/StoreDepartmentRequest.php)/[`UpdateDepartmentRequest.php`](app/Http/Requests/Department/UpdateDepartmentRequest.php) — không có rule cho `code` (tự sinh) |
| Output / Resource | [`DepartmentResource.php`](app/Http/Resources/DepartmentResource.php) — `manager` bọc qua `EmployeeResource` (ẩn field nhạy cảm đúng logic), `children` đệ quy |
| Database & API | `departments`; `GET/POST /departments`, `GET /departments/tree`, `PUT/DELETE /departments/{id}` ([`routes/api/v1/departments.php`](routes/api/v1/departments.php)); quyền `department.view`/`department.manage` |
| Test | [`DepartmentTest.php`](tests/Feature/Department/DepartmentTest.php) |
| Ghi chú | Gán/đổi `manager_id` tự đồng bộ Chức vụ qua `syncHeadPosition()` (mục 7, dựa cột `positions.type`, không so tên chuỗi). Xóa phòng ban còn con bị chặn ở backend, không chỉ ở UI |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 7. Chức vụ (Position)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`Positions.vue`](resources/js/views/Position/Positions.vue), [`PositionForm.vue`](resources/js/views/Position/PositionForm.vue) — không dùng Pinia store (chỉ 1 trang dùng) |
| Xử lý Backend | [`PositionController.php`](app/Http/Controllers/Api/V1/PositionController.php), [`PositionService.php`](app/Services/PositionService.php) (`generateCode()`, `ensureHeadPosition()`/`ensureDefaultPosition()`), [`PositionRepository.php`](app/Repositories/PositionRepository.php) |
| Model | [`Position.php`](app/Models/Position.php) — `belongsTo` Department, cột `type` (`null`/`head`/`default`) |
| Database & API | `positions`; `GET/POST /positions`, `PUT/DELETE /positions/{id}` ([`routes/api/v1/positions.php`](routes/api/v1/positions.php)); dùng chung quyền `department.view`/`department.manage` |
| Test | [`PositionTest.php`](tests/Feature/Position/PositionTest.php) |
| Ghi chú | 2 bản ghi hệ thống (`type=head`/`default`) bị khóa Sửa/Xóa trên UI, tránh phá đồng bộ tự động với Department/EmployeeTransfer |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 8. Nhân viên (Employee)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`Employees.vue`](resources/js/views/Employee/Employees.vue) (phân trang server-side, `StatCards` 4 thẻ), [`EmployeeForm.vue`](resources/js/views/Employee/EmployeeForm.vue) (Chức vụ lọc theo Phòng ban, Quản lý trực tiếp dùng ref riêng không qua store chung, Tỉnh/Xã cascading, nút "Tạo nhanh" Phòng ban/Chức vụ ngay trong form), [`useEmployeeStore.js`](resources/js/stores/useEmployeeStore.js), [`employeeService.js`](resources/js/services/employeeService.js). **Bảng gộp cột** (2026-09-24, theo yêu cầu người dùng: "gộp mấy cột... để nhìn nó gọn") — 6 cột cũ (Mã/Họ tên/Phòng ban/Chức vụ/Email/Trạng thái) rút còn 4: cột "Nhân viên" gộp avatar+họ tên+mã+email+phòng ban·chức vụ vào 1 ô nhiều dòng (`#item.full_name`), tách riêng thêm 2 cột mới "Lương" (`agreed_salary`) và "Ngày vào làm" (`hire_date`), giữ nguyên "Trạng thái". **Thêm cột "Nghỉ phép"** (2026-09-24, theo yêu cầu người dùng: "hiển thị thêm cột nghỉ phép số ngày nghỉ phép") — hiện `"{còn lại}/{được cấp} ngày"` của quỹ "Nghỉ phép năm" NĂM NAY (`leave_remaining_days`/`leave_allocated_days`), `formatLeaveDays()` — chưa có bản ghi `LeaveBalance` nào (rất mới, tự hết sau tối đa 1 ngày nhờ job `leave:sync-accrual` — mục 29) hoặc bị ẩn vì cấp dưới thì hiện "—", cùng cách xử lý với cột "Lương". |
| Chi tiết nhân viên (6 tab) | [`EmployeeDetail.vue`](resources/js/views/Employee/EmployeeDetail.vue) — chỉ còn khung `v-tabs`/`v-window` + tải `employee` chính + `FilePreviewDialog` dùng chung. Mỗi tab tách thành component riêng (dễ bảo trì hơn 1 file gộp cả 1865 dòng): [`EmployeeProfileTab.vue`](resources/js/views/Employee/EmployeeProfileTab.vue) (Sơ yếu lý lịch + dialog tạo tài khoản đăng nhập), [`EmployeeContractsTab.vue`](resources/js/views/Employee/EmployeeContractsTab.vue), [`EmployeeDocumentsTab.vue`](resources/js/views/Employee/EmployeeDocumentsTab.vue), [`EmployeeTransfersTab.vue`](resources/js/views/Employee/EmployeeTransfersTab.vue), [`EmployeeShiftAssignmentsTab.vue`](resources/js/views/Employee/EmployeeShiftAssignmentsTab.vue); tab "Chấm công" dùng `AttendanceHistoryPanel.vue` (mục 18), tab "Lương/Phép" dùng [`EmployeeSalaryLeaveTab.vue`](resources/js/views/Employee/EmployeeSalaryLeaveTab.vue) (bổ sung 2026-09-16 — trước đó là placeholder tĩnh từ Ngày 27, để chờ 2 module Payroll/Nghỉ phép làm xong; giờ ghép lại bằng 2 API mới: `GET /leave-requests/balances/{employee}` và `GET /employees/{employee}/payslips`, dialog "Xem phiếu lương" tái dùng `PayrollPayslipDialog.vue` — mục 27). Mỗi tab con tự gọi API riêng của nó (`onMounted`), component cha chỉ `v-if` mount lần đầu khi mở đúng tab (không cần cờ "đã tải" nữa vì `v-window` không unmount lại tab đã mở); tab con nào cần mở `FilePreviewDialog`/báo tải lại `employee` thì `emit` sự kiện lên cha (`preview`, `account-created`, `transferred`) |
| Xử lý Backend | [`EmployeeController.php`](app/Http/Controllers/Api/V1/EmployeeController.php), [`EmployeeService.php`](app/Services/EmployeeService.php) (`generateCode()`, `wouldCreateCycle()`, `updateAvatar()`, `stats()`), [`EmployeeRepository.php`](app/Repositories/EmployeeRepository.php) |
| **Hợp đồng ĐẦU TIÊN tự tạo cùng lúc tạo nhân viên** (2026-09-24, theo yêu cầu người dùng: "điền lương cơ bản vào luôn... không cần tạo hđ như bây giờ") | **Đối chiếu tài liệu**: `Ke-hoach-trien-khai-du-an-QLNS.docx` coi "Hợp đồng" là tab RIÊNG bắt buộc (Ngày 27) và Payroll tính "dựa trên... hợp đồng" (Ngày 47) — mâu thuẫn trực tiếp với yêu cầu "không cần tạo hđ", đã dừng lại hỏi rõ trước khi làm. Đã CHỐT: giữ nguyên kiến trúc Payroll dựa trên `EmployeeContract` (đúng tài liệu), chỉ bỏ bước THAO TÁC THỦ CÔNG — `EmployeeService::create()` gọi luôn `EmployeeContractService::create()` trong CÙNG transaction. `contract_type` suy từ `employment_status` (`active` → `chinh_thuc`, còn lại/mặc định `probation` → `thu_viec`) — không hỏi thêm field riêng. `start_date` DÙNG CHUNG `hire_date` — không hỏi ngày thứ 2. `EmployeeContractService::create()` đổi `$file` thành **nullable** (trước đó bắt buộc) — Hợp đồng tự tạo ở bước này CHƯA có file bản giấy đã ký, HR upload sau ở tab "Hợp đồng" (form riêng ở đó vẫn giữ nguyên `contract_file` bắt buộc khi ký hợp đồng MỚI/tiếp theo, KHÔNG áp dụng nới lỏng này). `StoreEmployeeRequest` thêm `agreed_salary` (bắt buộc). |
| **Lương đóng BHXH KHÔNG còn là field nhập riêng** (2026-09-24, theo yêu cầu người dùng: "lương đóng bh sẽ tính là lương cb luôn không tách ra" — hỏi lại phạm vi, người dùng CHỌN áp dụng TOÀN HỆ THỐNG) | `EmployeeContractService::create()` ghi đè `$data['insurance_salary'] = $data['agreed_salary']` NGAY ĐẦU HÀM, bất kể client gửi gì lên — đây là điểm ĐỦ để áp dụng nhất quán cho MỌI đường tạo hợp đồng: cả Hợp đồng tự tạo lúc tạo nhân viên (`EmployeeService::create()`) lẫn tạo tay ở tab "Hợp đồng" (`EmployeeContractController::store()`). Bỏ hẳn field `insurance_salary` khỏi validation (`StoreEmployeeContractRequest`, `StoreEmployeeRequest` — rule `required_if:contract_type,chinh_thuc` cũ đã xóa) và khỏi UI (`EmployeeContractsTab.vue` — bỏ luôn ô nhập + watcher tự xóa giá trị khi đổi loại hợp đồng đã hết tác dụng; `EmployeeForm.vue` — bỏ ô "Lương đóng BHXH"). Dialog xem chi tiết hợp đồng (`EmployeeContractsTab.vue`, `MyProfileContractsTab.vue`) gộp còn 1 dòng "Lương thỏa thuận (= lương đóng BHXH)", không hiện 2 số giống hệt nhau. Cột `insurance_salary` trong DB **vẫn giữ nguyên** (không xóa) — `PayrollService` vẫn đọc từ đó để tính bảo hiểm, chỉ là giá trị giờ LUÔN bằng `agreed_salary`. |
| Hợp đồng | [`EmployeeContractController.php`](app/Http/Controllers/Api/V1/EmployeeContractController.php)/[`EmployeeContractService.php`](app/Services/EmployeeContractService.php)/[`EmployeeContractRepository.php`](app/Repositories/EmployeeContractRepository.php) — Tạo+Xem+Tải+Chấm dứt (không Sửa/Xóa thô), IDOR check ở `download()`/`terminate()` (so `employee_id` trước khi thao tác). Số hợp đồng TỰ SINH theo `contract_type` (`HDTV-xxx` thử việc / `HDCT-xxx` chính thức, `EmployeeContractService::generateContractNumber()`), client gửi lên bị bỏ qua. **4 trạng thái vòng đời hợp đồng** — `pending` (đã ký, chưa tới ngày bắt đầu) → `active` (đang hiệu lực) → `expired`/`terminated` (kết thúc tự nhiên/chủ động). **4 cơ chế quản lý** (bổ sung 2026-09-16, cập nhật thêm `pending` cùng ngày sau khi phát hiện ký hợp đồng TRƯỚC ngày bắt đầu làm auto-supersede đụng nhầm hợp đồng đang thật sự áp dụng): (1) *Tạo hợp đồng ký trước hạn* — `EmployeeContractService::create()` so `start_date` với hôm nay: còn ở tương lai thì tạo với `status=pending` và KHÔNG đụng gì tới hợp đồng `active` hiện có; đã tới/qua hạn thì tạo `active` luôn và mới auto-supersede (2) hợp đồng `active` cũ sang `expired` ngay trong transaction; (3) *Tự động kích hoạt hợp đồng pending* — Artisan command [`ActivatePendingContracts.php`](app/Console/Commands/ActivatePendingContracts.php) (`contracts:activate-pending`), chạy hằng ngày 00:05 (TRƯỚC `contracts:expire` chạy 00:06 — xem [`routes/console.php`](routes/console.php)), chuyển `pending` → `active` cho hợp đồng đã tới `start_date`, đồng thời supersede hợp đồng `active` khác của cùng nhân viên đúng lúc đó (không sớm hơn); (4) *Tự động hết hạn* — Artisan command [`ExpireEmployeeContracts.php`](app/Console/Commands/ExpireEmployeeContracts.php) (`contracts:expire`), chuyển hợp đồng `end_date` < hôm nay + `status=active` sang `expired`; (5) *Chấm dứt chủ động* — `EmployeeContractService::terminate()` (chỉ cho phép từ `active` — `pending`/`expired`/`terminated` đều bị chặn) đặt `status=terminated` + `terminated_at=hôm nay`, gọi qua `POST /employees/{employee}/contracts/{contract}/terminate` (`permission:employee.update`), nút "Chấm dứt hợp đồng" chỉ hiện ở [`EmployeeContractsTab.vue`](resources/js/views/Employee/EmployeeContractsTab.vue) (phía HR, `v-if status===active` nên tự động không hiện cho `pending`), KHÔNG có ở `MyProfileContractsTab.vue` (tự phục vụ, chỉ đọc). `StoreEmployeeContractRequest`'s `status` rule chỉ để tài liệu hóa enum hợp lệ — client gửi gì cũng bị Service ghi đè. Sau khi tạo hợp đồng mới, Frontend gọi lại `loadContracts()` (tải lại cả danh sách) thay vì chỉ nối thêm bản ghi mới — cần thiết để dòng hợp đồng cũ hiển thị đúng `expired` do bị auto-supersede, nối thêm đơn thuần sẽ để dòng cũ hiện sai "Còn hiệu lực" cho tới khi tải lại trang (bug thật đã vấp khi kiểm thử bằng Playwright). |
| Tài liệu | [`EmployeeDocumentController.php`](app/Http/Controllers/Api/V1/EmployeeDocumentController.php)/Service/Repository — nhận PDF/JPG/PNG/DOCX/XLSX, có Xóa mềm, tên tải xuống giữ đúng đuôi qua [`EmployeeDocument::downloadFileName()`](app/Models/EmployeeDocument.php) |
| Luân chuyển | [`EmployeeTransferController.php`](app/Http/Controllers/Api/V1/EmployeeTransferController.php)/Service/Repository — áp dụng NGAY LẬP TỨC (không chờ `effective_date`, chưa có queue/lịch chạy nền), chặn chọn trùng phòng ban hiện tại, điều chuyển 1 Trưởng phòng thì tự dọn `manager_id` phòng cũ + hạ Chức vụ mặc định ở phòng mới. **"Quản lý mới" chỉ được là Trưởng phòng của phòng ban MỚI** (2026-09-21, theo yêu cầu người dùng): `EmployeeTransferService::create()` kiểm `Department.manager_id == new_manager_id` (422 nếu khác, hoặc nếu phòng đó chưa có Trưởng phòng) TRƯỚC bước kiểm vòng lặp; dropdown ở `EmployeeTransfersTab.vue` chỉ liệt kê đúng `manager` của phòng ban đang chọn (lấy từ cây `departmentService.tree()`, không gọi thêm API), khóa lại khi chưa chọn phòng/phòng chưa có Trưởng phòng, và reset khi đổi phòng ban. Bỏ để trống thì vẫn giữ nguyên quản lý hiện tại như cũ |
| Model | [`Employee.php`](app/Models/Employee.php) — thêm `activeContract()` (2026-09-24, `hasOne` lọc `status=active`, dùng để hiện cột "Lương" ở `Employees.vue`; `hasOne` thường là đủ KHÔNG cần `ofMany()` vì vòng đời hợp đồng đảm bảo tối đa 1 bản `active` tại 1 thời điểm — xem mục "4 cơ chế quản lý" ở trên) và `currentYearLeaveBalance()` (2026-09-24, `hasOne` lọc `year=năm nay` + `whereHas('leaveType', entitlement>0)`, dùng để hiện cột "Nghỉ phép" — cùng lý do `hasOne` là đủ: chỉ đúng 1 loại phép có quỹ theo năm tại 1 thời điểm, xem mục 30), [`EmployeeContract.php`](app/Models/EmployeeContract.php), [`EmployeeBankAccount.php`](app/Models/EmployeeBankAccount.php), [`EmployeeDocument.php`](app/Models/EmployeeDocument.php), [`EmployeeTransfer.php`](app/Models/EmployeeTransfer.php) (không `SoftDeletes` — bản ghi lịch sử) |
| Validation | [`StoreEmployeeRequest.php`](app/Http/Requests/Employee/StoreEmployeeRequest.php)/[`UpdateEmployeeRequest.php`](app/Http/Requests/Employee/UpdateEmployeeRequest.php) — 14 field bắt buộc lúc tạo (13 field hồ sơ + `agreed_salary` — mục Hợp đồng tự tạo ở trên; **`UpdateEmployeeRequest` KHÔNG có 2 field lương**, sửa lương sau vẫn phải qua tab "Hợp đồng" ký hợp đồng mới), `commune_code` phải thuộc đúng `province_code` (mục 9) |
| Output / Resource | [`EmployeeResource.php`](app/Http/Resources/EmployeeResource.php) — ẩn field nhạy cảm (CCCD, SĐT, ngày sinh, **`agreed_salary`**, **`leave_allocated_days`/`leave_remaining_days`** — 2026-09-24, coi lương + số ngày nghỉ phép nhạy cảm y hệt các field kia, cùng cơ chế `ancestorIds`, không thêm quyền riêng) khi người xem là cấp dưới, `manager` lồng đệ quy qua chính Resource, cache `ancestorIds` theo viewer tránh N+1. `agreed_salary` đọc từ `activeContract` (nạp qua `loadMissing()`/`EmployeeRepository::paginate()` — mục Model), nhân viên chưa từng có hợp đồng nào (dữ liệu cũ trước 2026-09-24) trả về `null`, Frontend hiện "—". `leave_remaining_days` đọc từ `currentYearLeaveBalance` qua helper `private static leaveRemainingDays()` (công thức GIỐNG HỆT `LeaveRequestService::remainingDays()` — `allocated + carried_forward + adjusted - used`, KHÔNG trừ thêm đơn đang chờ duyệt như `available_days`), `leave_allocated_days` đọc thẳng `allocated_days`; chưa có bản ghi `LeaveBalance` nào cũng trả `null`, KHÔNG tự tính chiếu như `LeaveAccrualService::targetAllocatedDays()` |
| Database & API | `employees` (+ index `full_name`/`employment_status`), `positions`, `employee_contracts`, `employee_bank_accounts`, `employee_documents`, `employee_transfers`; `GET/POST /employees`, `/employees/stats`, `/employees/me`, `GET/PUT/DELETE /employees/{id}`, `POST /employees/{id}/avatar`, `.../contracts`, `.../documents`, `.../transfers` ([`routes/api/v1/employees.php`](routes/api/v1/employees.php) — **`/stats` và `/me` phải khai TRƯỚC `/{employee}`**); 4 quyền riêng `employee.view/create/update/delete` |
| Lưu trữ file | Avatar → disk `public` (tự xóa file cũ); hợp đồng/tài liệu → disk `local` (riêng tư, chỉ tải qua route có kiểm IDOR) |
| Giới hạn dung lượng tải lên (2026-09-21) | Giới hạn thực tế là mức THẤP NHẤT trong 3 tầng: Laravel validation (`max:` tính bằng KB) < PHP (`docker/php/php.ini`: `upload_max_filesize=20M`, `post_max_size=25M`) ≈ nginx (`docker/nginx/default.conf`: `client_max_body_size 20m`) — nên Backend là bên chặn thật. Hợp đồng **5MB** (PDF), tài liệu cá nhân **10MB** (PDF/JPG/PNG/DOCX/XLSX, dùng chung tab Tài liệu của HR + "Hồ sơ của tôi"), quyết định điều động **10MB** (PDF), giấy tờ đính kèm đơn nghỉ phép **5MB** (JPG/PNG/PDF). Ảnh đại diện có rule 2MB ở `EmployeeController::uploadAvatar` nhưng chưa có giao diện tải lên. **Frontend hiển thị giới hạn ở MỌI ô tải tệp** qua [`InputFile.vue`](resources/js/components/common/InputFile.vue) (bọc `v-file-input`, dòng gợi ý "Định dạng …, tối đa …MB" luôn hiện kể cả sau khi chọn file) — số MB/định dạng chỉ khai báo MỘT chỗ trong `UPLOAD_LIMITS` (export từ chính file đó), **mỗi mục phải khớp rule Backend tương ứng** (ghi rõ Request nào ngay trong comment), đổi 1 bên phải đổi cả bên kia. Thêm chỗ upload mới → dùng `<InputFile :limit="UPLOAD_LIMITS.xxx" />`, không tự viết `v-file-input` + `placeholder` ghi số MB. Chưa chặn dung lượng ở phía Frontend (chỉ hiển thị); file >20MB bị nginx trả 413 không kèm JSON nên giao diện chỉ hiện lỗi chung |
| Test | [`EmployeeTest.php`](tests/Feature/Employee/EmployeeTest.php) (bao gồm 3 test cho Hợp đồng tự tạo: `test_creating_employee_auto_creates_first_contract`, `test_active_employment_status_creates_official_contract`, `test_contract_start_date_matches_hire_date_and_is_pending_when_hire_date_is_in_the_future`; 2 test cho cột "Lương": `test_employee_data_includes_current_agreed_salary_from_active_contract`, `test_subordinate_cannot_see_superior_agreed_salary`; 4 test mới cho cột "Nghỉ phép": `test_employee_data_includes_current_year_leave_remaining_days`, `test_leave_balance_ignores_non_entitled_leave_types`, `test_employee_without_leave_balance_shows_null_leave_days`, `test_subordinate_cannot_see_superior_leave_balance`), [`EmployeeContractTest.php`](tests/Feature/Employee/EmployeeContractTest.php) (bao gồm auto-supersede/pending/terminate, `test_insurance_salary_always_matches_agreed_salary`), [`EmployeeDocumentTest.php`](tests/Feature/Employee/EmployeeDocumentTest.php), [`EmployeeTransferTest.php`](tests/Feature/Employee/EmployeeTransferTest.php), [`ExpireEmployeeContractsTest.php`](tests/Feature/Console/ExpireEmployeeContractsTest.php) (Artisan command `contracts:expire`), [`ActivatePendingContractsTest.php`](tests/Feature/Console/ActivatePendingContractsTest.php) (Artisan command `contracts:activate-pending`) |
| Ghi chú | Không chặn xóa nhân viên còn cấp dưới (khác Department). Docker: tạo thư mục `storage/app/private/...` bằng tinker/root trước sẽ khiến request thật (`www-data`) bị Permission denied |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 9. Địa chỉ hành chính (Tỉnh/Xã)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`addressService.js`](resources/js/services/addressService.js) (`provinces()`, `communes(provinceCode)`) — dùng trong `EmployeeForm.vue` |
| Xử lý Backend | [`AddressController.php`](app/Http/Controllers/Api/V1/AddressController.php) — chỉ đọc |
| Model | [`Province.php`](app/Models/Province.php), [`Commune.php`](app/Models/Commune.php) — PK là `code`, không tự tăng |
| Database & API | `provinces`, `communes`; `GET /addresses/provinces`, `/addresses/communes?province_code=` |
| Nguồn dữ liệu | Tải nguyên trạng từ [provinces.open-api.vn](https://provinces.open-api.vn) (34 Tỉnh, 3321 Xã — theo sáp nhập 2025, không còn Huyện), lưu tĩnh ở [`database/seeders/data/`](database/seeders/data), nạp qua [`ProvinceCommuneSeeder.php`](database/seeders/ProvinceCommuneSeeder.php) |
| Test | Trong [`EmployeeTest.php`](tests/Feature/Employee/EmployeeTest.php) |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 10. Hạ tầng chung

| Hạng mục | Chi tiết |
| --- | --- |
| Axios | [`bootstrap.js`](resources/js/bootstrap.js) — tự gắn `Authorization`, tự xử lý 401/403 |
| Component dùng chung | [`components/common/`](resources/js/components/common): `FormDialog.vue` (khung dialog — cần prop **`scrollable`** để form dài cuộn đúng, thiếu thì bị cắt cụt), `FormSection.vue`, `DataTable.vue` (prop `actions` tự dựng cột Thao tác + hộp xác nhận, xem `confirm.input`), `SearchField.vue`, `PageHeader.vue`, `StatusChip.vue` (so khớp qua `String(status)`), `SearchSelect.vue` (bọc `v-autocomplete`, tìm không dấu qua `normalizeVietnamese()`, không khai prop `modelValue` — rơi qua `$attrs`), `InputDate.vue` (bọc `VDateInput` của `vuetify/labs`, export thêm `parseIsoDate`/`toIsoDate`/`todayIso`/`shiftIsoDate`), `InputMoney.vue`, `InputFile.vue` (ô chọn tệp — kèm dòng hiển thị giới hạn dung lượng, xem mục 8 "Giới hạn dung lượng tải lên"), `FilePreviewDialog.vue` (xem trước PDF/ảnh/DOCX/XLSX, 3 thư viện `import()` động để lazy-load), `AppToast.vue` + [`useToastStore.js`](resources/js/stores/useToastStore.js) (không dùng `v-snackbar-queue` — không tự tắt ở Vuetify 3.13.3, tự viết state máy), `AppLoadingBar.vue` + [`useLoadingStore.js`](resources/js/stores/useLoadingStore.js) (đếm số việc đang chạy, không phải cờ bool) |
| Glassmorphism | Class `.glass-panel` tại [`resources/css/app.css`](resources/css/app.css) |
| Docker | [`docker-compose.yml`](docker-compose.yml), chi tiết ở [`docs/LOCAL_DEVELOPMENT.md`](docs/LOCAL_DEVELOPMENT.md) |
| Swagger | [`config/l5-swagger.php`](config/l5-swagger.php), xem tại `/api/documentation`, sinh lại bằng `php artisan l5-swagger:generate` |
| Ghi chú | `FilePreviewDialog`: `pdfDoc` phải là `shallowRef` (không phải `ref`), giải phóng bằng `destroy()` của loading task (không phải `cleanup()`), hủy `renderTask` cũ trước khi vẽ lại. Namespace hoa/thường: Controller khai `Api\V1` nhưng git từng track thư mục `Api/v1/` — đã sửa bằng `git mv` 2 bước; NTFS không phân biệt hoa/thường nhưng Linux (CI/server) thì có, nhớ kiểm tra lại nếu thêm Controller mới. `DataTable.vue` dùng Vuetify 3 (không phải 4) — slot `#bottom` không có `setPage`/`itemsLength`, bảng tự quản lý `page` qua `v-model:page` + đọc `serverItemsLength` tự truyền vào |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 11. Tạo tài khoản đăng nhập cho Nhân viên

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`EmployeeDetail.vue`](resources/js/views/Employee/EmployeeDetail.vue) (nút "Tạo tài khoản đăng nhập"), [`EmployeeForm.vue`](resources/js/views/Employee/EmployeeForm.vue) (checkbox tạo ngay lúc Thêm mới), [`roleService.js`](resources/js/services/roleService.js) |
| Xử lý Backend | [`EmployeeAccountController.php`](app/Http/Controllers/Api/V1/EmployeeAccountController.php), [`EmployeeAccountService.php`](app/Services/EmployeeAccountService.php) (chặn tạo trùng, transaction tạo User + attach Role), [`RoleController.php`](app/Http/Controllers/Api/V1/RoleController.php) |
| Gợi ý Role theo Chức vụ | `PositionService::suggestRole()` ghi vào `role_positions` khi tự sinh Position lần đầu (Trưởng phòng → Manager, Nhân viên → Employee) |
| Database & API | `role_positions`; `POST /employees/{id}/account`, `GET /roles` — quyền `employee.update` |
| Test | [`EmployeeAccountTest.php`](tests/Feature/Employee/EmployeeAccountTest.php) |
| Ghi chú | Mật khẩu ngẫu nhiên nội bộ, tái dùng luồng "quên mật khẩu" (mục 1) để gửi email đặt mật khẩu lần đầu — không đặt/lộ mật khẩu trần |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 12. Ca làm việc (Work Shift)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`WorkShifts.vue`](resources/js/views/WorkShift/WorkShifts.vue) (cột "Ca mặc định"), [`WorkShiftForm.vue`](resources/js/views/WorkShift/WorkShiftForm.vue) (giờ dùng native `type="time"`) |
| Xử lý Backend | [`WorkShiftController.php`](app/Http/Controllers/Api/V1/WorkShiftController.php), [`WorkShiftService.php`](app/Services/WorkShiftService.php) (`generateCode()`; tắt `is_active` thì tự gỡ Ca khỏi mọi nhân viên đang gán; `getDefault()`/`clearOtherDefaults()` — mục 28), [`WorkShiftRepository.php`](app/Repositories/WorkShiftRepository.php) |
| Model | [`WorkShift.php`](app/Models/WorkShift.php) — `is_default` (bool), `break_start_time`/`break_end_time` (2026-09-23, thay `break_minutes` cũ — xem mục 28) |
| Database & API | `work_shifts`; `GET/POST /work-shifts`, `GET /work-shifts/default`, `PUT/DELETE /work-shifts/{id}` ([`routes/api/v1/work-shifts.php`](routes/api/v1/work-shifts.php)); quyền `shift.view`/`shift.manage` (Manager chỉ có `view`) |
| Test | [`WorkShiftTest.php`](tests/Feature/WorkShift/WorkShiftTest.php) |
| Ghi chú | Chưa xử lý ca qua đêm (`end_time < start_time`). Xem mục 28 cho "Ca mặc định" + giờ nghỉ trưa. |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 13. Điểm chấm công (Attendance Location)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`AttendanceLocations.vue`](resources/js/views/AttendanceLocation/AttendanceLocations.vue), [`AttendanceLocationForm.vue`](resources/js/views/AttendanceLocation/AttendanceLocationForm.vue) (form đổi field theo `method`) |
| Xử lý Backend | [`AttendanceLocationController.php`](app/Http/Controllers/Api/V1/AttendanceLocationController.php), [`AttendanceLocationService.php`](app/Services/AttendanceLocationService.php) (`generateCode()`, tự sinh `qr_secret` — không nhận từ client) |
| Model | [`AttendanceLocation.php`](app/Models/AttendanceLocation.php) |
| Database & API | `attendance_locations`; `GET/POST /attendance-locations`, `PUT/DELETE .../{id}`; quyền `location.view`/`location.manage` (chỉ HR, Manager không có) |
| Test | [`AttendanceLocationTest.php`](tests/Feature/AttendanceLocation/AttendanceLocationTest.php) |
| Ghi chú | Từ 2026-09-21 đây là danh sách địa điểm của công ty **để đối chiếu** với IP/vị trí/QR của thiết bị chấm công (xem mục 16), không phải "phương thức nhân viên chọn". Giao diện đổi chữ tương ứng: cột/ô "Phương thức" → "Cách nhận diện" (giá trị `method` vẫn là `wifi`/`gps`/`qr`) |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 14. Gán ca làm việc cho nhân viên

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | Tab "Ca làm việc" trong [`EmployeeDetail.vue`](resources/js/views/Employee/EmployeeDetail.vue) |
| Xử lý Backend | [`EmployeeShiftAssignmentController.php`](app/Http/Controllers/Api/V1/EmployeeShiftAssignmentController.php)/Service/Repository |
| Model | [`EmployeeShiftAssignment.php`](app/Models/EmployeeShiftAssignment.php) (`work_days` cast `array`) |
| Quy tắc nghiệp vụ | 1 nhân viên gán được **nhiều ca**, chỉ cấm 2 ca **chồng giờ + chồng ngày trong tuần + chồng khoảng hiệu lực** (`assertNoConflict()`) |
| Database & API | `employee_shift_assignments`; `.../employees/{id}/shift-assignments`; quyền `shift.view`/`shift.manage`; có `mine()` |
| Test | [`EmployeeShiftAssignmentTest.php`](tests/Feature/Employee/EmployeeShiftAssignmentTest.php) |
| Ghi chú | `work_shift_id` phải đang `is_active=1` (không chỉ tồn tại), IDOR check ở update/destroy. **KHÔNG còn là bước bắt buộc** (2026-09-24, theo yêu cầu người dùng) — mọi nhân viên active/probation tự động có "Ca mặc định" (mục 28) mà KHÔNG cần HR vào đây gán tay; tab này giờ chỉ dùng khi thật sự cần 1 người có lịch RIÊNG hẳn khác mặc định (ca đêm, part-time cố định...) — gán 1 bản ở đây thì bản đó THẮNG, nhân viên không còn rơi vào diện tự động nữa (xem mục 28, `AssignMissingDefaultShift` chỉ đụng người **KHÔNG có** bản gán `active` nào). |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 15. Hồ sơ của tôi (My Profile)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`MyProfile.vue`](resources/js/views/Me/MyProfile.vue) — chỉ còn khung `v-tabs`/`v-window` + thẻ hero/`StatCards` đầu trang + `FilePreviewDialog` dùng chung. 5 tab tách component riêng (cùng khuôn `EmployeeDetail.vue`, mục 8): [`MyProfileInfoTab.vue`](resources/js/views/Me/MyProfileInfoTab.vue) (Thông tin cá nhân + dialog sửa liên hệ), [`MyProfileContractsTab.vue`](resources/js/views/Me/MyProfileContractsTab.vue), [`MyProfileDocumentsTab.vue`](resources/js/views/Me/MyProfileDocumentsTab.vue) (có tự nộp tài liệu), [`MyProfileShiftsTab.vue`](resources/js/views/Me/MyProfileShiftsTab.vue), [`MyProfileTransfersTab.vue`](resources/js/views/Me/MyProfileTransfersTab.vue) (2 tab sau chỉ đọc, không có thao tác) |
| Xử lý Backend | `mine()` thêm vào 4 Controller (Contract/Document/ShiftAssignment/Transfer), `EmployeeController::updateMine()`, `EmployeeDocumentController::storeMine()` |
| Validation | [`UpdateMyProfileRequest.php`](app/Http/Requests/Employee/UpdateMyProfileRequest.php) — chỉ 5 field liên hệ (`phone`/`personal_email`/`address_detail`/`province_code`/`commune_code`), không cho tự sửa field định danh/công việc |
| Database & API | `/employees/me`, `PUT /employees/me`, `.../me/documents`, `.../me/contracts`, `.../me/shift-assignments`, `.../me/transfers` — chỉ cần `auth:api`, không cần permission |
| Test | Rải trong `EmployeeTest.php`/`EmployeeContractTest.php`/`EmployeeDocumentTest.php`/`EmployeeShiftAssignmentTest.php`/`EmployeeTransferTest.php` |
| Ghi chú | Route tải Hợp đồng/Tài liệu bỏ `permission:` ở route, tự kiểm tra "chính chủ HOẶC có quyền" bên trong hàm |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 16. Chấm công (Attendance check-in/out)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`CheckIn.vue`](resources/js/views/Attendance/CheckIn.vue) — nay là switcher desktop/mobile, xem mục 26 |
| Xử lý Backend | [`AttendanceController.php`](app/Http/Controllers/Api/V1/AttendanceController.php) (`checkIn`/`checkOut`/`today`/`mine`/`index`), [`AttendanceService.php`](app/Services/AttendanceService.php) (khớp điểm/ca, tính trễ-sớm-OT, `captureDeviceContext()`), [`Services/Attendance/DeviceInfoParser.php`](app/Services/Attendance/DeviceInfoParser.php) (User-Agent → tên thiết bị), [`Services/Attendance/ReverseGeocoder.php`](app/Services/Attendance/ReverseGeocoder.php) (tọa độ → địa chỉ chữ qua Nominatim/OpenStreetMap), [`AttendanceRepository.php`](app/Repositories/AttendanceRepository.php), [`AttendanceLogRepository.php`](app/Repositories/AttendanceLogRepository.php) |
| Model | [`Attendance.php`](app/Models/Attendance.php), [`AttendanceLog.php`](app/Models/AttendanceLog.php) (`const UPDATED_AT = null`, không `SoftDeletes` — log bất biến) |
| Validation | [`CheckInRequest.php`](app/Http/Requests/Attendance/CheckInRequest.php)/[`CheckOutRequest.php`](app/Http/Requests/Attendance/CheckOutRequest.php) — **không còn field `method`** (2026-09-21): chỉ `work_shift_id` bắt buộc; `latitude`/`longitude` tùy chọn nhưng phải đi cặp đôi (`required_with`), `qr_reference` tùy chọn |
| Database & API | `attendances` (unique `employee_id`+`attendance_date`+`work_shift_id` — 1 dòng/CA/ngày, cho phép nhiều ca cùng ngày), `attendance_logs` (thêm 2026-09-21 [`..._add_device_info_to_attendance_logs_table.php`](database/migrations/2026_09_21_000002_add_device_info_to_attendance_logs_table.php): `address`, `device_name`, `user_agent`, đều nullable); `POST /check-in`, `/check-out`, `GET /today`, `/me`, `/` (HR) ([`routes/api/v1/attendances.php`](routes/api/v1/attendances.php)); quyền `attendance.check`/`view_own`/`view_all` |
| Test | [`AttendanceTest.php`](tests/Feature/Attendance/AttendanceTest.php) (có nhóm test ghi IP/địa chỉ/thiết bị, luôn `Http::preventStrayRequests()` + `fakeNominatim()` — test không bao giờ gọi mạng thật), [`AttendanceServiceCalculationTest.php`](tests/Unit/Services/AttendanceServiceCalculationTest.php) (Unit — bao gồm ranh giới ân hạn OT), [`DeviceInfoParserTest.php`](tests/Unit/Services/Attendance/DeviceInfoParserTest.php), [`ReverseGeocoderTest.php`](tests/Unit/Services/Attendance/ReverseGeocoderTest.php), [`AttendanceApprovalTest.php`](tests/Feature/Attendance/AttendanceApprovalTest.php) (duyệt chấm công) |
| Hiển thị giờ | [`composables/useCheckIn.js`](resources/js/composables/useCheckIn.js) export `formatMinutesAsHours()` — Trễ/Về sớm/OT hiện dạng `"0.5h (30 phút)"` để dễ đối chiếu khi tính lương sau này (Phase 4). Chỉ đổi hiển thị — backend vẫn lưu/cộng dồn nguyên phút, chỉ quy đổi ra giờ 1 lần lúc tính lương để tránh sai số làm tròn tích lũy qua nhiều ngày |
| **Duyệt chấm công** (2026-09-21, theo yêu cầu người dùng: mọi lượt chấm công phải được HR duyệt, chưa duyệt thì không tính công/lương) | **Đơn vị duyệt = 1 bản ghi `attendances` (ca + ngày)**, gồm cả lượt vào lẫn ra — không duyệt riêng từng log. Cột mới (migration [`2026_09_21_000003_add_approval_to_attendances_table.php`](database/migrations/2026_09_21_000003_add_approval_to_attendances_table.php)): `approval_status` (`pending`/`approved`/`rejected`, mặc định `pending`, hằng số `Attendance::APPROVAL_*`), `approved_by`, `approved_at`, `approval_note`. **TÁCH RIÊNG khỏi `status`** (`pending`/`completed`/`needs_review` = trạng thái CA làm việc): 2 trục độc lập, 1 bản ghi có thể `completed` mà vẫn chờ duyệt. Migration **tự chuyển mọi bản ghi CÓ SẴN sang `approved`** (ghi chú "Tự động duyệt…") để lịch sử và bảng lương các tháng trước không bị mất công — chỉ lượt chấm công SAU thời điểm này mới phải duyệt. Quyền mới **`attendance.approve`** (Admin + HR; Manager/Employee không có) trong `PermissionSeeder`/`RolePermissionSeeder` — DB đã có sẵn phải chạy lại 2 seeder này. API: `PUT /attendances/{attendance}/approval` (`whereNumber`, body `status`=`approved`/`rejected` + `decision_note`, **từ chối bắt buộc có lý do**) → `AttendanceController::decideApproval()` → `AttendanceService::decideApproval()`. Xem danh sách/lọc qua "Tổng hợp chấm công trong ngày" ngay dưới đây (không còn màn riêng chỉ để duyệt). Quy tắc: duyệt được **NGAY TỪ LÚC CHẤM CÔNG VÀO**, không cần chờ chấm công ra (2026-09-23, sửa lại theo đúng ý người dùng — bản đầu 2026-09-21 bắt phải chấm công ra mới cho duyệt, HIỂU SAI mục đích: việc duyệt là XÁC NHẬN LƯỢT CHẤM CÔNG VÀO có thật hay không — HR nhìn giờ vào/thiết bị/vị trí ngay khi nhân viên vừa vào ca để biết đi sớm/trễ, có thật sự tới nơi làm việc hay không, không phải xác nhận giờ công CUỐI NGÀY); chỉ đòi hỏi ĐÃ có `first_check_in_at` (bản ghi thật sự luôn có ngay khi tạo, xem `AttendanceRepository::findOrCreateForShift()` — kiểm tra này chỉ để phòng vệ); được ĐỔI quyết định (duyệt ↔ từ chối, vì HR có thể bấm nhầm), chặn quyết định trùng trạng thái hiện tại. **Duyệt yêu cầu điều chỉnh/bổ sung công (`correction`/`supplement`) = duyệt luôn bản ghi** (`AttendanceAdjustmentService::decide()`, tránh bắt HR duyệt 2 lần; `excuse`/`overtime`/từ chối thì KHÔNG đụng). **Ảnh hưởng tính công**: `PayrollService::calculateWorkedMetrics()` chỉ lấy `approval_status=approved`; `AttendanceService::summarizeHistory()` cũng chỉ cộng `total_work_days`/`total_work_minutes` từ bản ghi đã duyệt (cùng luật để số ở màn lịch sử khớp phiếu lương) và trả thêm `unapproved_count` (thẻ "Chưa được duyệt" ở lịch sử). **Chốt chặn tạo bảng lương**: `PayrollService::generateForPeriod()` báo 422 nếu kỳ đó còn bản ghi ĐÃ chấm ra mà `approval_status=pending` — vì bảng lương chỉ tính MỘT LẦN, không tự tính lại khi duyệt sau (bản ghi quên chấm ra không bị tính để khỏi chặn bảng lương vô thời hạn). Frontend: [`AttendanceApprovals.vue`](resources/js/views/Attendance/AttendanceApprovals.vue) (route `/attendance-approvals`, menu "Duyệt chấm công", dùng `DataTable` `actions` + `confirm.input` như màn Duyệt điều chỉnh công), component dùng chung [`components/attendance/AttendanceLogList.vue`](resources/js/components/attendance/AttendanceLogList.vue) (khối thiết bị/IP/vị trí từng lượt — dùng cả ở dialog chi tiết của `AttendanceHistoryPanel.vue`), `APPROVAL_STATUS_MAP` export từ `useCheckIn.js`. **Gộp chip trạng thái ca + duyệt công (2026-09-24, theo yêu cầu người dùng: "khi được duyệt mới được đang trong ca")** — trước đó thẻ ca hôm nay + "Lịch sử gần đây" (Desktop + Mobile) hiện 2 chip riêng (trạng thái ca LUÔN hiện, chip duyệt công chỉ hiện SAU khi đã chấm công ra — `v-if="last_check_out_at"`), nay gộp còn ĐÚNG 1 chip qua `mergedAttendanceStatus()`/`MERGED_ATTENDANCE_STATUS_MAP` (export mới từ `useCheckIn.js`, dùng CHUNG cho `CheckInDesktop.vue`/`CheckInMobile.vue` — đúng lý do file `useCheckIn.js` tồn tại, tránh lệch logic 2 nơi): chưa `approved` (kể cả đang `needs_review`) → LUÔN hiện "Chờ duyệt" bất kể đã chấm công ra hay chưa (khác hành vi cũ), che trạng thái ca bên dưới; `rejected` → hiện "Bị từ chối"; đã `approved` → mới hiện đúng trạng thái ca (`Đang trong ca`/`Hoàn tất`/`Cần xem lại`, dùng lại đúng nhãn `ATTENDANCE_STATUS_MAP` cũ). Cột "Duyệt công" ở bảng "Lịch sử gần đây" (Desktop) đã XÓA, gộp vào cột "Trạng thái". Cùng quy tắc đã áp dụng trước đó ở [`AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue) (mục ngay trên, `mergedStatusFor()` — 2 hàm riêng vì 1 bên có thêm `absent`/`on_leave` không áp dụng ở màn tự chấm công). Test: [`AttendanceApprovalTest.php`](tests/Feature/Attendance/AttendanceApprovalTest.php) + test thêm ở `AttendanceHistoryTest`/`PayrollTest`/`AttendanceTest`. **Chưa làm**: duyệt hàng loạt (mỗi ca 1 lượt bấm — khối lượng lớn nếu nhiều nhân viên), thông báo cho nhân viên khi bị từ chối. |
| **Tổng hợp chấm công trong ngày** (2026-09-23, theo yêu cầu người dùng — thay hẳn màn "Duyệt chấm công" cũ) | Màn cũ (`AttendanceApprovals.vue`, route `/attendance-approvals`) chỉ liệt kê bản ghi ĐÃ CÓ trong `attendances` nên không thấy được ai CHƯA chấm công — đã XÓA, thay bằng [`AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue) (route `/attendance-overview`, menu "Tổng hợp chấm công") liệt kê MỌI ca được gán cho MỌI nhân viên vào 1 ngày, kể cả ca chưa ai chấm công (suy ra `absent`) hoặc trùng ngày nghỉ phép đã duyệt (suy ra `on_leave`) — cùng lúc XEM và DUYỆT tại 1 chỗ. **2 quyền tách riêng, đúng ý nghĩa từng việc**: trang gate bằng `attendance.view_all` (HR **và Manager** xem được — trước đó Manager có quyền này nhưng không có màn nào dùng tới), nút Duyệt/Từ chối trong bảng chỉ hiện khi có thêm `attendance.approve` (`auth.permissions.includes(...)` ở FE, cùng khuôn `canManage`/`canUpdate` các trang khác) — Backend vẫn tự chặn 403 ở API duyệt dù nút có lỡ hiện. Backend: `AttendanceRepository::listAssignmentsForDate()` (mọi `EmployeeShiftAssignment` đang active phủ đúng ngày, lọc `department_id`/`work_shift_id` trong SQL, lọc THỨ trong tuần ở PHP — cùng lý do JSON `work_days` như `listActiveAssignmentsForDate()`) + `listAttendancesForDate()` (bản ghi `attendances` trong ngày, keyBy `employee_id|work_shift_id`) → `AttendanceService::dailyOverview()` ghép 2 nguồn + set nghỉ phép đã duyệt (`approvedLeaveEmployeeIdsForDate()`) thành 1 danh sách `rows` (không phân trang — quy mô "1 ngày x toàn công ty" nhỏ, **1 dòng/ca**, 1 người có 2 ca/ngày ra 2 dòng) + `summary` (tổng/hoàn tất/đang trong ca/cần xem lại/vắng/nghỉ phép/chờ duyệt) → `GET /attendances/overview?date=&department_id=&work_shift_id=&status=&approval_status=` (`AttendanceController::overview()`). **`summary` đếm theo NHÂN VIÊN, không theo ca** (2026-09-23, theo phản hồi người dùng sau khi thấy "6 ca" ở thẻ tổng quan dù chỉ 3 người thật vì mỗi người có 2 ca chồng nhau) — `summarizeDailyOverview()` gộp các dòng cùng `employee_id`, 1 người rơi vào ĐÚNG 1 nhóm theo thứ tự ưu tiên cố định `needs_review > pending > absent > completed > on_leave` (hằng số `EMPLOYEE_STATUS_PRIORITY`, sửa lại 2026-09-23 theo phản hồi người dùng — bản đầu để `absent` trên `pending` nên 1 người rõ ràng đang trong ca sáng vẫn bị `summary` tính "Vắng" chỉ vì ca chiều của họ chưa có bản ghi; "đang trong ca" là sự thật NGAY LÚC NÀY nên phải thắng "vắng" suy ra từ 1 ca KHÁC) khi các ca của họ có trạng thái khác nhau; `on_leave` chỉ khi TẤT CẢ ca trong ngày đều nghỉ phép. Thẻ tổng quan chỉ hiện SỐ, không kèm đơn vị "người"/"ca" (nhãn thẻ đã đủ rõ nghĩa). **Hạn chế còn lại, chưa sửa**: `absent` (ở mức TỪNG CA, ngoài phạm vi ưu tiên ở trên) vẫn không phân biệt "đã lỡ giờ" với "ca chưa tới giờ" — 1 ca CHƯA tới giờ bắt đầu vẫn hiện "Vắng" ngay khi xem `dailyOverview()` của "hôm nay"; người dùng cho biết sẽ chỉnh lại cách gán ca (không để 1 người chồng nhiều ca/ngày kiểu demo hiện tại) nên chưa xử lý thêm ở đây. **status ở màn này là 5 giá trị RIÊNG cho "hôm nay"** (`pending`/`completed`/`needs_review` lấy thẳng `attendances.status` khi đã có bản ghi, `absent`/`on_leave` suy ra khi chưa có) — KHÔNG dùng chung `deriveHistoryStatus()` (mục 18, `full`/`late`/`insufficient`) vì hàm đó sẽ tính nhầm 1 người đang trong ca đúng giờ thành "thiếu công" chỉ vì chưa chấm công ra. Vẫn giữ nguyên (không đổi): route `GET /attendances` (`index()`/`list()`, phân trang) — hạ tầng liệt kê chung, hiện không còn màn nào ở FE gọi tới nhưng không phải phần bị thay thế ở đây nên không xóa. Test: [`AttendanceOverviewTest.php`](tests/Feature/Attendance/AttendanceOverviewTest.php) (13 test — kể cả ca gán lệch thứ trong tuần, 1 người 2 ca/ngày, quyền xem vs quyền duyệt tách riêng). **Gộp cột "Trạng thái ca" + "Duyệt công" thành 1 cột "Trạng thái"** (2026-09-24, theo yêu cầu người dùng: "chưa được duyệt thì là chờ duyệt, khi duyệt rồi mới là đang trong ca") — THUẦN FRONTEND, KHÔNG đổi API/dữ liệu (`item.status` + `item.attendance.approval_status` vẫn y nguyên, chỉ đổi cách HIỂN THỊ): `mergedStatusFor(item)` trong `AttendanceOverview.vue` — chưa có bản ghi (`absent`/`on_leave`) hiện thẳng trạng thái ca; có bản ghi mà `rejected` hiện "Từ chối"; có bản ghi mà CHƯA `approved` (kể cả đang `needs_review`) hiện "Chờ duyệt", **che luôn** trạng thái ca bên dưới cho tới khi duyệt xong; đã `approved` mới hiện đúng trạng thái ca (`Đang trong ca`/`Hoàn tất`/`Cần xem lại`). Dialog "Chi tiết thiết bị/vị trí" (bấm icon mắt) KHÔNG đổi — vẫn hiện riêng chip duyệt công qua `APPROVAL_STATUS_MAP`, vì đó là màn xem sâu 1 bản ghi, không phải dòng tổng quan. Bộ lọc "Trạng thái ca" ở đầu trang GIỮ NGUYÊN (vẫn lọc theo `status` thô qua API, không theo cột hiển thị đã gộp). |
| Ghi chú | **Chấm công = ghi nhận dữ liệu của CHÍNH thiết bị đang bấm** (2026-09-21, theo yêu cầu người dùng: dùng điện thoại thì ghi IP điện thoại, địa chỉ nơi bấm, tên thiết bị — trước đó bị hiểu nhầm là nhân viên chọn 1 trong 3 phương thức Wifi/GPS/QR). Mỗi lượt vào/ra TỰ ghi vào `attendance_logs`: `ip_address` (server đọc `request()->ip()`), `device_name` (`DeviceInfoParser::describe()`, VD "iPhone (iOS 17.2) · Safari"; **giới hạn của trình duyệt** — không lấy được tên thương mại như "iPhone 15 Pro", Android hay chỉ còn "Android 13" vì Chrome mới che mã máy) + `user_agent` gốc, tọa độ `latitude`/`longitude`/`accuracy_meters` do trình duyệt gửi lên, `address` (`ReverseGeocoder` đổi tọa độ → địa chỉ chữ qua Nominatim/OpenStreetMap, cấu hình `config/services.php` → `nominatim`, biến `NOMINATIM_URL`/`NOMINATIM_USER_AGENT`; timeout 3s, cache 1 ngày theo tọa độ làm tròn 4 chữ số). **Fail-soft có chủ đích**: từ chối quyền vị trí / http không phải https / dịch vụ địa chỉ sập → vẫn chấm công được, chỉ thiếu `address` (tọa độ thô vẫn lưu nếu có); tra địa chỉ chạy TRƯỚC `DB::transaction()` để không giữ khóa DB lúc chờ mạng. **Lưu ý riêng tư**: tọa độ nhân viên được gửi sang máy chủ OpenStreetMap (chỉ tọa độ, không kèm danh tính); Nominatim yêu cầu User-Agent nhận diện ứng dụng và ~1 request/giây — nên đổi `NOMINATIM_USER_AGENT` thành tên+email liên hệ thật khi triển khai. **"Điểm chấm công" của công ty (mục 13) giờ chỉ để ĐỐI CHIẾU/gắn nhãn** — `matchLocation()` thu cả IP + tọa độ + QR rồi tự khớp theo thứ tự ưu tiên QR > Wifi (IP trong `allowed_ip_cidr`) > GPS (trong bán kính); khớp → `status=pending` + ghi điểm, không khớp → `needs_review`. Cột `attendance_logs.method` đổi nghĩa thành "khớp bằng cách nào" (`wifi`/`gps`/`qr` theo điểm đã khớp, `device` = không khớp điểm nào; bản ghi cũ trước thay đổi vẫn là lựa chọn của nhân viên). Frontend: bỏ 3 tab Wifi/GPS/QR ở `CheckInDesktop.vue`/`CheckInMobile.vue`, `useCheckIn.js` tự xin vị trí mỗi lần bấm (bị từ chối → toast cảnh báo rồi vẫn gửi), ô mã QR còn lại là tùy chọn; [`AttendanceHistoryPanel.vue`](resources/js/views/Attendance/AttendanceHistoryPanel.vue) dialog "Chi tiết chấm công" hiện mỗi lượt là 1 khối (Thiết bị / IP / Vị trí+tọa độ+sai số / Điểm chấm công). **Chưa kiểm chứng với Nominatim thật** (máy phát triển không phân giải được tên miền openstreetmap.org) — nhánh thành công chỉ được test bằng `Http::fake()`. Ngoài ra: `matchLocation()` không khớp điểm nào (wifi/gps/qr) thì **vẫn cho chấm công**, chỉ đánh dấu `status=needs_review` (không chặn cứng). OT tính từ phần **SAU `end_time`**, trừ thêm `OVERTIME_GRACE_MINUTES=5` phút ân hạn CỐ ĐỊNH toàn hệ thống (2026-09-21, theo yêu cầu người dùng — ra trễ vài phút do dọn dẹp/di chuyển không tính OT ngay; VD ca kết thúc 17:00: check-out 17:05 → 0 phút OT, 17:06 → 1 phút, 17:30 → 25 phút), không phải `actual - standard`. `overtime_minutes` lưu **luôn đúng số thực đã trừ ân hạn**, kể cả khi chưa đạt ngưỡng trả lương — xem mục 27 (PayrollService) cho ngưỡng đó + cổng duyệt OT (mục 17). **Khung giờ check-in** (Ngày 44 — bug người dùng phát hiện): trước đó không so giờ hiện tại với giờ ca, cho phép chấm công 1 ca cách xa giờ thật (vd chấm Ca chiều lúc 8h sáng) — đã sửa bằng `assertWithinCheckInWindow()` (chỉ cho vào sớm tối đa 30 phút trước `start_time`, chặn hẳn sau `end_time`, dùng "Xin bổ sung chấm công" thay thế, mục 17). WiFi "mất điểm" khi test qua Docker Desktop là do NAT (source IP bị đổi thành gateway container), không phải bug — dev local cần đặt `allowed_ip_cidr` theo đúng subnet Docker, **đổi lại khi triển khai thật** |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 17. Điều chỉnh công (Attendance Adjustment)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`CheckIn.vue`](resources/js/views/Attendance/CheckIn.vue) — "Xin điều chỉnh" (sửa bản ghi có sẵn), "Xin bổ sung chấm công" (tạo bản ghi cho ca+ngày chưa từng chấm công), "Xin miễn trừ đi muộn" (mục 24), "Xin duyệt OT" (2026-09-21, cùng khuôn "Xin miễn trừ đi muộn" — nút chỉ hiện khi `overtime_minutes > 0 && !overtime_approved`), "Xin làm ngoài lịch" (2026-09-23, xem hàng riêng bên dưới); [`AttendanceAdjustments.vue`](resources/js/views/Attendance/AttendanceAdjustments.vue) — HR duyệt, dùng `confirm.input` của `DataTable.vue` |
| Xử lý Backend | [`AttendanceAdjustmentController.php`](app/Http/Controllers/Api/V1/AttendanceAdjustmentController.php) (`store`/`mine`/`index`/`decide`), [`AttendanceAdjustmentService.php`](app/Services/AttendanceAdjustmentService.php) (`requestForEmployee()`/`decide()` rẽ nhánh theo `type`: `correction`/`supplement`/`excuse`/`overtime`/`extra_shift`), [`AttendanceAdjustmentRepository.php`](app/Repositories/AttendanceAdjustmentRepository.php) |
| Model | [`AttendanceAdjustment.php`](app/Models/AttendanceAdjustment.php) — `employee_id`/`work_shift_id`/`attendance_date` set cho cả 5 loại (đọc trực tiếp không cần qua `attendance`) |
| Validation | [`StoreAttendanceAdjustmentRequest.php`](app/Http/Requests/Attendance/StoreAttendanceAdjustmentRequest.php) (rẽ nhánh theo `type`), [`DecideAttendanceAdjustmentRequest.php`](app/Http/Requests/Attendance/DecideAttendanceAdjustmentRequest.php) |
| Database & API | `attendance_adjustments`; `POST /adjustments`, `GET /adjustments/me`, `GET/PUT /adjustments`, `/{id}` ([`routes/api/v1/attendances.php`](routes/api/v1/attendances.php) — `/adjustments/me` phải khai TRƯỚC `/adjustments/{id}`) |
| Test | [`AttendanceAdjustmentTest.php`](tests/Feature/Attendance/AttendanceAdjustmentTest.php) (bao gồm type `overtime`, kể cả nhánh xin TRƯỚC khi chấm công ra), [`AttendanceExtraShiftTest.php`](tests/Feature/Attendance/AttendanceExtraShiftTest.php) (15 test — type `extra_shift`, gồm cả "Tự chọn giờ") |
| Ghi chú | Duyệt `correction`/`supplement` thì áp giờ đề xuất vào `Attendance` qua `AttendanceService::applyAdjustment()` (tái dùng công thức trễ/sớm/OT), `status=completed` chỉ khi đủ cả giờ vào lẫn ra. Duyệt `excuse` **chỉ** set `late_excused=true`, duyệt `overtime` (2026-09-21) **chỉ** set `overtime_approved=true` — cả 2 đều không đổi giờ (mục 24). Từ chối ở loại nào cũng không đụng `Attendance`. `overtime` type: nhân viên CHỦ ĐỘNG gửi yêu cầu (không phải HR tự duyệt hàng loạt) — chặn nếu `overtime_minutes<=0` hoặc đã `overtime_approved`; PayrollService (mục 27) chỉ trả lương OT khi cờ này = true VÀ đạt ngưỡng phút tối thiểu |
| **`extra_shift` — "Xin làm ngoài lịch"** (2026-09-23, theo yêu cầu người dùng — gộp chung "xin làm OT"/"xin làm thêm ngày"/"làm bù T7-CN không có trong lịch", đã hỏi lại và xác nhận 3 quyết định: (1) gộp chung 1 loại thay vì tách OT riêng, (2) làm ngày không trong lịch tính lương như ngày công BÌNH THƯỜNG — không phải OT, (3) nộp đơn ĐĂNG KÝ TRƯỚC rồi tự chấm công) | Khác hẳn `supplement` (ĐÃ làm, khai giờ thực tế, bắt buộc phải đang được GÁN ca đó): `extra_shift` là ĐĂNG KÝ TRƯỚC cho 1 ngày/ca **KHÔNG có trong lịch gán** — không có `proposed_check_in_at`/`proposed_check_out_at` (chưa làm gì cả). Validate lúc nộp (`AttendanceAdjustmentService::assertExtraShiftRequestIsValid()`): Ca phải `is_active`, ngày phải hôm nay/tương lai (`after_or_equal:today` — ngược `before_or_equal:today` của `supplement`), CHƯA có ca này vào đúng ngày đó (`resolveActiveAssignment()` phải null — có rồi thì cứ chấm công bình thường), và chưa có yêu cầu `extra_shift` khác đang `pending` cho đúng ca+ngày. **Duyệt KHÔNG đụng `Attendance`** (chưa có gì để sửa) — chỉ gọi [`EmployeeShiftAssignmentService::createOneOffAssignment()`](app/Services/EmployeeShiftAssignmentService.php) tạo 1 bản `employee_shift_assignments` **CHỈ ÁP DỤNG ĐÚNG 1 NGÀY** (`effective_from`=`effective_to`=ngày đăng ký, `work_days`=[đúng thứ của ngày đó]) để MỞ KHÓA — từ đó nhân viên tự bấm Chấm công vào/ra **hoàn toàn bình thường** khi tới ngày, không cần sửa gì ở `AttendanceService::checkIn()`/`checkOut()` (tự động được tính công như ngày thường, work_coefficient của Ca quyết định — đúng quyết định (2) ở trên, KHÔNG có logic OT/hệ số đặc biệt nào cho ngày này). `GET /work-shifts` (mục 12) đổi middleware từ `permission:shift.view` sang `permission:shift.view,attendance.check` — nhân viên không có `shift.view` (không thấy trang "Ca làm việc") vẫn cần đọc danh sách ca đang hoạt động để chọn ở dropdown này (`workShiftService.list()`, khác `employeeService.myShiftAssignments()` mà `supplement` dùng — đó chỉ trả ca ĐÃ gán). Frontend: 1 dialog dùng CHUNG cho cả 2 nút "Xin làm ngoài lịch"/"Xin OT" (2026-09-23, sửa lại theo yêu cầu người dùng — xem hàng riêng bên dưới), cộng khối "Đơn xin làm ngoài lịch / OT của tôi" (đọc qua `attendanceService.myAdjustments()` lọc `type==='extra_shift' || type==='overtime'`, dùng `APPROVAL_STATUS_MAP` có sẵn, gắn chip "Ngoài lịch"/"OT" phân biệt). **Hạn chế đã biết, chưa làm**: HR không "rút lại" được 1 yêu cầu đã duyệt qua chính màn Duyệt điều chỉnh công (`decide()` không cho xử lý lại yêu cầu `pending`→khác) — muốn hủy phải vào tab "Ca làm việc" của nhân viên xóa tay bản gán ca 1-ngày vừa tạo. |
| **1 dialog, 2 tab "Làm ngoài lịch" / "Xin OT" + "Tự chọn giờ"** (2026-09-23, theo yêu cầu người dùng) | Gộp nút "Xin làm ngoài lịch" và "Xin OT" (PageHeader `CheckInDesktop.vue`/`CheckInMobile.vue`) vào CHUNG 1 dialog `<v-tabs>`/`<v-window>` (`extraShiftDialog`/`extraShiftTab` trong `useCheckIn.js`), `openExtraShiftDialog(tab)` mở sẵn đúng tab theo nút đã bấm. **Tab "Làm ngoài lịch"** thêm `<v-btn-toggle>` "Chọn ca có sẵn" / "Tự chọn giờ" (`extraShiftForm.mode`) — chọn "Tự chọn giờ" thì gửi `custom_start_time`/`custom_end_time` (HH:mm) thay vì `work_shift_id`; `StoreAttendanceAdjustmentRequest` bắt buộc ĐÚNG 1 trong 2 cách (`work_shift_id` HOẶC cặp `custom_*`, `required_without_all`/`required_without`, `custom_end_time` phải `after:custom_start_time`); Backend (`AttendanceAdjustmentService::requestForEmployee()`) tự gọi [`WorkShiftService::createCustomOneOff()`](app/Services/WorkShiftService.php) tạo NGAY 1 `WorkShift` mới (tên `"Ca tùy chỉnh HH:mm-HH:mm"`, KHÔNG `is_default`, châm chước 5 phút mặc định) rồi xử lý TIẾP hệt như đã chọn 1 Ca có sẵn — không viết đường xử lý riêng, tái dùng nguyên `assertExtraShiftRequestIsValid()`/`createOneOffAssignment()`. **Tab "Xin OT"** dùng lại type `overtime` sẵn có — dropdown `otTodayOptions` (computed từ `todayShifts` đã tải, lọc: đã chấm công vào, chưa `overtime_approved`, và chưa chấm công ra HOẶC đã có `overtime_minutes>0`), gọi `submitOtRequestFromToday()` (payload giống hệt `submitOtApprovalRequest()` cũ — `type=overtime`+`attendance_id`+`reason` — chỉ khác NGUỒN chọn `attendance_id`: từ ca ĐANG làm hôm nay thay vì 1 dòng lịch sử đã hoàn tất). |
| **Xin duyệt OT TRƯỚC khi chấm công ra** (2026-09-23, theo yêu cầu người dùng — "hôm nay kết thúc ca 17:00 thì xin OT từ đó tới lúc chấm công ra") | Nới lỏng `AttendanceAdjustmentService::requestForEmployee()` nhánh `overtime` (dùng bởi CẢ tab "Xin OT" MỚI lẫn nút "Xin duyệt OT" CŨ ở dòng lịch sử — không tách type riêng): trước đây bắt buộc `overtime_minutes>0` (chỉ xin được SAU khi đã chấm công ra); giờ chỉ bắt `first_check_in_at` khác null (đã bắt đầu ca) — nếu ĐÃ chấm công ra (`last_check_out_at` khác null) VẪN giữ nguyên chặn `overtime_minutes<=0` (chắc chắn không có gì để duyệt). `decide()` **không đổi gì** — duyệt luôn chỉ set `overtime_approved=true` NGAY (dù `overtime_minutes` hiện đang là 0, chưa tính được vì còn đang làm) — tới lúc `checkOut()` tính ra số phút OT thật, cờ đã có sẵn nên KHÔNG cần xin lại lần 2, `PayrollService` (mục 27) đọc đúng cờ + số phút cuối cùng khi tính lương. |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 18. Lịch sử chấm công (báo cáo theo tháng)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`AttendanceHistoryPanel.vue`](resources/js/views/Attendance/AttendanceHistoryPanel.vue) — dùng chung cho [`AttendanceHistory.vue`](resources/js/views/Attendance/AttendanceHistory.vue) (tự xem) và tab "Chấm công" trong `EmployeeDetail.vue` (Admin xem theo nhân viên) |
| Xử lý Backend | `AttendanceController::historyMine()`/`history()`, `AttendanceService::history()`/`deriveHistoryStatus()`/`summarizeHistory()` — không có migration riêng, tính từ dữ liệu đã có |
| Database & API | `GET /attendances/history/me`, `/history/{employee}` (phải khai `/me` TRƯỚC); quyền `attendance.view_own`/`view_all` |
| Test | [`AttendanceHistoryTest.php`](tests/Feature/Attendance/AttendanceHistoryTest.php) |
| Ghi chú | Nhãn suy theo thứ tự ưu tiên `on_leave` > `late` > `insufficient` > `full`/`absent` — đơn nghỉ phép `approved` (không phải `hourly`) tự che ngày thành `on_leave`, loại khỏi cả "Vắng" lẫn thống kê giờ công. `total_work_days` trong `summary` (2026-09-16, cập nhật): tính qua [`WorkTimeCalculationService`](app/Services/WorkTimeCalculationService.php) dùng CHUNG với `PayrollService` (mục 27) — KHÔNG còn dùng `WorkShift.work_coefficient` nữa (xem mục 12). **`date_to` bị ép không vượt quá HÔM NAY** (2026-09-24, lỗi thật người dùng phát hiện: bộ lọc mặc định "Tháng hiện tại" kéo tới hết tháng, các ngày SAU hôm nay chưa xảy ra vẫn bị dựng dòng "Vắng" rồi xếp LÊN TRÊN ngày vừa chấm công thật vì `sortBy` ngày giảm dần không phân biệt "chưa tới" với "đã qua mà vắng" — `history()` tự `min($dateTo, hôm nay)` trước khi dựng `CarbonPeriod`, khoảng lọc toàn tương lai thì trả rỗng, không lỗi) — xem `test_history_excludes_future_dates_even_when_requested`. |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 19. Nghỉ phép — Tạo đơn (Leave Request)

| Hạng mục | Chi tiết |
| --- | --- |
| Xử lý Backend | [`LeaveRequestController.php`](app/Http/Controllers/Api/V1/LeaveRequestController.php), [`LeaveRequestService.php`](app/Services/LeaveRequestService.php) (`calculateTotalDays()`, `calculateHourlyDays()`, kiểm quỹ phép), [`LeaveRequestRepository.php`](app/Repositories/LeaveRequestRepository.php) (`findOverlapping()`, `sumPendingDaysForYear()`), [`LeaveBalanceRepository.php`](app/Repositories/LeaveBalanceRepository.php) |
| Model | [`LeaveType.php`](app/Models/LeaveType.php), [`LeaveBalance.php`](app/Models/LeaveBalance.php), [`LeaveRequest.php`](app/Models/LeaveRequest.php) |
| Seeder | [`LeaveTypeSeeder.php`](database/seeders/LeaveTypeSeeder.php) — 3 loại (`annual`/`other`/`unpaid`) — trước 2026-09-24 có 5 loại (`annual`/`sick`/`maternity`/`paternity`/`unpaid`), xem mục 30 |
| Validation | [`StoreLeaveRequest.php`](app/Http/Requests/Leave/StoreLeaveRequest.php) |
| Database & API | `leave_types`, `leave_balances`, `leave_requests`, `leave_approvals`; `POST /leave-requests`, `GET /leave-requests/me`, `/balances/me`, `/` (HR) ([`routes/api/v1/leave-requests.php`](routes/api/v1/leave-requests.php)); quyền `leave.request`/`view_own`/`view_all` |
| Test | [`LeaveRequestTest.php`](tests/Feature/Leave/LeaveRequestTest.php) |
| Ghi chú | `total_days` bỏ qua Thứ 7/CN; chỉ loại `annual` bị giới hạn quỹ (2 loại kia `annual_entitlement_days=0`, tính theo chế độ riêng — xem mục 30). Quỹ phép **không trừ lúc tạo đơn** — chỉ trừ khi duyệt xong (`deductBalance()`, mục 20); lúc kiểm tra "đủ phép" đã trừ cả `total_days` của các đơn đang `pending`/`manager_approved` cùng loại (`available_days`), tránh cộng dồn vượt quỹ. Chặn đơn phép trùng ngày qua `findOverlapping()` (mọi đơn chưa bị từ chối). Mọi cột `decimal` phải cast `'float'` trong Model (tránh hiện `"1.00"`) — nhưng **không** ép `is_active` thành `boolean` (vỡ `StatusChip` tra theo khóa số nguyên `1`/`0`) |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 20. Nghỉ phép — Duyệt nhiều cấp (Leave Approval)

| Hạng mục | Chi tiết |
| --- | --- |
| Xử lý Backend | `LeaveRequestController::decide()`, [`LeaveApprovalService.php`](app/Services/LeaveApprovalService.php), [`LeaveApprovalRepository.php`](app/Repositories/LeaveApprovalRepository.php) |
| Permission | Tách `leave.approve` (gốc) thành `leave.approve_manager`/`leave.approve_hr` |
| Model | [`LeaveApproval.php`](app/Models/LeaveApproval.php) |
| Test | [`LeaveApprovalTest.php`](tests/Feature/Leave/LeaveApprovalTest.php) |
| Ghi chú | Luồng `pending` → (Manager) → `manager_approved` → (HR) → `approved`; **từ chối ở cấp nào cũng kết thúc luôn**; không có quản lý trực tiếp thì HR duyệt thẳng; duyệt xong cấp cuối mới gọi `deductBalance()`. Tham số Controller phải khớp CHÍNH XÁC tên route model binding (không có fallback theo vị trí — gõ nhầm tên sẽ âm thầm nhận 1 Model rỗng) |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 21. Nghỉ phép — Email thông báo kết quả duyệt (Queue) — **ĐÃ BỎ (2026-09-24)**

| Hạng mục | Chi tiết |
| --- | --- |
| **Đã bỏ, thay bằng thông báo trong-app** (2026-09-24, theo yêu cầu người dùng: "không muốn gửi bằng mail nữa chỉ có gửi thông báo qua web") | `LeaveDecisionMail.php`/`leave-decision.blade.php` đã **XÓA HẲN** (không còn dùng ở đâu). Kết quả duyệt/từ chối giờ CHỈ báo qua thông báo web realtime — xem mục 33 (`LeaveApprovalService::notifyEmployeeDecision()`). **Đối chiếu tài liệu**: YEU_CAU_DU_AN mục 3.1 dòng 9 ghi "...qua WebSocket... **hoặc** Email Queue..." — dùng chữ "hoặc", không bắt buộc cả hai kênh cùng lúc, nên quyết định này không mâu thuẫn tài liệu. |
| Xử lý Backend (lịch sử, không còn đúng — xem hàng trên) | ~~`LeaveDecisionMail.php` (`ShouldQueue`) + view `leave-decision.blade.php`~~ |
| Hạ tầng | Service `queue` trong [`docker-compose.yml`](docker-compose.yml) (`php artisan queue:work`) vẫn giữ nguyên — dùng chung cho email khác ([`PasswordResetMail.php`](app/Mail/PasswordResetMail.php)) và cho kênh email tổng quát của [`NotificationService.php`](app/Services/NotificationService.php) (mục 33), không phải xóa vì mục này. |
| Test | Đã chuyển 2 test cũ trong `LeaveApprovalTest.php` (`test_mail_is_sent_...`) thành `test_no_mail_sent_...` (lưới chặn hồi quy — khẳng định KHÔNG còn gửi mail), xem mục 33. |
| Danh sách tệp cần sửa khi bảo trì | Không còn — mục này chỉ giữ lại làm lịch sử quyết định, xem mục 33 để biết hành vi hiện tại. |

## 22. Nghỉ phép — Form tạo đơn (Frontend, tự phục vụ)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`LeaveRequests.vue`](resources/js/views/Leave/LeaveRequests.vue) — dialog tạo đơn (nghỉ theo giờ, đính kèm bắt buộc với loại `other`/"Nghỉ khác theo chế độ/luật" — xem mục 30), thẻ quỹ phép còn lại, bảng "Đơn của tôi"; [`leaveRequestService.js`](resources/js/services/leaveRequestService.js), [`leaveTypeService.js`](resources/js/services/leaveTypeService.js) |
| Xử lý Backend | [`LeaveTypeController::index()`](app/Http/Controllers/Api/V1/LeaveTypeController.php) (chỉ đọc), migration thêm `start_time`/`end_time` cho nghỉ theo giờ ([`...000003_add_hourly_fields...`](database/migrations/2026_09_11_000003_add_hourly_fields_to_leave_requests_table.php)) |
| Database & API | `GET /leave-types`; `GET /leave-requests/{id}/evidence` (IDOR check trong hàm, không qua middleware) |
| Ghi chú | Dropdown loại phép ghép sẵn "Có lương/Không lương + giới hạn N ngày" vào tên lựa chọn, không cần component riêng |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 23. Nghỉ phép — Duyệt (Frontend, Manager/HR)

| Hạng mục | Chi tiết |
| --- | --- |
| Giao diện Frontend | [`LeaveManagement.vue`](resources/js/views/Leave/LeaveManagement.vue) — theo khuôn `AttendanceAdjustments.vue` (mục 17), không tự đoán cấp Manager/HR ở Frontend (để Service tự quyết theo danh tính người bấm) |
| Test | Kiểm chứng qua Playwright — logic duyệt đã có 10 test ở mục 20 |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 24. Ngày 42 — Edge case chấm công

3 edge case theo kế hoạch, đã xác nhận qua `AskUserQuestion` trước khi làm:

| Hạng mục | Chi tiết |
| --- | --- |
| "Chấm công quên Check-out" | Không cần thêm code — cơ chế `type=correction` sẵn có (mục 17) đã xử lý đúng (chỉ đề xuất `proposed_check_out_at`, giữ nguyên giờ vào) |
| "Chấm công quên Check-out" — nhắc chủ động (2026-09-25) | Tài liệu chỉ nêu tên edge case, không quy định cách xử lý — người dùng yêu cầu thêm bước **CHỦ ĐỘNG nhắc** (trước đó chỉ có remedy bị động ở dòng trên, nhân viên phải tự nhớ mà xin `correction`). `attendance:remind-checkout` (`app/Console/Commands/RemindMissingCheckout.php`), chạy `everyMinute()` (`routes/console.php`): quét `attendances` hôm nay có `first_check_in_at` nhưng `last_check_out_at` NULL và **đã quá giờ `work_shifts.end_time` của Ca hơn 5 phút** (ngưỡng "5 phút" là giả định của phiên này, không có trong tài liệu) → gửi 1 thông báo trong-app (`NotificationService::send()`, type `attendance.checkout_reminder`, KHÔNG kèm email — đúng quyết định "chỉ gửi thông báo qua web" mục 21) cho `employee->user`. Cột mới `attendances.checkout_reminder_sent_at` ([migration `...2026_09_25_000001`](database/migrations/2026_09_25_000001_add_checkout_reminder_sent_at_to_attendances_table.php)) đánh dấu đã gửi để KHÔNG lặp lại mỗi phút. Bỏ qua nếu nhân viên không có tài khoản đăng nhập (không có nơi nhận thông báo trong-app). Test: `tests/Feature/Console/RemindMissingCheckoutTest.php` |
| "Đơn phép trùng ngày" | Đã xử lý ở mục 19 (`findOverlapping()`) |
| "Đi muộn có lý do" | Type mới `excuse` trong `attendance_adjustments` + cột `attendances.late_excused` ([migration `...000004`](database/migrations/2026_09_11_000004_add_late_excused_to_attendances_table.php)). Nhân viên gửi `type=excuse` (chỉ cần `attendance_id`+`reason`, không sửa giờ) — validate `late_minutes > 0` và chưa từng được miễn trừ. HR duyệt chỉ set `late_excused=true`, không gọi `applyAdjustment()`. Ảnh hưởng `deriveHistoryStatus()`/`summarizeHistory()` (mục 18) — ngày được miễn trừ không còn tính là "Đi muộn" |
| Giao diện Frontend | `CheckIn.vue` nút "Xin miễn trừ đi muộn" (hiện khi `late_minutes > 0 && !late_excused`), `AttendanceAdjustments.vue` thêm `excuse` vào `ADJUSTMENT_TYPE_MAP` |
| Test | `AttendanceAdjustmentTest.php`, `AttendanceHistoryTest.php` |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 25. Ngày 43 — Unit Test & Integration Test cho Attendance & Leave

| Hạng mục | Chi tiết |
| --- | --- |
| Integration Test | Phần lớn đã có sẵn từ các ngày trước (99 test) — vá 1 lỗ hổng: [`LeaveTypeTest.php`](tests/Feature/Leave/LeaveTypeTest.php) (3 test, `LeaveTypeController` chưa từng được test riêng) |
| Unit Test (mới) | [`tests/Unit/Services/AttendanceServiceCalculationTest.php`](tests/Unit/Services/AttendanceServiceCalculationTest.php) (21 test), [`LeaveRequestServiceCalculationTest.php`](tests/Unit/Services/LeaveRequestServiceCalculationTest.php) (13 test) — gọi thẳng các hàm `private` qua Reflection, dùng chung trait [`InteractsWithPrivateMethods.php`](tests/Unit/Concerns/InteractsWithPrivateMethods.php) |
| Ghi chú | Test dựng `Attendance` với field cast `datetime` (gán giá trị `Carbon`) phải kế thừa `Tests\TestCase` (boot Laravel, không cần `RefreshDatabase`) chứ không phải `PHPUnit\Framework\TestCase` trần — Eloquent cần connection resolver để tính `getDateFormat()` dù không thật sự chạm DB |
| Danh sách tệp cần sửa khi bảo trì | Không áp dụng — bổ sung Test cho code đã có ở mục 16-24 |

## 26. Ngày 44 — Responsive UI cho Check-in mobile

| Hạng mục | Chi tiết |
| --- | --- |
| Sidebar | [`AppLayout.vue`](resources/js/components/layout/AppLayout.vue)/[`AppSidebar.vue`](resources/js/components/layout/AppSidebar.vue) — `v-navigation-drawer` chuyển `temporary` (overlay, ẩn mặc định) dưới breakpoint mobile của Vuetify (1280px mặc định), `permanent` + `rail` như cũ ở desktop |
| Check-in | [`composables/useCheckIn.js`](resources/js/composables/useCheckIn.js) (toàn bộ state/logic dùng chung) + [`CheckInDesktop.vue`](resources/js/views/Attendance/CheckInDesktop.vue) (bảng) + [`CheckInMobile.vue`](resources/js/views/Attendance/CheckInMobile.vue) (danh sách thẻ, dialog fullscreen) + [`CheckIn.vue`](resources/js/views/Attendance/CheckIn.vue) (switcher theo `useDisplay().mobile`) — tách UI theo thiết bị nhưng dùng chung 1 lớp logic, tránh rủi ro lệch code giữa 2 bản |
| Ghi chú | Dialog `fullscreen` dùng `.glass-panel` (nền trong suốt) → chữ trang phía sau xuyên qua vì `fullscreen` không có scrim — sửa dùng nền đặc mặc định của `v-card`/`v-toolbar`. Khởi tạo `drawerOpen = ref(false)` cố định làm sidebar ẩn ở **mọi** kích thước kể cả desktop — Vuetify chỉ tự ép `permanent` hiện lại khi component KHÔNG nhận `v-model` tường minh, đã sửa bằng `ref(!mobile.value)` + `watch(mobile, ...)` tự đồng bộ khi resize qua breakpoint |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 26b. Responsive cho 3 màn nhân viên hay dùng trên di động (2026-09-25)

> Người dùng dán 1 checklist UX tham khảo (từ ChatGPT), tôi rà lại code thật và xác nhận: trang Chấm công đã responsive tốt (mục 26) nhưng 3 màn khác nhân viên cũng dùng hằng ngày trên điện thoại — "Nghỉ phép" (tạo đơn/xem đơn của tôi), "Phiếu lương", "Ca làm việc" (2 tab sau nằm trong Hồ sơ của tôi) — vẫn dùng `<v-table>` phẳng nhiều cột, không đọc được trên màn hình hẹp.

| Hạng mục | Chi tiết |
| --- | --- |
| Cách làm | KHÔNG tách file Desktop/Mobile riêng như Check-in (mục 26) — 3 trang này chỉ có 1 bảng đơn giản (không có dialog phức tạp đi kèm bảng), tách file sẽ nhân đôi phần dialog/form không liên quan tới việc responsive. Thay vào đó: `useDisplay().mobile` ngay trong CÙNG 1 file, `v-if="mobile"` render danh sách thẻ xếp dọc (cùng phong cách card đã dùng ở [`CheckInMobile.vue`](resources/js/views/Attendance/CheckInMobile.vue)), `v-else` giữ nguyên `<v-table>` cho desktop — không đổi gì về logic tải dữ liệu/gọi API. |
| Đã sửa | [`LeaveRequests.vue`](resources/js/views/Leave/LeaveRequests.vue) (bảng "Đơn của tôi", 6 cột), [`MyProfilePayslipsTab.vue`](resources/js/views/Me/MyProfilePayslipsTab.vue) (4 cột), [`MyProfileShiftsTab.vue`](resources/js/views/Me/MyProfileShiftsTab.vue) (5 cột). **Bổ sung cùng ngày** (người dùng yêu cầu làm nốt) — [`MyProfileContractsTab.vue`](resources/js/views/Me/MyProfileContractsTab.vue) (7 cột, kèm 3 nút thao tác/dòng: Xem chi tiết/Xem trước/Tải file — gộp vào 1 hàng nút trong thẻ), [`MyProfileDocumentsTab.vue`](resources/js/views/Me/MyProfileDocumentsTab.vue) (6 cột). Cả 4 tab trong "Hồ sơ của tôi" giờ đồng bộ — chỉ còn đúng 1 tab CHƯA làm (xem dòng dưới). |
| Chưa làm / giới hạn đã biết | [`MyProfileTransfersTab.vue`](resources/js/views/Me/MyProfileTransfersTab.vue) (tab "Luân chuyển") — người dùng chỉ nói "2 tab khác" (khớp đúng Hợp đồng/Tài liệu đã nêu tên trước đó), chưa yêu cầu tab này nên CHƯA đụng tới. Các trang admin dùng [`DataTable.vue`](resources/js/components/common/DataTable.vue) (Nhân viên, Tổng hợp chấm công, Duyệt nghỉ phép, Bảng lương...) chưa rà — bảng đó nhiều cột hơn nữa và có phân trang/thao tác phức tạp hơn, HR/Admin thường dùng trên desktop nên độ ưu tiên thấp hơn các màn nhân viên tự phục vụ ở trên. KHÔNG verify được bằng mắt qua trình duyệt thật (không có Playwright trong phiên này). |
| Danh sách tệp cần sửa khi bảo trì | [`resources/js/views/Leave/LeaveRequests.vue`](resources/js/views/Leave/LeaveRequests.vue), [`resources/js/views/Me/MyProfilePayslipsTab.vue`](resources/js/views/Me/MyProfilePayslipsTab.vue), [`resources/js/views/Me/MyProfileShiftsTab.vue`](resources/js/views/Me/MyProfileShiftsTab.vue), [`resources/js/views/Me/MyProfileContractsTab.vue`](resources/js/views/Me/MyProfileContractsTab.vue), [`resources/js/views/Me/MyProfileDocumentsTab.vue`](resources/js/views/Me/MyProfileDocumentsTab.vue) |

## 27. Ngày 46-48 — Payroll (Bảng lương): Model, Repository, Service, Controller/Route, tính lương tự động

| Hạng mục | Chi tiết |
| --- | --- |
| Database | 4 bảng đã có sẵn từ migration Ngày 03-04 (`salary_components`, `payrolls`, `payroll_details`, `payroll_detail_components` — thiết kế snapshot, xem Model), cộng 1 migration mới bổ sung `deleted_at` cho `salary_components` ([`...000001_add_soft_deletes_to_salary_components_table.php`](database/migrations/2026_09_14_000001_add_soft_deletes_to_salary_components_table.php)) và 1 bảng `holidays` (đã có từ đầu, migration Ngày 03) |
| Model | [`Payroll.php`](app/Models/Payroll.php) (`createdBy()`/`closedBy()` — `belongsTo` phải truyền tay tên cột vì không theo quy ước `user_id`), [`PayrollDetail.php`](app/Models/PayrollDetail.php), [`PayrollDetailComponent.php`](app/Models/PayrollDetailComponent.php) (snapshot `component_name_snapshot`/`calculation_snapshot`, KHÔNG đọc live qua `salaryComponent()` để hiển thị phiếu lương cũ), [`SalaryComponent.php`](app/Models/SalaryComponent.php), [`Holiday.php`](app/Models/Holiday.php) (mới, trước đó bảng `holidays` chưa có Model — hiện vẫn chưa có CRUD/seeder, đang rỗng) |
| Xử lý Backend | [`PayrollRepository.php`](app/Repositories/PayrollRepository.php), [`PayrollDetailRepository.php`](app/Repositories/PayrollDetailRepository.php), [`PayrollService.php`](app/Services/PayrollService.php) (`generateForPeriod()` tự tính lương cho toàn bộ nhân viên active+probation có hợp đồng active — kéo dữ liệu chấm công/nghỉ phép/hợp đồng thật; `calculateWorkedMetrics()` tự tính ngày công quy đổi + tiền OT trực tiếp từ `actual_work_minutes`/`overtime_minutes` theo GIỜ, KHÔNG qua `AttendanceService` (xem Ghi chú thiết kế bên dưới); `close()`/`markAsPaid()` state machine processing→closed→paid), [`PersonalIncomeTaxCalculator.php`](app/Services/Payroll/PersonalIncomeTaxCalculator.php) (biểu thuế TNCN lũy tiến 7 bậc, tách riêng để unit-test độc lập) |
| Giao diện API | [`PayrollController.php`](app/Http/Controllers/Api/V1/PayrollController.php) (`index`/`show`/`generate`/`close`/`markAsPaid`/`mine`), [`GeneratePayrollRequest.php`](app/Http/Requests/Payroll/GeneratePayrollRequest.php), [`PayrollResource.php`](app/Http/Resources/PayrollResource.php)/[`PayrollDetailResource.php`](app/Http/Resources/PayrollDetailResource.php), route [`routes/api/v1/payrolls.php`](routes/api/v1/payrolls.php) (`/me` đặt TRƯỚC `/{payroll}` để tránh khớp nhầm route) — quyền `payroll.view_all`/`payroll.manage`/`payroll.view_own` đã có sẵn từ Ngày 08, không cần thêm permission mới |
| Test | [`PersonalIncomeTaxCalculatorTest.php`](tests/Unit/Services/Payroll/PersonalIncomeTaxCalculatorTest.php) (6 test Unit thuần), [`WorkTimeCalculationServiceTest.php`](tests/Unit/Services/WorkTimeCalculationServiceTest.php) (25 test Unit thuần — mọi ranh giới bậc thang + trần phút trễ + `work_coefficient`/ca chia đôi, dùng chung Attendance/Payroll), [`PayrollTest.php`](tests/Feature/Payroll/PayrollTest.php) (17 test — gọi thẳng `PayrollService`, số liệu kịch bản chính đối chiếu đúng tay tính qua tinker, gồm test khóa lại bug lương-theo-công đã sửa + test khóa quy tắc giới hạn 1.0 ngày công/ngày + test khóa bug chọn nhầm hợp đồng theo status hiện tại + 3 test khóa cổng duyệt OT), [`PayrollControllerTest.php`](tests/Feature/Payroll/PayrollControllerTest.php) (18 test — đi qua HTTP thật + phân quyền theo role, gồm `forEmployee()`/tab Lương-Phép) |
| Thiết kế "ngày công quy đổi" (2026-09-16, theo góp ý người dùng — bản đầu; refactor cùng ngày sang bậc thang, xem dưới) | Lưu/tính NỘI BỘ theo GIỜ, hiển thị/tính lương theo NGÀY CÔNG cho nhân viên lương tháng. Đơn giá giờ OT tính riêng theo `standard_work_minutes` của đúng ca hôm OT xảy ra, không còn hằng số 8h cố định cho mọi nhân viên — phần này KHÔNG đổi. |
| **Refactor "ngày công quy đổi" → bậc thang dùng chung** (2026-09-16, theo yêu cầu người dùng; sửa lại cùng ngày sau khi phát hiện bug ca chia đôi) | Thay công thức tuyến tính (`actual_work_minutes ÷ standard_work_minutes`, tối đa 1.0/ngày) bằng [`WorkTimeCalculationService::dayEquivalentFor()`](app/Services/WorkTimeCalculationService.php) — **bậc thang theo % giờ làm** so với `standard_work_minutes` CỦA ĐÚNG CA hôm đó (100%→1.0, 75%→0.75, 50%→0.5, 25%→0.25, dưới 25%→0), kết hợp **trần theo phút đi muộn/về sớm** (≤15p không phạt, 15-30p trần 0.75, 30-60p trần 0.5, >60p trần 0.25), **NHÂN thêm với `work_coefficient` của ca đó** — công thức đầy đủ: `min(bậc giờ làm, trần phút trễ) × work_coefficient`. `work_coefficient` **VẪN đang được dùng** (bản đầu tiên định bỏ hẳn, nhưng bug thật đã vấp: nhân viên chia ca sáng+chiều mỗi ca 0.5 công làm đủ CẢ 2 ca bị cộng thành 2.0 thay vì đúng 1.0 nếu bỏ nó — 2 khái niệm ĐỘC LẬP: bậc thang trả lời "làm đủ/thiếu bao nhiêu SO VỚI CHÍNH ca đó", `work_coefficient` trả lời "ca này đáng bao nhiêu phần của 1 ngày", phải nhân chứ không thay thế nhau). Hằng số đặt cứng trong Service (không có bảng DB/trang Admin). **Dùng CHUNG cho cả 2 module** — thay thế công thức đọc `work_coefficient` trực tiếp cũ trong `AttendanceService::summarizeHistory()` (mục 18) LẪN công thức tuyến tính cũ trong `PayrollService::calculateWorkedMetrics()` (mục 27), giải quyết dứt điểm phân kỳ "2 module tính ngày công khác nhau" đã ghi chú trước đó. OT vẫn tính riêng qua `overtime_minutes`, không đổi bởi refactor này (xem dòng "Cổng duyệt OT" bên dưới cho thay đổi khác của OT) |
| **Cổng duyệt chấm công** (2026-09-21, theo yêu cầu người dùng) | `calculateWorkedMetrics()` chỉ tính bản ghi `attendances.approval_status=approved`; `generateForPeriod()` chặn tạo bảng lương khi kỳ đó còn bản ghi đã chấm công ra mà chưa duyệt (422, thông báo số bản ghi còn lại). Chi tiết + lý do xem mục 16 hàng "Duyệt chấm công". Test: `PayrollTest::test_only_approved_attendance_counts_toward_salary` và 2 test chốt chặn |
| **Cổng duyệt OT** (2026-09-21, theo yêu cầu người dùng) | OT chỉ được **TRẢ LƯƠNG** khi (1) `Attendance.overtime_approved=true` (nhân viên gửi yêu cầu qua `AttendanceAdjustment` type `overtime`, HR duyệt — mục 17) VÀ (2) đạt `PayrollService::OVERTIME_MINIMUM_PAYABLE_MINUTES=25` phút (tương đương 30 phút THỰC TẾ trước khi trừ `AttendanceService::OVERTIME_GRACE_MINUTES=5` phút ân hạn — mục 16, phương án B: ngưỡng áp trên số phút thực tế TRƯỚC khi trừ ân hạn). Không đạt 1 trong 2 điều kiện thì CẢ `overtime_minutes` LẪN `overtime_amount` trả về trong `PayrollDetail` đều = 0 (tránh lệch số phút hiện ra với số tiền thực trả trên phiếu lương) — nhưng cột gốc `Attendance.overtime_minutes` vẫn giữ nguyên số thực, không bị xóa/zero, HR vẫn thấy đúng dữ liệu chấm công gốc |
| Ghi chú — các giả định cần xác nhận lại trước khi dùng số liệu thật | (1) Mốc bậc thuế TNCN + giảm trừ bản thân 11tr + bảo hiểm 10.5% là số liệu **pháp luật**, có thể đã lỗi thời — chưa kiểm tra lại quy định hiện hành. (2) Chưa trừ giảm trừ người phụ thuộc (Employee/Contract chưa có cột lưu số người phụ thuộc). (3) OT tính đồng giá 150%, chưa phân biệt ngày thường/cuối tuần/lễ. (4) `total_payroll_amount` = tổng `net_salary`, không phải gross. (5) Bảng `holidays` đang rỗng, chưa có CRUD/seeder — `standard_work_days` hiện chỉ trừ T7/CN, chưa trừ được ngày lễ thật nào. (6) `generate()` cố tình KHÔNG trả kèm `details` trong JSON — khác `show()` có tải kèm. (7) BHXH/BHYT/BHTN vẫn gộp chung 1 số `insurance_amount` (10.5%) ở Backend — `PayrollPayslipDialog.vue` hiển thị nguyên 1 dòng gộp "BHXH, BHYT, BHTN (10,5%)" (khớp mẫu phiếu lương công ty cung cấp), không tách 3 dòng riêng nữa, không đổi gì DB. (8) Bảo hiểm vẫn tính trên NGUYÊN `insurance_salary` hợp đồng bất kể đi làm bao nhiêu ngày trong tháng — chưa prorate theo công như `base_salary`, có thể ra `net_salary` âm nếu 1 tháng đi làm quá ít |
| Bug đã sửa | (1) `PayrollController::generate()` ban đầu dùng `response()->json(...)` không bọc `"data"` — sửa thành `->response()->setStatusCode(201)`. (2) **Bug tính lương thật** (phát hiện qua người dùng thật báo lỗi, không phải test): công thức cũ lấy NGUYÊN lương hợp đồng rồi chỉ trừ nếu có đơn xin nghỉ phép KHÔNG LƯƠNG đã duyệt — nhân viên đi làm rất ít nhưng không nộp đơn nghỉ phép nào vẫn được tính đủ lương. Sửa: `base_salary` giờ = đơn giá ngày công × (số ngày thực tế đi làm + số ngày nghỉ phép CÓ LƯƠNG đã duyệt). (3) `overtime_amount` quên khai trong `$fillable`/`$casts` của `PayrollDetail.php` lúc mới thêm cột — Eloquent âm thầm bỏ qua khi lưu, luôn ra 0 dù Service tính đúng. (4) Đơn giá giờ OT ban đầu chia cố định cho 8h — sửa theo đúng `standard_work_minutes` của từng ca (phát hiện qua người dùng thật hỏi lại, dẫn tới thiết kế "ngày công quy đổi" ở trên). (5) **Chọn nhầm hợp đồng khi chốt lương retroactive** (phát hiện qua tự rà soát code sau khi thêm tính năng quản lý trạng thái hợp đồng — mục 8): `generateForPeriod()` chọn hợp đồng bằng `where('status','active')` (trạng thái HIỆN TẠI) thay vì hợp đồng hiệu lực TRONG ĐÚNG KỲ đang tính — từ khi có auto-expire (job hằng ngày)/auto-supersede (mục 8), hợp đồng của kỳ vừa qua có thể đã đổi status trước khi HR kịp chốt lương (quy trình bình thường: chốt lương SAU khi tháng đã qua), khiến Payroll vớ nhầm hợp đồng của kỳ SAU (sai lương/BH dùng để tính, sai luôn cả `employee_contract_id` lưu vào chi tiết lương). Sửa bằng hàm riêng `contractDuringPeriod()`: lọc theo `start_date <= kỳ kết thúc` và (`end_date` null hoặc `>= kỳ bắt đầu`), bỏ hẳn điều kiện `status` khỏi câu query này |
| Phiếu lương (Payslip) — giao diện + Xuất PDF (2026-09-16) | [`PayrollPayslipDialog.vue`](resources/js/views/Payroll/PayrollPayslipDialog.vue) dựng lại theo đúng mẫu phiếu lương công ty cung cấp (bảng I. Thông tin chung / II. Phụ cấp / III. Lương thêm giờ - Thưởng / IV. Khoản trừ, rowSpan cột STT/Mục, thanh "THỰC NHẬN" tô xanh cuối bảng) — tái dùng bởi cả [`PayrollDetail.vue`](resources/js/views/Payroll/PayrollDetail.vue) (HR) lẫn [`MyProfilePayslipsTab.vue`](resources/js/views/Me/MyProfilePayslipsTab.vue) (tự phục vụ). Nút "Xuất PDF" tạo file `.pdf` THẬT tải thẳng về máy (khác nút "In / Xuất PDF" cũ chỉ gọi `window.print()` — người dùng phải tự chọn "Lưu dưới dạng PDF" ở hộp thoại in, không phải xuất trực tiếp), dùng `jspdf` + `jspdf-autotable` (thêm mới, xem `package.json`). jsPDF mặc định không có font hỗ trợ dấu tiếng Việt — nhúng riêng font Noto Sans (`public/fonts/NotoSans-Regular.ttf`, tải về từ `google/fonts` repo, ~2MB, chỉ 1 style "normal") qua `addFileToVFS()`/`addFont()`, fetch 1 lần lúc xuất (không bundle vào JS chính). **Bẫy đã tự phát hiện qua kiểm thử thật (render PDF ra ảnh so sánh)**: KHÔNG được dùng `fontStyle: "bold"/"italic"` cho bất kỳ ô nào chứa chữ tiếng Việt khi chỉ nhúng đúng 1 style "normal" — `jspdf-autotable` âm thầm rơi về font mặc định (Helvetica, không dấu) cho ô đó, làm chữ có dấu vỡ dòng/giãn cách sai; nhấn mạnh bằng `fillColor`/`textColor` (không đổi font) thay vì đổi độ đậm chữ. Backend bổ sung `position_name`/`department_name` vào `employee` lồng trong [`PayrollDetailResource.php`](app/Http/Resources/PayrollDetailResource.php) (eager-load thêm ở `PayrollRepository::find()` và `PayrollController::mine()`) để có đủ dữ liệu cho khung "I. Thông tin chung". |
| Chưa làm | `PayrollList.vue`/`PayrollDetail.vue` cho `salary_components`/`holidays` (quản lý qua CRUD); các dòng chi tiết theo từng khoản riêng (Ăn trưa/Xăng xe/Điện thoại/Đi lại, Lương khoán/Thưởng KD/Thưởng KPI, Truy thu/Tạm ứng lương) trong mẫu phiếu lương công ty — hiện gộp chung vào `total_allowance`/`overtime_amount` có sẵn, chưa làm itemized (quyết định 2026-09-16: ưu tiên lên giao diện trước, itemized để sau) |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 28. Cài đặt hệ thống — Ca mặc định (2026-09-23)

| Hạng mục | Chi tiết |
| --- | --- |
| Bối cảnh (theo yêu cầu người dùng) | Trước đó nhân viên MỚI tạo KHÔNG có ca nào cho tới khi HR vào tab "Ca làm việc" (mục 14) tự gán — người dùng muốn có sẵn 1 khung giờ MẶC ĐỊNH (giờ vào-ra, nghỉ trưa từ-đến) cấu hình 1 chỗ trong "Cài đặt", tự áp cho nhân viên mới, ĐỔI ở đây áp dụng NGAY cho toàn bộ nhân viên đang dùng ca mặc định. **Đối chiếu tài liệu**: mục 5 yêu cầu "ca làm việc linh hoạt" — đã hỏi lại người dùng và XÁC NHẬN vẫn giữ khả năng tạo nhiều loại ca khác nhau (mục 12) cho trường hợp đặc biệt (ca đêm, part-time…), "Ca mặc định" chỉ là 1 ca ĐƯỢC ĐÁNH DẤU trong số đó, không thay thế toàn bộ hệ thống multi-shift. |
| Thiết kế cốt lõi | "Ca mặc định" **KHÔNG phải bảng riêng** — vẫn là 1 dòng `work_shifts` bình thường, chỉ thêm cờ `is_default=true` (đúng 1 dòng tại 1 thời điểm, `WorkShiftService::create()`/`update()` tự gỡ cờ khỏi ca khác qua `clearOtherDefaults()`, lưu từng bản ghi qua `save()` — không dùng bulk `->update()` — để `AuditObserver` vẫn ghi vết). **"Đổi áp dụng ngay cho toàn bộ nhân viên"** vì thế xảy ra HOÀN TOÀN TỰ NHIÊN, không cần code lan truyền riêng: mọi nhân viên đang gán ca mặc định cùng trỏ chung 1 `work_shift_id`, sửa giờ trên dòng đó là sửa cho tất cả — CHỈ áp dụng cho người đang dùng CHÍNH ca mặc định, người đã được gán 1 ca KHÁC (ca đêm...) không bị đụng tới. Migration [`2026_09_23_000001_add_default_and_break_times_to_work_shifts_table.php`](database/migrations/2026_09_23_000001_add_default_and_break_times_to_work_shifts_table.php) thêm `is_default`, và đổi `break_minutes` (chỉ 1 số phút, KHÔNG dùng để tính toán ở đâu) → `break_start_time`/`break_end_time` (giờ nghỉ trưa TỪ-ĐẾN rõ ràng, dữ liệu cũ không tự quy đổi được nên để NULL). Ràng buộc an toàn: không tắt `is_active` được khi đang `is_default` (`WorkShiftService::update()` chặn 422 — nếu không, nhân viên mới sẽ bị gán vào ca đã ngừng hoạt động), không xóa được ca đang `is_default` (`delete()` chặn 422) — cả 2 đều bắt "đặt ca khác làm mặc định trước". |
| Nhân viên mới tự động có ca | [`EmployeeShiftAssignmentService::assignDefaultShift()`](app/Services/EmployeeShiftAssignmentService.php) — tìm `WorkShift::default()->active()->first()`, tạo 1 `EmployeeShiftAssignment` (`work_days` = cột `work_days` của chính Ca mặc định nếu đã cấu hình, rơi về T2-T6 nếu NULL — xem mục "Ngày làm việc" bên dưới; `effective_from` = `hire_date`, không giới hạn `effective_to`). Gọi từ [`EmployeeService::create()`](app/Services/EmployeeService.php) trong CÙNG transaction tạo nhân viên. Chưa cấu hình ca mặc định nào → tạo nhân viên vẫn thành công bình thường, chỉ là chưa có ca (giống hành vi cũ, không báo lỗi). |
| **Ngày làm việc trong tuần của Ca mặc định cấu hình được ở Cài đặt** (2026-09-24, theo yêu cầu người dùng: hỏi "trong cài đặt có set ngày làm việc chưa hay T7 CN vẫn chấm công được" → xác nhận T7/CN mặc định BỊ CHẶN chấm công đúng như thiết kế, nhưng ngày làm việc T2-T6 khi đó vẫn đang hard-code, cần cho cấu hình được) | Thêm cột `work_days` (JSON, nullable) vào `work_shifts` — migration [`2026_09_24_000001_add_work_days_to_work_shifts_table.php`](database/migrations/2026_09_24_000001_add_work_days_to_work_shifts_table.php). CHỈ Ca mặc định thật sự dùng cột này; Ca khác/bản gán tay ở tab "Ca làm việc" (mục 14) vẫn tự chọn `work_days` riêng của `EmployeeShiftAssignment` như cũ. `assignDefaultShift()` đọc `$defaultShift->work_days ?: DEFAULT_WORK_DAYS` (hằng số T2-T6 giờ chỉ còn là **phương án dự phòng** khi cột NULL — ca tạo trước migration này hoặc Cài đặt chưa từng lưu lần nào). Đổi giá trị này qua `PUT /work-shifts/{id}` (Settings.vue gửi kèm) → `WorkShiftService::update()` gọi thêm [`EmployeeShiftAssignmentService::resyncOpenEndedWorkDays()`](app/Services/EmployeeShiftAssignmentService.php) áp dụng NGAY cho MỌI bản gán "đang mở" (`effective_to IS NULL`) đang trỏ tới ca này — nhất quán với cách giờ vào/ra đã propagate tự động (đã hỏi lại người dùng, chọn "Áp dụng ngay cho cả người đã gán trước"). **Không đụng** bản gán 1-ngày của "Xin làm ngoài lịch/OT" (`createOneOffAssignment()`, `effective_to` đã chốt cụ thể) — đó là ngoại lệ đã xong việc, không phải "đang theo mặc định" nữa. Validate `work_days` (`StoreWorkShiftRequest`/`UpdateWorkShiftRequest`): mảng số 1-7, không trùng, giống hệt quy tắc đã có ở `Store/UpdateEmployeeShiftAssignmentRequest`. Frontend [`Settings.vue`](resources/js/views/Settings/Settings.vue): thêm `v-btn-toggle multiple` chọn T2...CN (cùng `WEEK_DAYS`/nhãn với [`EmployeeShiftAssignmentsTab.vue`](resources/js/views/Employee/EmployeeShiftAssignmentsTab.vue) — 2 nơi hiển thị cùng khái niệm, giữ khớp nhãn). **T7/CN mặc định BỊ CHẶN chấm công** nếu không nằm trong `work_days` — `AttendanceService::checkIn()` → `resolveActiveAssignment()` không khớp ngày nào thì ném lỗi 422 ngay bước chấm công vào; muốn làm T7/CN thì phải "Xin làm ngoài lịch" trước (mục 17) để có bản gán 1-ngày unlock riêng ngày đó — đây là hành vi ĐÃ CHỦ ĐÍCH, không phải thiếu sót. Test: [`WorkShiftTest.php`](tests/Feature/WorkShift/WorkShiftTest.php) (6 test mới: validate work_days, fallback T2-T6, dùng giá trị đã cấu hình, resync bản gán đang mở, KHÔNG đụng bản gán 1-ngày). |
| **"Gán ca làm việc" KHÔNG còn là bước bắt buộc** (2026-09-24, theo yêu cầu người dùng: "bất kỳ nhân viên nào cũng tự động có giờ làm việc mặc định T2-T6, không còn quản lý/gán ca nữa — muốn làm thêm/làm bù thì xin đơn") | Đã hỏi lại và xác nhận: trang "Ca làm việc" (mục 12) + tab "Gán ca làm việc" (mục 14) **giữ nguyên, không ẩn/xóa** — chỉ không còn là thao tác HR phải làm cho MỖI nhân viên nữa, chỉ dùng khi cần 1 người có lịch RIÊNG hẳn khác mặc định. Lệch với `assignDefaultShift()` (chỉ chạy lúc TẠO MỚI): thêm [`AssignMissingDefaultShift`](app/Console/Commands/AssignMissingDefaultShift.php) (`php artisan shifts:assign-missing-default`, lịch hằng ngày 00:07 sau `contracts:expire` — [`routes/console.php`](routes/console.php)) làm LƯỚI AN TOÀN: quét mọi nhân viên `active`/`probation` **KHÔNG có bất kỳ bản gán ca `active` nào** rồi gọi `assignDefaultShift()` cho từng người — bắt các trường hợp `assignDefaultShift()` lúc tạo KHÔNG xử lý được (nhân viên có TRƯỚC tính năng này, công ty cấu hình Ca mặc định SAU khi đã có nhân viên, hoặc lỡ mất hết ca vì `WorkShiftService::update()` tự gỡ toàn bộ bản gán khi HR tắt `is_active` của Ca đang dùng). **Chỉ thêm mới, không đụng người ĐÃ có bất kỳ bản gán nào** (dù chỉ 1 ca, dù không phủ hết tuần) — không ghi đè/xóa lịch riêng đã có. Đã chạy 1 lần thủ công trên DB dev (gán được cho nhân viên duy nhất còn thiếu ca). Test: [`AssignMissingDefaultShiftTest.php`](tests/Feature/Console/AssignMissingDefaultShiftTest.php) (6 test). |
| Giờ nghỉ trưa TRỪ THẬT vào giờ công (theo yêu cầu người dùng — khác `break_minutes` cũ chỉ hiển thị tham khảo, không đụng vào đâu) | [`AttendanceService::calculateActualWorkMinutes()`](app/Services/AttendanceService.php) — `actual_work_minutes` = khoảng chấm công VÀO-RA trừ đi PHẦN GIAO với `[break_start_time, break_end_time]` của Ca (không giao thì không trừ gì, vd ra về trước giờ nghỉ trưa). Áp dụng ở CẢ 2 nơi tính `actual_work_minutes`: `checkOut()` và `applyAdjustment()` (dùng khi duyệt "Xin điều chỉnh"/"Xin bổ sung chấm công" — mục 17) — cùng 1 hàm, không viết lại. Ca CHƯA khai báo giờ nghỉ trưa (`break_start_time`/`break_end_time` = NULL, gồm mọi ca tạo trước 2026-09-23) thì không trừ gì, giữ nguyên hành vi cũ — không cần quy đổi dữ liệu cũ. |
| Giao diện Frontend | [`Settings.vue`](resources/js/views/Settings/Settings.vue) (route `/settings`, menu "Cài đặt" trong nhóm HỆ THỐNG, quyền `shift.manage`) — **CHỈ hiện đúng 5 field** người dùng yêu cầu (Giờ vào, Giờ ra, Nghỉ trưa từ-đến, và từ 2026-09-24 thêm Ngày làm việc trong tuần), không phải trang riêng ở Backend: gọi thẳng `GET /work-shifts/default` để đọc, rồi `POST`/`PUT /work-shifts` (y hệt API của trang "Ca làm việc") để lưu — ghép thêm `is_default: true` và các field không hiện ở form (tên "Ca mặc định" nếu tạo mới, hoặc giữ nguyên `late_grace_minutes`/`early_leave_grace_minutes`/`work_coefficient` cũ nếu sửa). "Số phút công chuẩn" **KHÔNG cho nhập tay ở màn này** — luôn TỰ TÍNH lại = (giờ ra − giờ vào − giờ nghỉ trưa) mỗi lần lưu (khớp đúng nhãn hiển thị ngay phía trên nút Lưu, tránh lệch giữa cái thấy và cái lưu) — khác trang "Ca làm việc" đầy đủ (`WorkShiftForm.vue`, vẫn cho nhập tay, dùng khi sửa ca không phải mặc định hoặc cần chỉnh chi tiết châm chước/hệ số công, KHÔNG có ô "Ngày làm việc" — chỉ Settings.vue mới cần). `WorkShiftForm.vue` cũng thêm switch "Ca mặc định" + khóa switch "Trạng thái hoạt động" khi đang là ca mặc định (khớp ràng buộc Backend). |
| Database & API | Không có bảng/endpoint MỚI riêng cho "Settings" — tái dùng nguyên `work_shifts` + `GET/POST/PUT /work-shifts...` (mục 12), chỉ thêm `GET /work-shifts/default` (trả `{data: null}` nếu chưa cấu hình, không phải 404 — trạng thái hợp lệ cho lần đầu thiết lập) và cột `work_days` (2026-09-24, xem mục "Ngày làm việc" ở trên). |
| Test | [`WorkShiftTest.php`](tests/Feature/WorkShift/WorkShiftTest.php) (16 test mới cho "Ca mặc định": giờ nghỉ trưa phải đi cặp + đến sau từ, `/work-shifts/default`, đúng 1 mặc định khi tạo/sửa, chặn tắt hoạt động/xóa ca mặc định, nhân viên mới tự động có ca, validate/fallback/resync `work_days`), [`AttendanceTest.php`](tests/Feature/Attendance/AttendanceTest.php) (trừ giờ nghỉ trưa lúc checkout, không trừ khi không giao nhau), [`AttendanceAdjustmentTest.php`](tests/Feature/Attendance/AttendanceAdjustmentTest.php) (trừ giờ nghỉ trưa khi duyệt "Bổ sung chấm công"). |
| Chưa làm | Chưa có nút "reset về ca mặc định" cho nhân viên đã bị gán lệch (vd HR gán tay 1 ca khác rồi muốn quay lại theo mặc định — hiện phải Xóa bản gán tay đó thủ công ở tab "Ca làm việc"). |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 29. Nghỉ phép — Tích lũy theo tháng + thưởng thâm niên (2026-09-24)

| Hạng mục | Chi tiết |
| --- | --- |
| Bối cảnh (theo yêu cầu người dùng) | Người dùng phát hiện `allocated_days` trước đó cấp THẲNG đủ `annual_entitlement_days` (12) ngay từ dòng `LeaveBalance` đầu tiên — sai với BLLĐ Điều 113 (chưa làm đủ 12 tháng thì phép năm phải theo tỷ lệ số tháng đã làm) và Điều 114 (đủ 5 năm làm việc thì +1 ngày/năm, không hề có trong code). Đã hỏi lại và CHỐT qua nhiều vòng câu hỏi (+ 1 lần sửa lại công thức năm đầu sau khi người dùng chỉ ra bản đầu tiên vẫn còn sai): (1) Năm dương lịch ĐẦU TIÊN đi làm tích lũy CỨ ĐỦ 30 NGÀY làm việc tính từ đúng ngày vào làm (không phải theo tháng dương lịch — bản đầu tôi làm tính theo tháng, cho ngay 1 ngày từ hôm đầu tiên đi làm, SAI, đã bị bắt lỗi và sửa lại); từ năm thứ 2 trở đi cấp ĐỦ ngay lập tức — dùng hết thì hết, KHÔNG có cơ chế bù lại giữa năm dù làm thêm bao nhiêu tháng (đã xác nhận bằng ví dụ cụ thể: dùng hết 12/12 tháng 3 thì qua tháng 4 vẫn là 0/12, phải chờ sang năm sau). (2) Đồng bộ HẰNG NGÀY (không phải hằng tháng) vì (3) thưởng thâm niên phải tính ĐÚNG ngày tròn năm gia nhập (hire_date anniversary), không phải đầu năm dương lịch. |
| Thiết kế cốt lõi | Toàn bộ công thức gom về đúng 1 hàm THUẦN (không đụng DB) [`LeaveAccrualService::targetAllocatedDays()`](app/Services/LeaveAccrualService.php): `year === hire_date.year` (năm đầu) → cứ ĐỦ 30 NGÀY làm việc (tính bằng `diff()->days` từ đúng ngày vào làm, KHÔNG phải theo tháng dương lịch) thì +1 ngày phép — ngày đầu tiên đi làm CHƯA có ngày phép nào; không cần chặn trần riêng vì số ngày từ lúc vào làm tới hết năm đó (tối đa ~366) chia 30 làm tròn xuống tự nhiên không bao giờ vượt quá 12. `year > hire_date.year` (từ năm 2) → cấp đủ `annual_entitlement_days` + thưởng thâm niên (`intdiv(số năm làm tính đến asOf, 5)`, dùng `diff()->y` — DateInterval thuần, KHÔNG dùng `diffInYears()` vì Carbon 3 trả về float xấp xỉ, sai lệch đúng ngày biên mốc tròn năm). Ghi/đồng bộ qua `resolveOrSyncBalance()` — **idempotent, chỉ TĂNG `allocated_days`, không bao giờ giảm** (an toàn gọi lại nhiều lần/nhiều nơi), không đụng `used_days`/`carried_forward_days`/`adjusted_days`. Loại phép `annual_entitlement_days = 0` ("Nghỉ khác theo chế độ/luật", "Nghỉ không lương" — mục 30) luôn trả về 0 — không tích lũy/thưởng gì (giữ đúng hành vi cũ, mục 19). |
| Nơi dùng công thức | (1) [`LeaveRequestService::create()`](app/Services/LeaveRequestService.php) — thay `LeaveBalanceRepository::findOrCreateForYear()` cấp thẳng bằng `LeaveAccrualService::resolveOrSyncBalance()` khi nộp đơn. (2) `listBalancesForEmployee()` — khi CHƯA có dòng `LeaveBalance` nào, hiển thị (KHÔNG lưu) đúng `targetAllocatedDays()` tại HÔM NAY thay vì `annual_entitlement_days` thô. (3) Job hằng ngày [`SyncLeaveAccrual`](app/Console/Commands/SyncLeaveAccrual.php) (`php artisan leave:sync-accrual`, lịch `dailyAt('00:20')` — [`routes/console.php`](routes/console.php)) quét mọi nhân viên `active`/`probation` × mọi `LeaveType::active()` có `annual_entitlement_days > 0`, gọi `resolveOrSyncBalance()` cho từng cặp — đây là nơi DUY NHẤT khiến mốc thâm niên/30-ngày mới TỰ ĐỘNG cập nhật cho nhân viên KHÔNG nộp đơn phép trong ngày đó. `LeaveBalanceRepository::findOrCreateForYear()` đổi chữ ký, nhận `$initialAllocatedDays` do Service tính sẵn truyền vào (Repository chỉ lo lưu, không còn tự quyết định giá trị). |
| Database & API | Không có bảng/cột MỚI — dùng nguyên `leave_balances.allocated_days` đã có (mục 19), chỉ đổi CÔNG THỨC tính giá trị ghi vào đó. Dữ liệu `LeaveBalance` ĐÃ TẠO TRƯỚC 2026-09-24 (theo công thức cũ) **không tự động backfill lại** — job chỉ TĂNG khi công thức mới cho ra số LỚN HƠN số đang lưu, giữ nguyên nếu đã bằng/lớn hơn (an toàn, không hạ quyền lợi nhân viên đã có). |
| Test | [`LeaveAccrualServiceTest.php`](tests/Unit/Services/LeaveAccrualServiceTest.php) (Unit, 11 test: công thức thuần — tích lũy năm đầu, trần, cấp đủ từ năm 2, thâm niên đúng/trước ngày tròn năm, loại phép không quota, năm trước khi vào làm), [`LeaveAccrualSyncTest.php`](tests/Feature/Leave/LeaveAccrualSyncTest.php) (Feature, 7 test: `resolveOrSyncBalance()` qua DB thật, job `leave:sync-accrual` — tạo/tăng/idempotent/bỏ qua nghỉ việc/bỏ qua loại phép không quota). Cập nhật `makeEmployee()` mặc định `hire_date` sang 3 năm trước (thay vì `now()`) ở [`LeaveRequestTest.php`](tests/Feature/Leave/LeaveRequestTest.php)/[`LeaveApprovalTest.php`](tests/Feature/Leave/LeaveApprovalTest.php) — các test đó KHÔNG nhắm vào tích lũy nên cần nhân viên "đã qua năm đầu" mặc định để không bị ảnh hưởng; thêm test riêng cho nhân viên mới (`hire_date` cùng năm) ở đúng chỗ cũ đã test sai (`test_balances_mine_shows_full_entitlement_when_no_balance_row_yet`). |
| Frontend | [`EmployeeSalaryLeaveTab.vue`](resources/js/views/Employee/EmployeeSalaryLeaveTab.vue) (bảng, HR xem) không cần sửa — hiển thị `allocated_days`/`available_days` ĐỘNG từ API. [`LeaveRequests.vue`](resources/js/views/Leave/LeaveRequests.vue) (thẻ "Quỹ phép còn lại", nhân viên tự xem) **CẦN sửa** — lọc thẻ cũ `.filter((b) => b.allocated_days > 0)` vô tình ẨN LUÔN thẻ khi nhân viên mới chưa tích lũy được ngày nào (`allocated_days = 0` giờ là trạng thái HỢP LỆ, không còn nghĩa là "loại phép này không áp dụng" như trước 2026-09-24) — trông như tính năng bị hỏng. Đổi lọc sang `leave_type.annual_entitlement_days > 0` (loại phép CÓ áp dụng quỹ hay không, không phải đã tích được bao nhiêu) + thêm nhánh hiển thị riêng "đang tích lũy" (màu `info`, không phải `error`) khi `allocated_days <= 0`. |
| Sự cố dữ liệu dev DB (bài học, không phải code) | Lúc code đang sửa dở công thức năm đầu (từ "theo tháng" sang "theo 30 ngày"), tôi có chạy `leave:sync-accrual` trên DB dev thật GIỮA 2 lần sửa — vài nhân viên mới hire bị "kẹt" lại số sai (1 hoặc 12) vì cơ chế "chỉ tăng không giảm" của `resolveOrSyncBalance()` không tự hạ về đúng sau khi công thức được sửa tiếp. Đã dò và sửa tay 3 dòng bị kẹt (đều `used_days=0`, an toàn). **Rút kinh nghiệm**: không chạy job đồng bộ trên DB thật cho tới khi công thức đã chốt HOÀN TOÀN với người dùng, tránh phải dọn tay lại. |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 30. Nghỉ phép — Gộp danh mục loại phép còn 3 nhóm (2026-09-24)

| Hạng mục | Chi tiết |
| --- | --- |
| Bối cảnh (theo yêu cầu người dùng) | "bỏ những cái như thai sản gì đi chỉ có nghỉ phép có lương và nghỉ không lương hoặc nghỉ khác theo chế độ hay luật" — thu gọn từ 5 loại (`annual`/`sick`/`maternity`/`paternity`/`unpaid`) còn 3 (`annual`/`other`/`unpaid`), gộp `sick`+`maternity`+`paternity` cũ thành 1 loại `other` ("Nghỉ khác theo chế độ/luật"). 2 tài liệu dự án không quy định danh mục loại phép cụ thể (chỉ nói chung "quản lý nghỉ phép") — không có mâu thuẫn cần dừng lại hỏi. |
| Thiết kế cốt lõi | [`LeaveTypeSeeder.php`](database/seeders/LeaveTypeSeeder.php) đổi còn 3 dòng — áp dụng cho môi trường MỚI/test (`RefreshDatabase` + `seed()`). Môi trường ĐÃ có dữ liệu cũ (dev/staging/prod) dọn qua migration [`2026_09_24_000002_consolidate_leave_types.php`](database/migrations/2026_09_24_000002_consolidate_leave_types.php) — thêm dòng `other`, XÓA THẬT (không chỉ tắt `is_active`) 3 dòng cũ vì đã kiểm tra dev DB không có `leave_requests`/`leave_balances` nào tham chiếu; nếu môi trường khác lỡ có dữ liệu tham chiếu, FK `restrictOnDelete()` tự chặn migration lại (không âm thầm mất dữ liệu) — cần xử lý tay trước khi chạy lại. Yêu cầu đính kèm giấy tờ bắt buộc (trước đây áp cho `sick`/`maternity`/`paternity`) chuyển sang áp cho đúng 1 code `other` — cả 2 nơi validate: [`StoreLeaveRequest::withValidator()`](app/Http/Requests/Leave/StoreLeaveRequest.php) (backend) và `attachmentRequired` computed trong [`LeaveRequests.vue`](resources/js/views/Leave/LeaveRequests.vue) (frontend, hiện ô upload) — **phải sửa cả 2, thiếu 1 bên sẽ lệch giữa UI và validate thật**. |
| Test | [`LeaveTypeTest.php`](tests/Feature/Leave/LeaveTypeTest.php) (sửa 2 test theo code mới), [`LeaveRequestTest.php`](tests/Feature/Leave/LeaveRequestTest.php) (3 test đính kèm giấy tờ đổi từ `code=sick` sang `code=other`, đổi tên test cho khớp). |
| Khóa lựa chọn "Nghỉ phép năm" khi hết quỹ (2026-09-24, theo yêu cầu người dùng: "không có ngày nghỉ phép năm thì không cho tạo đơn với loại nghỉ phép năm") | [`LeaveRequests.vue`](resources/js/views/Leave/LeaveRequests.vue) — dropdown "Loại phép" đối chiếu `rawLeaveTypes` (danh sách thô từ API) với `balances` (số dư mới nhất, nạp lại MỖI LẦN mở form) qua computed `leaveTypeOptions`: loại phép CÓ giới hạn quỹ (`annual_entitlement_days > 0`) mà `available_days <= 0` thì gắn `props: {disabled: true}` (Vuetify tự khóa item trong danh sách) + đổi nhãn thêm "— hết quỹ, không thể chọn". Chỉ chặn ở UI (trải nghiệm), **backend giữ nguyên** validate cũ (`LeaveRequestService::create()` đã chặn đúng trường hợp này qua so `totalDays > availableDays`) làm lưới an toàn cuối. |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 31. Sửa lỗi "Attempt to read property id on null" — bản gán ca mồ côi (2026-09-24)

| Hạng mục | Chi tiết |
| --- | --- |
| Bối cảnh (lỗi thật, người dùng báo qua log) | HR xóa 1 Ca làm việc (`DELETE /work-shifts/{id}`) đang còn nhân viên gán vào — [`WorkShiftService::delete()`](app/Services/WorkShiftService.php) TRƯỚC ĐÂY chỉ chặn xóa ca MẶC ĐỊNH, không hề gỡ các `EmployeeShiftAssignment` còn trỏ tới Ca sắp xóa (khác `update()` khi tắt `is_active` — có gọi `removeAllForWorkShift()`). Ca bị xóa mềm nhưng bản gán còn sót ("mồ côi") — quan hệ `$assignment->workShift` trả về `null` (SoftDeletes lọc khỏi query mặc định), mọi nơi đọc thẳng `->workShift->id`/`->workShift->start_time` mà không kiểm tra null đều sập trang với lỗi PHP "Attempt to read property ... on null". Ảnh hưởng ít nhất 3 hàm: `AttendanceService::dailyOverview()` ("Tổng quan chấm công"), `listActiveAssignmentsForDate()` (dùng chung bởi `history()`/`checkIn()`/`checkOut()`/`resolveActiveAssignment()`). |
| Sửa TẬN GỐC (chặn phát sinh mồ côi mới) | [`WorkShiftService::delete()`](app/Services/WorkShiftService.php) giờ gọi `removeAllForWorkShift()` TRƯỚC khi xóa Ca (bọc `DB::transaction`) — giống hệt cách `update()` đã làm khi tắt `is_active`. Đồng thời phát hiện Ca lỗi TƯƠNG TỰ ở chiều ngược lại: [`EmployeeService::delete()`](app/Services/EmployeeService.php) xóa nhân viên cũng KHÔNG gỡ bản gán ca của họ — thêm [`EmployeeShiftAssignmentService::removeAllForEmployee()`](app/Services/EmployeeShiftAssignmentService.php) (cùng khuôn với `removeAllForWorkShift()`, xóa từng bản ghi qua `delete()` thường để giữ audit log) và gọi trong `EmployeeService::delete()`. |
| Lưới an toàn (chặn crash dù mồ côi phát sinh từ đường khác/dữ liệu cũ) | [`AttendanceService::listActiveAssignmentsForDate()`](app/Services/AttendanceService.php) — lọc bỏ bản gán có `workShift === null` TRƯỚC `sortBy()` (sửa ở ĐÚNG 1 chỗ, tự bảo vệ mọi hàm dùng chung: `history()`/`checkIn()`/`checkOut()`/`resolveActiveAssignment()`). `dailyOverview()` (dùng repository query khác, không đi qua hàm trên) lọc riêng: bỏ assignment có `employee === null` HOẶC `workShift === null` trước khi `unique()`/dựng `$row`. |
| Dữ liệu dev DB đã dọn tay | Xác nhận qua log lỗi thật (`storage/logs/laravel.log`, 2026-09-23 15:42): Ca id=2 ("Ca chiều") bị xóa lúc 15:42:37 để lại 2 bản gán mồ côi (nhân viên id 50, 60). Đã gọi `EmployeeShiftAssignmentService::removeAllForWorkShift(2)` dọn sạch, xác nhận `dailyOverview()` chạy lại không còn lỗi. |
| Test | [`WorkShiftTest.php`](tests/Feature/WorkShift/WorkShiftTest.php) (`test_deleting_work_shift_removes_it_from_all_employees`), [`EmployeeTest.php`](tests/Feature/Employee/EmployeeTest.php) (`test_deleting_employee_removes_their_shift_assignments`), [`AttendanceOverviewTest.php`](tests/Feature/Attendance/AttendanceOverviewTest.php) (`test_overview_ignores_orphaned_assignment_pointing_to_a_deleted_work_shift` — ép mồ côi bằng xóa mềm THẲNG, bỏ qua tầng Service, để test đúng LƯỚI AN TOÀN chứ không chỉ test chỗ đã vá), [`AttendanceHistoryTest.php`](tests/Feature/Attendance/AttendanceHistoryTest.php) (tương tự cho `history()`). |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 32. Trạng thái Online/Offline nhân viên (Realtime, Laravel Reverb) (2026-09-24)

| Hạng mục | Chi tiết |
| --- | --- |
| Bối cảnh (theo yêu cầu người dùng) | "có thể nào làm được chế độ on/off cho nhân viên không ví dụ nhân viên này đang onl trong web thì có chấm xanh chỗ avatar" — hỏi lại 2 phương án (polling "last active" đơn giản, không cần hạ tầng mới _vs_ WebSocket thật qua Laravel Reverb) bằng AskUserQuestion, người dùng chọn **"Thật sự real-time: WebSocket (Laravel Reverb)"**. **Đối chiếu tài liệu**: mục 3.1 STT 9 "Thông báo Realtime" ghi "Gửi thông báo qua WebSocket (Laravel Echo/Pusher) ... khi có sự kiện duyệt đơn/lương" — đúng hướng công nghệ (Echo/Reverb tương thích giao thức Pusher) nhưng PHẠM VI khác: tài liệu mô tả THÔNG BÁO nghiệp vụ (duyệt đơn, lương), còn tính năng này là hiển thị ONLINE/OFFLINE — ngoài phạm vi mô tả cụ thể, đã nêu rõ giả định và người dùng xác nhận chọn hướng WebSocket nên coi là được chấp thuận triển khai. Đây là tính năng realtime ĐẦU TIÊN của dự án — trước đó chưa có hạ tầng broadcasting nào. |
| Thiết kế cốt lõi | Cài `laravel/reverb` (WebSocket server tương thích giao thức Pusher, chạy như 1 service Docker riêng, KHÔNG chạy chung tiến trình với `app`/`queue`). Dùng "presence channel" (kênh biết CHÍNH XÁC ai đang có mặt, tự bắn sự kiện khi có người vào/ra) tên `presence.online-employees`, khai báo quyền truy cập + payload thành viên ở [`routes/channels.php`](routes/channels.php) — **MỌI** `User` đã đăng nhập đều JOIN/XEM được (kể cả tài khoản hệ thống không gắn `employee`, vd `admin@qlns.local` seed sẵn — phát sinh lỗi 403 thật khi thiết kế ban đầu chặn cứng, đã sửa), nhưng chỉ user NÀO CÓ `employee` liên kết mới có `employee_id` khác null trong payload (`full_name`/`avatar` cũng theo `employee`, rơi về `user_name` nếu không có) — tức chỉ người đó mới khiến 1 DÒNG trong bảng Nhân viên thật sự hiện chấm xanh, còn Admin/HR "online" nhưng không tương ứng dòng nào thì không hiện gì cả (đúng ý, vì Admin không phải 1 dòng trong bảng). Đăng ký broadcasting qua API fluent `Application::configure()->withBroadcasting(routes/channels.php, ['prefix' => 'api/v1', 'middleware' => ['auth:api']])` trong [`bootstrap/app.php`](bootstrap/app.php) (Laravel 12, không cần tạo `BroadcastServiceProvider` riêng) — sinh route xác thực kênh tại `POST /api/v1/broadcasting/auth`, dùng ĐÚNG guard `jwt` sẵn có của dự án (không phải Sanctum). |
| Vì sao KHÔNG chạy `reverb:install` tự động | Lệnh cài đặt chính thức của Reverb nhắm vào kiểu đăng ký provider qua `config/app.php` (`// App\Providers\BroadcastServiceProvider::class` dạng comment) — dự án này đăng ký provider qua [`bootstrap/providers.php`](bootstrap/providers.php) (chuẩn Laravel 12 mới), không khớp cơ chế tự động của lệnh cài. Đã đọc source lệnh cài (`vendor/laravel/reverb/.../InstallCommand.php`) và làm THỦ CÔNG từng bước tương đương (publish config, thêm biến `.env`, khai `withBroadcasting()`) để đảm bảo đúng với cấu trúc thật của dự án thay vì chạy 1 lệnh có thể áp sai chỗ. |
| Docker Compose — service `reverb` mới | [`docker-compose.yml`](docker-compose.yml) — thêm service `reverb` (dùng chung image `qlns-app:local`/Dockerfile với `app`/`queue`, lệnh `php artisan reverb:start --host=0.0.0.0 --port=6001`), expose cổng `6001` TRỰC TIẾP ra host (theo đúng khuôn mẫu `mysql`/`redis` đã có trong dự án — không qua Nginx reverse-proxy) qua biến `FORWARD_REVERB_PORT`. `depends_on` mysql/redis healthy giống `app`/`queue`. |
| **2 bộ giá trị Reverb KHÁC NHAU** — bẫy dễ nhầm khi bảo trì | Backend (PHP container gọi `broadcast()` để BẮN sự kiện) dùng `REVERB_HOST=reverb` (tên service trong mạng Docker nội bộ) — nhưng trình duyệt (Echo/Pusher-js CHẠY Ở MÁY NGƯỜI DÙNG, không nằm trong mạng Docker) phải nối tới `localhost:6001` (cổng đã forward ra host). Vì vậy có 2 bộ biến riêng trong [`.env`](.env)/[`.env.example`](.env.example): `REVERB_HOST`/`REVERB_PORT`/`REVERB_SCHEME` (phía PHP, `.env` runtime — đổi xong chỉ cần restart container) và `VITE_REVERB_HOST`/`VITE_REVERB_PORT`/`VITE_REVERB_SCHEME`/`VITE_REVERB_APP_KEY` (phía frontend, ĐỌC LÚC BUILD bởi Vite qua `import.meta.env` — đổi xong PHẢI `npm run build` lại thì trình duyệt mới thấy, sửa `.env` không tự áp dụng như biến PHP). Nếu đổi `FORWARD_REVERB_PORT` để tránh xung đột cổng, phải đổi `VITE_REVERB_PORT` tương ứng rồi build lại — đã ghi chú thêm ở [`docs/LOCAL_DEVELOPMENT.md`](docs/LOCAL_DEVELOPMENT.md). |
| Frontend — kết nối & xác thực | [`resources/js/echo.js`](resources/js/echo.js) (mới) — bọc `laravel-echo`+`pusher-js` (`npm install laravel-echo pusher-js`), tạo LƯỜI (`getEcho()`, chỉ mở kết nối WebSocket khi thật sự có nơi gọi lần đầu) nhưng dùng CHUNG 1 kết nối cho TOÀN APP (xem hàng dưới). Đăng nhập hệ thống dùng JWT Bearer token lưu ở `localStorage` (không phải cookie/Sanctum) nên KHÔNG dùng cơ chế xác thực kênh mặc định của Echo (dựa vào cookie) — truyền `authorizer` tùy biến, tự gọi `axios` (module `window.axios` đã có sẵn interceptor tự đính `Authorization: Bearer` ở [`resources/js/bootstrap.js`](resources/js/bootstrap.js)) tới `/api/v1/broadcasting/auth`. |
| Frontend — kết nối theo TOÀN APP, không theo riêng 1 trang (sửa 2026-09-24 sau khi phát hiện lỗi thật: nhân viên đang ở trang "Hồ sơ của tôi" không hề hiện online, vì bản đầu chỉ `join()` lúc `Employees.vue` mount) | [`usePresenceStore.js`](resources/js/stores/usePresenceStore.js) (mới, Pinia) — sở hữu `onlineEmployeeIds`/`connect()`/`disconnect()`, KHÔNG còn đặt trong `Employees.vue`. [`App.vue`](resources/js/App.vue) (root component, sống suốt vòng đời app bất kể đang ở trang nào) `watch(() => auth.isAuthenticated, ..., {immediate: true})` → đăng nhập (hoặc mở app mà token đã có sẵn trong `localStorage`) thì `presence.connect()`, đăng xuất thì `presence.disconnect()`. `connect()` join kênh, lắng 3 sự kiện: `.here(users)` (danh sách đang có mặt lúc mới vào), `.joining(user)`/`.leaving(user)` (cập nhật REALTIME) → gom vào 1 `Set` (khóa bằng `employee_id`, KHÔNG phải `user_id`, khớp `item.id` của bảng nhân viên; lọc bỏ `employee_id === null` — tài khoản không gắn nhân viên). |
| Frontend — hiển thị chấm xanh | [`Employees.vue`](resources/js/views/Employee/Employees.vue) — CHỈ còn đọc `presence.isOnline(item.id)` thẳng trong template (`<v-badge dot :model-value="presence.isOnline(item.id)">` bọc quanh avatar hiện có, ẩn hẳn badge nếu offline, không hiện chấm xám), không tự `join`/`leave`/mount-unmount kết nối nữa — vòng đời kết nối do `App.vue` lo (hàng trên). |
| Database & API | Không có bảng/migration MỚI — "online" là trạng thái TỨC THỜI trong bộ nhớ của Reverb (ai đang giữ kết nối WebSocket), không lưu DB. API mới duy nhất là `POST /api/v1/broadcasting/auth` (do `withBroadcasting()` tự sinh, không phải route viết tay). |
| Kiểm thử đã làm | KHÔNG có Playwright trong môi trường phiên làm việc này — đã kiểm thử bằng kịch bản Node.js dùng `pusher-js` thật (mô phỏng chính xác luồng trình duyệt: đăng nhập lấy JWT → mở WebSocket tới Reverb → xác thực qua `/api/v1/broadcasting/auth` thật) với 2 tài khoản đồng thời: xác nhận `here` trả đúng danh sách, `joining`/`leaving` bắn đúng sự kiện khi 1 bên vào/ra kênh. Dữ liệu test dùng nhân viên/tài khoản `ZZ-PRES-01`/`zz-presence-test@qlns.local` tạo tạm qua tinker rồi XÓA (mềm) ngay sau khi test xong, không đụng dữ liệu thật. Suite PHPUnit đầy đủ (546 test) chạy lại xanh sau khi thêm `withBroadcasting()`/quan hệ mới — không có test PHP riêng cho tính năng này vì bản chất là kết nối WebSocket, không có logic nghiệp vụ nào để unit-test (channel authorization trong `routes/channels.php` chỉ là 1 điều kiện `if` đơn giản). |
| Chưa làm / giới hạn đã biết | Chưa scale nhiều instance Reverb (biến `REVERB_SCALING_ENABLED` có sẵn trong `config/reverb.php` dùng Redis, chưa bật — 1 instance là đủ cho quy mô dev/nội bộ hiện tại). Chưa có debounce/tolerance khi mất mạng chập chờn (mất kết nối là tự động rớt "online" ngay, không có khoảng trễ ân hạn vài giây như nhiều app chat làm) — chấp nhận được vì đây chỉ là tín hiệu tham khảo, không phải dữ liệu nghiệp vụ quan trọng. |
| Danh sách tệp cần sửa khi bảo trì | [`routes/channels.php`](routes/channels.php), [`bootstrap/app.php`](bootstrap/app.php), [`docker-compose.yml`](docker-compose.yml), [`.env`](.env)/[`.env.example`](.env.example), [`resources/js/echo.js`](resources/js/echo.js), [`resources/js/stores/usePresenceStore.js`](resources/js/stores/usePresenceStore.js), [`resources/js/App.vue`](resources/js/App.vue), [`resources/js/views/Employee/Employees.vue`](resources/js/views/Employee/Employees.vue), [`docs/LOCAL_DEVELOPMENT.md`](docs/LOCAL_DEVELOPMENT.md) |

## 33. Thông báo (Notification) — đơn phép mới/kết quả duyệt + live-feed chấm công (2026-09-24)

> **Cập nhật cùng ngày**: ban đầu "kết quả duyệt" gửi CẢ thông báo web LẪN email (`LeaveDecisionMail`) — người dùng sau đó yêu cầu bỏ hẳn email, chỉ còn web. Các hàng bên dưới đã sửa lại cho khớp hành vi hiện tại; xem mục 21 để biết lịch sử phần email đã gỡ.

| Hạng mục | Chi tiết |
| --- | --- |
| Bối cảnh (theo yêu cầu người dùng, làm CÙNG lúc với mục 2 mở rộng RBAC UI) | Người dùng yêu cầu làm cả "Thông báo" và "Phân quyền", đã hỏi lại thứ tự nên làm trước — chốt RBAC trước (rủi ro thấp, chỉ CRUD trên dữ liệu có sẵn), Thông báo sau (rủi ro cao hơn vì phải cắm hook vào `LeaveRequestService`/`LeaveApprovalService`/`AttendanceService` đang chạy ổn). **Đối chiếu tài liệu**: Ke-hoach mục Tuần 13 (Ngày 61-65) mô tả đúng 3 việc đã làm ở đây (đơn phép mới → HR/Manager; kết quả duyệt → nhân viên; check-in/out realtime → HR) + thêm đa kênh Email/In-app (Ngày 64) — **không làm** Ngày 61 (rate limiting, không liên quan notification, để riêng). Đã hỏi lại phạm vi v1 bằng AskUserQuestion, người dùng chọn làm ĐỦ CẢ 3 việc kể cả check-in/out live-feed (phức tạp hơn, xem quyết định thiết kế tách biệt bên dưới). |
| **Quyết định thiết kế cốt lõi — 2 loại "thông báo" KHÁC HẲN nhau, KHÔNG dùng chung 1 cơ chế** | (1) **Thông báo cá nhân** (đơn phép mới/kết quả duyệt) — có PERSIST vào bảng `notifications`, có trạng thái đọc/chưa đọc, hiện ở chuông trên header. (2) **Live-feed chấm công** cho HR — CHỈ broadcast qua WebSocket, **KHÔNG ghi bảng `notifications`** — nếu lưu DB sẽ spam hộp thư của MỌI HR mỗi lần BẤT KỲ nhân viên nào chấm công (khác hẳn nhu cầu "xem trực tiếp đang có ai vào/ra", không phải "hộp thư cá nhân"). 2 luồng dùng 2 Event Laravel riêng, không đi chung qua `NotificationService`. **Cập nhật 2026-09-25**: người dùng sau đó yêu cầu THÊM 1 thông báo cá nhân THẬT khi chấm công VÀO (xem dòng "Chấm công vào → báo người có quyền duyệt" bên dưới) — 2 cơ chế giờ chạy SONG SONG cho cùng 1 sự kiện chấm công vào (live-feed KHÔNG lưu DB vẫn giữ nguyên cho MỌI người xem `attendance.view_all`; thông báo cá nhân MỚI chỉ tới đúng nhóm `attendance.approve`, hẹp hơn nhiều nên không mâu thuẫn với lý do "tránh spam" ban đầu — lý do đó áp dụng cho việc gửi tới TOÀN BỘ `attendance.view_all`, không phải tới nhóm nhỏ `attendance.approve`). |
| **Chấm công VÀO → báo người có quyền duyệt** (2026-09-25, theo yêu cầu người dùng: "nếu nhân viên chấm công sẽ gửi thông báo cho người có quyền duyệt để họ biết") | [`AttendanceService::notifyApprovers()`](app/Services/AttendanceService.php) — gọi ngay sau `AttendanceChecked::dispatch()` trong `checkIn()` (KHÔNG gọi trong `checkOut()` — khớp đúng thiết kế "duyệt được ngay lúc chấm công vào, không cần chờ ra" ở `decideApproval()`, chấm công ra không phát sinh thêm việc cần duyệt). Người nhận: `User::withPermission('attendance.approve')->get()` (mặc định seed: Admin + HR). Type `attendance.pending_approval`, data `{attendance_id}`. Bấm vào thông báo → điều hướng sang "Tổng hợp chấm công" ([`NotificationCenter.vue`](resources/js/components/layout/NotificationCenter.vue) đổi `LEAVE_TYPE_ROUTES` thành `TYPE_ROUTES` tổng quát hơn, không còn ràng buộc theo `leave_request_id`), thêm nhãn hiển thị ở bộ lọc trang "Thông báo" ([`Notifications.vue`](resources/js/views/Notification/Notifications.vue)`::TYPE_LABELS`). Test: [`AttendanceCheckInNotificationTest.php`](tests/Feature/Notification/AttendanceCheckInNotificationTest.php) (mới — đúng người có quyền được báo, Manager không có quyền thì KHÔNG được báo, chấm công RA không tạo thêm thông báo); [`AttendanceLiveFeedTest.php`](tests/Feature/Attendance/AttendanceLiveFeedTest.php) test check-in đổi tên + assertion (trước đó khẳng định "không lưu DB", giờ khẳng định NGƯỢC LẠI có lưu — xem cập nhật ở dòng trên). |
| Backend — Model/Migration | [`Notification.php`](app/Models/Notification.php) (`use HasUuids;` — UUIDv7 tự sinh, khớp cột `id` kiểu UUID đã có sẵn migration từ trước), [`NotificationDelivery.php`](app/Models/NotificationDelivery.php) (PK int thường, theo dõi trạng thái gửi TỪNG kênh — `in_app`/`email`). 2 bảng `notifications`/`notification_deliveries` **đã có sẵn migration từ trước** (chưa từng dùng tới) — không migration mới. |
| Backend — Service trung tâm | [`NotificationService.php`](app/Services/NotificationService.php)::`send(User $recipient, string $type, string $title, string $message, array $data = [], ?Mailable $emailMailable = null, ?string $emailAddress = null)` — luôn tạo `Notification` + `NotificationDelivery(in_app, sent)` + bắn event `NotificationCreated` (realtime); nếu có `$emailMailable` thì thêm `NotificationDelivery(email, **queued** — không phải "sent", vì chỉ chắc chắn đã vào hàng đợi chứ chưa chắc gửi thành công)` rồi `Mail::to($emailAddress ?? $recipient->email)->queue(...)`. `$emailAddress` tách riêng khỏi `$recipient` vì email công việc (`company_email`) có thể khác email đăng nhập. |
| Backend — Theo dõi email gửi lỗi (rẻ, không cần Event/Listener mới) — **hạ tầng chung, hiện KHÔNG có Mailable nào dùng tới** (xem cập nhật đầu mục) | `NotificationService::sendEmail()` vẫn giữ khả năng nhận `$emailMailable` (tham số tùy chọn) — Mailable nào tự khai method `withNotificationDelivery(int $id)` (gắn SAU khi tạo vì lúc constructor còn chưa biết id) + `failed(Throwable $e)` thì được `NotificationService` tự gắn id bản ghi `NotificationDelivery` cho (kiểm bằng `method_exists`), Laravel tự gọi `failed()` khi Queue retry hết (`--tries=3`). `LeaveDecisionMail.php` (Mailable ĐẦU TIÊN và DUY NHẤT dùng cơ chế này) đã bị XÓA cùng ngày — giữ lại hạ tầng này vì `NotificationService` là chỗ dùng chung cho MỌI loại thông báo tương lai, không phải riêng nghỉ phép. |
| Backend — 2 Event mới | [`NotificationCreated.php`](app/Events/NotificationCreated.php) (`ShouldBroadcastNow`, kênh riêng `private-notifications.{user_id}`, `broadcastAs(): 'notification.created'`). [`AttendanceChecked.php`](app/Events/AttendanceChecked.php) (`ShouldBroadcastNow`, kênh CHUNG `private-attendance.live-feed`, `broadcastAs(): 'attendance.checked'`, payload chỉ employee_id/full_name/avatar/type(in\|out)/at — **không có gì lưu DB**, thuần broadcast). |
| Backend — 3 điểm hook (đều bắn SAU KHI transaction commit, đúng quy ước post-commit đã có sẵn ở email cũ) | (1) [`LeaveRequestService::create()`](app/Services/LeaveRequestService.php) — nộp đơn xong báo `manager?->user`; không có quản lý HOẶC quản lý chưa có tài khoản đăng nhập → fallback báo tất cả `User::withPermission('leave.approve_hr')` (khớp đúng logic bỏ qua cấp 1 đã có ở duyệt đơn). (2) [`LeaveApprovalService::decide()`](app/Services/LeaveApprovalService.php) — Manager duyệt xong (`manager_approved`) → báo HR tới lượt (loại trừ chính người vừa duyệt, tránh tự báo cho mình nếu 1 người vừa là Manager vừa có `leave.approve_hr`); có kết quả CUỐI (`approved`/`rejected`) → báo nhân viên **CHỈ qua web** (`notifyEmployeeDecision()`, không truyền `$emailMailable`/`$emailAddress` cho `NotificationService::send()` — đã bỏ email cùng ngày, xem cập nhật đầu mục). Nhân viên LUÔN có tài khoản đăng nhập tới bước này (`LeaveRequestController::store()` chỉ nhận đơn từ `$request->user()->employee`), nên không còn nhánh dự phòng "chưa có tài khoản" như bản email cũ. (3) [`AttendanceService::checkIn()`/`checkOut()`](app/Services/AttendanceService.php) — tách `return DB::transaction(...)` thành biến trung gian để bắn `AttendanceChecked` SAU khi transaction đóng (trước đó code cũ `return` thẳng trong transaction, phải sửa cấu trúc mới chèn được). |
| Backend — API cho hộp thư cá nhân | [`NotificationController.php`](app/Http/Controllers/Api/V1/NotificationController.php)/[`NotificationRepository.php`](app/Repositories/NotificationRepository.php) — `GET /api/v1/notifications` (phân trang + `unread_count` qua `->additional()`), `PATCH /notifications/{id}/read`, `PATCH /notifications/read-all` — gate `auth:api` thôi (không cần permission riêng, mỗi user chỉ thấy của mình, tương tự các endpoint `/me...`). Đánh dấu đọc thông báo KHÔNG PHẢI của mình → 404 (không tiết lộ có tồn tại hay không, cùng quy ước `abort(404)` đã dùng ở EmployeeContract/EmployeeDocument/EmployeeTransfer). |
| Backend — kênh WebSocket | [`routes/channels.php`](routes/channels.php) thêm `notifications.{userId}` (chỉ đúng chủ mới nghe được) và `attendance.live-feed` (chỉ ai có `attendance.view_all`). Dùng `$user->cachedPermissionCodes()` (mới thêm ở `User.php`, gom logic `Cache::remember("permission:{id}", 60, ...)` vốn trước đây chỉ nằm trong `EnsurePermission` — giờ dùng chung 1 chỗ, tránh 2 nơi tự cache lệch key/TTL). |
| **Bẫy hạ tầng test đã gặp — quan trọng khi thêm channel mới sau này** | `phpunit.xml` đặt `BROADCAST_CONNECTION=null` cho TOÀN BỘ test suite (mặc định an toàn của Laravel, tránh hàng trăm test khác vô tình bắn broadcast thật). `NullBroadcaster::auth()` LUÔN no-op (200 rỗng, KHÔNG hề gọi tới closure trong `routes/channels.php`) — test gọi thẳng `/api/v1/broadcasting/auth` phải tự đổi driver mới đo được đúng logic phân quyền. Đổi `config(['broadcasting.default' => 'reverb'])` LÀ CHƯA ĐỦ: **mỗi driver giữ `$channels` riêng** (không dùng chung), mà `routes/channels.php` đã chạy 1 lần lúc app boot (lúc đó driver mặc định vẫn là `null`) — phải `require base_path('routes/channels.php');` LẦN NỮA sau khi đổi config để đăng ký đúng vào driver hiện tại, nếu không mọi request đều rơi vào nhánh "không khớp channel nào" → luôn 403 bất kể quyền gì (lỗi thật đã gặp: test "HR được phép" báo sai thành 403). Xem [`AttendanceLiveFeedTest.php`](tests/Feature/Attendance/AttendanceLiveFeedTest.php) `setUp()`. |
| Frontend — 2 store realtime mới, dùng chung hạ tầng Echo đã có | [`useNotificationStore.js`](resources/js/stores/useNotificationStore.js) (mảng `notifications` + `unreadCount`, `connect(userId)` join kênh riêng + gọi API tải danh sách ban đầu, có REST action `fetchList/markRead/markAllRead`). [`useAttendanceFeedStore.js`](resources/js/stores/useAttendanceFeedStore.js) (chỉ giữ tối đa 20 sự kiện gần nhất trong bộ nhớ, KHÔNG gọi API nào — `connect(permissions)` tự bỏ qua nếu thiếu `attendance.view_all`, tránh mọi user thường đều cố join rồi bị 403 vô ích). Cả 2 đều theo đúng khuôn `usePresenceStore.js` (mục 32): `disconnect()` CHỈ rời kênh của mình (`getEcho().leave(...)`), KHÔNG tự gọi `disconnectEcho()`. |
| Frontend — sửa lỗi vòng đời kết nối CHUNG (ảnh hưởng cả mục 32, làm TRƯỚC khi thêm 2 store trên) | [`usePresenceStore.js`](resources/js/stores/usePresenceStore.js)'s `disconnect()` TRƯỚC ĐÂY tự gọi `disconnectEcho()` (phá HẲN kết nối WebSocket dùng chung) — nếu thêm nhiều store cùng làm vậy, store chạy sau vô tình MỞ LẠI 1 kết nối mới chỉ để rời kênh trên đó rồi bỏ hoang. Sửa: mỗi store chỉ rời kênh của mình; [`App.vue`](resources/js/App.vue) là nơi DUY NHẤT gọi `disconnectEcho()`, đúng 1 lần, SAU khi cả 3 store đã rời kênh. Cũng tách `App.vue`'s watcher thành 2: 1 cho `presence` (kết nối được NGAY lúc `isAuthenticated` true, không cần chờ gì thêm), 1 cho `notifications`/`attendanceFeed` (phải ĐỢI `auth.user` tải xong qua `fetchMe()` mới có `user.id`/`permissions` để join đúng kênh — nếu dùng chung 1 watcher với presence sẽ join hụt lúc `user` còn `null`, vd ngay sau F5 trang mà token đã có sẵn). |
| Frontend — hiển thị (chuông) | [`NotificationCenter.vue`](resources/js/components/layout/NotificationCenter.vue) (mới, thay nút chuông tĩnh trong [`AppHeader.vue`](resources/js/components/layout/AppHeader.vue)) — `v-badge` số chưa đọc, `v-menu` dropdown liệt kê thông báo gần nhất (chỉ tải lại khi MỞ dropdown, không poll liên tục — thông báo mới tự đẩy vào qua realtime), bấm vào 1 dòng → đánh dấu đã đọc + điều hướng theo `type` (`leave.decided` → trang tự phục vụ "Nghỉ phép"; `leave.pending_manager`/`leave.pending_hr` → trang "Duyệt nghỉ phép" — 2 route KHÁC NHAU, không dùng chung), cuối danh sách có link "Xem tất cả thông báo" sang trang riêng bên dưới. [`AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue) thêm dải chip nhỏ hiển thị vài lượt chấm công gần nhất (chỉ hiện khi có dữ liệu, trang này đã gate `attendance.view_all` ở router nên không cần kiểm tra quyền thêm). |
| **Trang riêng "Thông báo" đầy đủ** (2026-09-24, theo yêu cầu người dùng: "muốn có trang UI thông báo riêng để nhân viên có thể quản lí xem lại thông báo của mình hay admin cũng phải xem lại thông báo trong công ty" — không có trong 2 tài liệu dự án, đã hỏi lại cấu trúc trang qua AskUserQuestion, chọn 1 trang 2 tab thay vì 2 trang riêng) | [`Notifications.vue`](resources/js/views/Notification/Notifications.vue) (route `/notifications`, không cần permission riêng — ai đăng nhập cũng vào được, tab 2 tự ẩn/hiện trong trang) — 2 tab: **"Thông báo của tôi"** (phân trang server-side RIÊNG của trang này, KHÔNG dùng chung mảng của `useNotificationStore.js` — mảng đó phục vụ chuông realtime luôn là trang 1 mới nhất, dùng chung sẽ hỏng nội dung chuông khi sang trang 2+; chỉ mượn `notificationStore.markRead()`/`markAllRead()` để đồng bộ `unreadCount` ở chuông), có nút đánh dấu đã đọc từng dòng/tất cả. **"Toàn công ty"** — chỉ hiện nếu có quyền MỚI `notification.view_all` (mặc định chỉ Admin, seed qua [`PermissionSeeder.php`](database/seeders/PermissionSeeder.php), Admin tự có nhờ cơ chế "toàn quyền" có sẵn trong [`RolePermissionSeeder.php`](database/seeders/RolePermissionSeeder.php) — không cần sửa riêng), CHỈ XEM (không có nút đánh dấu đọc — không hợp lý để 1 người đánh dấu đã đọc thông báo của người khác thay họ), lọc theo loại + tìm theo tên nhân viên nhận. |
| Backend — API cho tab "Toàn công ty" | `GET /api/v1/notifications/all` (gate `notification.view_all`) — [`NotificationController::all()`](app/Http/Controllers/Api/V1/NotificationController.php)/[`NotificationService::listAll()`](app/Services/NotificationService.php)/[`NotificationRepository::paginateAll()`](app/Repositories/NotificationRepository.php) — nạp sẵn `user.employee` (tránh N+1), filter `type`/`search` (tìm theo `full_name` của nhân viên NHẬN thông báo, không phải nội dung thông báo). [`NotificationResource.php`](app/Http/Resources/NotificationResource.php) thêm field `recipient` qua `whenLoaded('user')` — danh sách "của tôi" không nạp quan hệ này nên field tự vắng mặt, không cần Resource riêng cho 2 luồng. |
| Test | [`LeaveNotificationTest.php`](tests/Feature/Notification/LeaveNotificationTest.php) (nộp đơn có/không quản lý, Manager duyệt → báo HR trừ người vừa duyệt, kết quả cuối → thông báo trong-app kèm `notification_deliveries(channel: in_app)` đúng trạng thái + `Mail::assertNothingSent()`, trạng thái trung gian KHÔNG báo nhân viên), [`NotificationInboxTest.php`](tests/Feature/Notification/NotificationInboxTest.php) (chỉ thấy/đọc được thông báo của chính mình, không đọc được của người khác — 404), [`AttendanceLiveFeedTest.php`](tests/Feature/Attendance/AttendanceLiveFeedTest.php) (auth kênh đúng theo quyền — xem bẫy test ở trên, `Event::fake` xác nhận bắn đúng sự kiện, và `assertDatabaseCount('notifications', 0)` sau check-in/out — chứng minh đúng yêu cầu "live-feed không lưu DB"). [`LeaveApprovalTest.php`](tests/Feature/Leave/LeaveApprovalTest.php) có thêm `test_no_mail_sent_when_hr_gives_final_approval`/`test_no_mail_sent_when_rejected` làm lưới chặn hồi quy ngay tại luồng duyệt chính. |
| Kiểm thử thủ công bổ sung (không thay thế PHPUnit, verify đúng đường dây THẬT tới Reverb) | Dùng kịch bản Node.js + `pusher-js` thật (như đã làm ở mục 32): đăng nhập lấy JWT thật, mở WebSocket thật, nộp đơn phép qua API thật → xác nhận client Manager nhận được sự kiện `notification.created` qua kênh riêng NGAY LẬP TỨC, đúng payload — verify tương tự cho cả 2 luồng "Manager từ chối trực tiếp" và "duyệt 2 cấp Manager→HR" (nhân viên KHÔNG nhận gì ở bước `manager_approved` trung gian, chỉ nhận đúng lúc HR duyệt cuối). **Lưu ý cho lần sau**: raw `pusher-js` (không qua Laravel Echo) phải `bind()` đúng TÊN SỰ KIỆN THẬT không có dấu chấm đầu (`notification.created`) — dấu chấm đầu (`.notification.created`) chỉ là quy ước RIÊNG của Echo's `.listen()` để bỏ qua namespace tự thêm, raw pusher-js không tự hiểu quy ước này (lỗi thật đã gặp khi viết kịch bản test, KHÔNG phải lỗi ở code Echo thật trong `useNotificationStore.js`). Dữ liệu test dùng tài khoản/nhân viên `ZZ-` prefix, đã dọn sạch (xóa mềm) sau khi xong. |
| Chưa làm / giới hạn đã biết | Không làm Ngày 61 (rate limiting — NFR riêng, không thuộc phạm vi "Thông báo"). Kết quả duyệt phép KHÔNG còn gửi email (bỏ theo yêu cầu người dùng 2026-09-24, xem mục 21) — cơ chế theo dõi email gửi lỗi (`failed()`) vẫn còn trong `NotificationService` cho các loại thông báo TƯƠNG LAI có dùng email, hiện chưa có Mailable nào gọi tới. Tab "Toàn công ty" chưa có lọc theo khoảng ngày (chỉ type + tên nhân viên) — thêm sau nếu cần. |
| Test (thêm cho trang "Toàn công ty") | [`NotificationAdminViewTest.php`](tests/Feature/Notification/NotificationAdminViewTest.php) — chặn quyền đúng (403 nếu thiếu `notification.view_all`), thấy được thông báo của MỌI user chứ không riêng mình, có field `recipient`, lọc `type`/`search` hoạt động đúng. |
| Danh sách tệp cần sửa khi bảo trì | [`app/Models/Notification.php`](app/Models/Notification.php), [`app/Models/NotificationDelivery.php`](app/Models/NotificationDelivery.php), [`app/Events/NotificationCreated.php`](app/Events/NotificationCreated.php), [`app/Events/AttendanceChecked.php`](app/Events/AttendanceChecked.php), [`app/Services/NotificationService.php`](app/Services/NotificationService.php), [`app/Repositories/NotificationRepository.php`](app/Repositories/NotificationRepository.php), [`app/Http/Controllers/Api/V1/NotificationController.php`](app/Http/Controllers/Api/V1/NotificationController.php), [`app/Http/Resources/NotificationResource.php`](app/Http/Resources/NotificationResource.php), [`routes/api/v1/notifications.php`](routes/api/v1/notifications.php), [`routes/channels.php`](routes/channels.php), [`database/seeders/PermissionSeeder.php`](database/seeders/PermissionSeeder.php), [`app/Services/LeaveRequestService.php`](app/Services/LeaveRequestService.php), [`app/Services/LeaveApprovalService.php`](app/Services/LeaveApprovalService.php), [`app/Services/AttendanceService.php`](app/Services/AttendanceService.php), [`app/Models/User.php`](app/Models/User.php), [`resources/js/stores/useNotificationStore.js`](resources/js/stores/useNotificationStore.js), [`resources/js/stores/useAttendanceFeedStore.js`](resources/js/stores/useAttendanceFeedStore.js), [`resources/js/services/notificationService.js`](resources/js/services/notificationService.js), [`resources/js/views/Notification/Notifications.vue`](resources/js/views/Notification/Notifications.vue), [`resources/js/App.vue`](resources/js/App.vue), [`resources/js/components/layout/NotificationCenter.vue`](resources/js/components/layout/NotificationCenter.vue), [`resources/js/components/layout/AppHeader.vue`](resources/js/components/layout/AppHeader.vue), [`resources/js/components/layout/AppSidebar.vue`](resources/js/components/layout/AppSidebar.vue), [`resources/js/router/index.js`](resources/js/router/index.js), [`resources/js/views/Attendance/AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue) |

## 34. Trang quản lý tự làm mới realtime khi người KHÁC vừa thao tác (2026-09-25)

> Không nằm trong 2 tài liệu dự án (chỉ nói tới thông báo cá nhân + live-feed check-in/out, mục 33) — theo yêu cầu người dùng: "đã có realtime nhưng khi thay đổi vẫn phải F5 lại mới hiển thị", ví dụ cụ thể "duyệt chấm công ở admin nhưng bên tài khoản nhân sự phải F5 lại mới thấy". Khác mục 33 (báo cho ĐÚNG 1 người liên quan), đây là báo cho **TẤT CẢ** người có quyền quản lý trang đó, kể cả người không phải người nhận thông báo cá nhân lần này.

| Hạng mục | Chi tiết |
| --- | --- |
| Nguyên tắc chung (tái dùng cho trang khác sau này) | Bắn 1 event `ShouldBroadcastNow` "rỗng" (không cần payload chi tiết) lên kênh private đã có/mới thêm, gate đúng permission xem trang đó. Frontend: store tương ứng chỉ tăng 1 biến đếm (`ref(0)`, KHÔNG dùng boolean/timestamp — đảm bảo `watch()` luôn bắt được kể cả 2 lần bắn liên tiếp cùng tick) mỗi khi nhận event. Trang gọi `watch(() => store.signal, () => loadData())` — 2-3 dòng, không cần sửa gì trong `loadData()` đã có sẵn. |
| Trang "Tổng hợp chấm công" (nút Duyệt/Từ chối) | [`AttendanceApprovalDecided.php`](app/Events/AttendanceApprovalDecided.php) — dùng LẠI kênh `attendance.live-feed` đã có (mục 33, cùng quyền `attendance.view_all`, cùng audience với `AttendanceChecked`), bắn trong [`AttendanceService::decideApproval()`](app/Services/AttendanceService.php). Frontend: [`useAttendanceFeedStore.js`](resources/js/stores/useAttendanceFeedStore.js) thêm field `approvalSignal` (tăng dần khi nhận `.attendance.approval-decided`), [`AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue) `watch()` field này để `loadData()` lại. |
| Trang "Duyệt nghỉ phép" (nộp đơn mới + Duyệt/Từ chối ở CẢ 2 cấp) | Kênh MỚI `leave-requests.live-feed` (gate `leave.approve_manager` HOẶC `leave.approve_hr`, xem [`routes/channels.php`](routes/channels.php)) — [`LeaveRequestChanged.php`](app/Events/LeaveRequestChanged.php), bắn trong [`LeaveRequestService::create()`](app/Services/LeaveRequestService.php) (đơn mới) và [`LeaveApprovalService::decide()`](app/Services/LeaveApprovalService.php) (bắn KHÔNG ĐIỀU KIỆN ở CẢ 3 kết quả — `manager_approved`/`approved`/`rejected` — khác 2 nhánh thông báo cá nhân ngay phía trên nó vốn chỉ tới ĐÚNG người ở đúng cấp; sự kiện này phải tới CẢ những HR/Manager khác không phải người nhận thông báo lần đó). Frontend: store mới [`useLeaveFeedStore.js`](resources/js/stores/useLeaveFeedStore.js) (field `signal`, cùng khuôn `useAttendanceFeedStore.js`), nối vòng đời connect/disconnect vào [`App.vue`](resources/js/App.vue) (cùng chỗ 3 store realtime khác), [`LeaveManagement.vue`](resources/js/views/Leave/LeaveManagement.vue) `watch()` để `loadData()` lại. |
| Test | [`AttendanceLiveFeedTest.php`](tests/Feature/Attendance/AttendanceLiveFeedTest.php) thêm `test_deciding_approval_dispatches_attendance_approval_decided_event`. [`LeaveLiveFeedTest.php`](tests/Feature/Notification/LeaveLiveFeedTest.php) (mới) — auth kênh đúng theo quyền (403 nếu thiếu cả 2 quyền duyệt), `Event::fake` xác nhận nộp đơn VÀ duyệt đơn đều bắn đúng sự kiện. |
| **Mở rộng (cùng ngày) — 1 Event/kênh DÙNG CHUNG cho TOÀN BỘ trang còn lại** (theo yêu cầu người dùng: "hãy làm toàn bộ trang cũng có realtime") | 2 dòng trên (`AttendanceApprovalDecided`/`LeaveRequestChanged`) mỗi trang 1 Event riêng — không thực tế nếu nhân ra cho ~10 trang còn lại, nên đổi cách: [`ResourceChanged.php`](app/Events/ResourceChanged.php) (event RỖNG, chỉ mang tên `$resource`, KHÔNG payload chi tiết) + 1 kênh THAM SỐ HÓA `resource-sync.{resource}` ([`routes/channels.php`](routes/channels.php), bảng `match($resource)` ánh xạ sang đúng permission — PHẢI khớp `meta.permission` của route tương ứng ở [`router/index.js`](resources/js/router/index.js), sai là rò rỉ/chặn nhầm). Frontend: 1 store DÙNG CHUNG [`useResourceSyncStore.js`](resources/js/stores/useResourceSyncStore.js) (`signals` là object phản ứng `{ [resource]: number }`, `connect(resource)`/`disconnect(resource)` — KHÁC 2 store live-feed ở trên (nối vòng đời App.vue theo phiên đăng nhập), store này **page-scoped**: mỗi trang tự `connect()` lúc `onMounted`, `disconnect()` lúc `onUnmounted`, vì mỗi resource chỉ có ý nghĩa lúc đúng trang đó đang mở. `App.vue` gọi thêm `resourceSync.resetAll()` lúc đăng xuất (dọn `joined` Set phòng trang hiện tại không kịp unmount trước khi mất kết nối chung). |
| 8 resource đã nối (Service bắn `ResourceChanged::dispatch($resource)` ngay sau khi Thêm/Sửa/Xóa, Frontend `watch(() => resourceSync.signals.X, loadData)`) | `departments` ([`DepartmentService`](app/Services/DepartmentService.php) ↔ [`Departments.vue`](resources/js/views/Department/Departments.vue)), `positions` ([`PositionService`](app/Services/PositionService.php) ↔ [`Positions.vue`](resources/js/views/Position/Positions.vue)), `roles` ([`RoleService`](app/Services/RoleService.php) VÀ [`PermissionService`](app/Services/PermissionService.php) — dùng CHUNG 1 resource vì cùng hiện trên [`Roles.vue`](resources/js/views/Role/Roles.vue)/`RolePermissionsDialog.vue`), `payrolls` ([`PayrollService`](app/Services/PayrollService.php)::`generateForPeriod()`/`close()`/`markAsPaid()` ↔ [`PayrollList.vue`](resources/js/views/Payroll/PayrollList.vue)), `employees` ([`EmployeeService`](app/Services/EmployeeService.php)::`create()`/`update()`/`delete()` — KHÔNG gồm `updateAvatar()`, đổi ảnh đại diện không đáng bắn cả trang làm mới — ↔ [`Employees.vue`](resources/js/views/Employee/Employees.vue), bắn cả `fetchData()` lẫn `fetchStats()`), `work_shifts` ([`WorkShiftService`](app/Services/WorkShiftService.php) — DÙNG CHUNG cho CẢ [`WorkShifts.vue`](resources/js/views/WorkShift/WorkShifts.vue) LẪN [`Settings.vue`](resources/js/views/Settings/Settings.vue) vì Cài đặt cũng sửa qua service này, xem mục 28), `attendance_locations` ([`AttendanceLocationService`](app/Services/AttendanceLocationService.php) ↔ [`AttendanceLocations.vue`](resources/js/views/AttendanceLocation/AttendanceLocations.vue)), `attendance_adjustments` ([`AttendanceAdjustmentService`](app/Services/AttendanceAdjustmentService.php)::`requestForEmployee()`/`decide()` ↔ [`AttendanceAdjustments.vue`](resources/js/views/Attendance/AttendanceAdjustments.vue) — mục 17, trang "Duyệt điều chỉnh công" trước đó CHƯA có). |
| Test (mở rộng) | [`ResourceSyncTest.php`](tests/Feature/ResourceSyncTest.php) (mới) — auth kênh đúng cho CẢ 8 resource (Admin qua, nhân viên thường bị chặn cả 8, tên resource lạ luôn bị chặn — bắt lỗi gõ sai trong bảng `match()`), `Event::fake` xác nhận từng Service bắn đúng `resource` sau Thêm/Sửa/Xóa (gọi thẳng Service qua `app(XService::class)`, không qua HTTP — nhanh hơn, đủ để xác nhận đúng dòng dispatch, vì phần xác thực/phân quyền HTTP đã có test riêng ở from mỗi tính năng gốc). |
| Phạm vi ĐÃ làm / CHƯA làm | Đã phủ toàn bộ trang danh sách/quản lý có bảng dữ liệu dùng chung nhiều người xem (10 trang: Tổng hợp chấm công + Duyệt nghỉ phép ở 2 dòng đầu, 8 resource ở dòng trên). **CHƯA làm** (cố tình bỏ qua, không phải sót): các trang CÁ NHÂN 1-người-1-lúc-xem (Đơn của tôi tự phục vụ, Lịch sử chấm công cá nhân, Hồ sơ của tôi, Chấm công) — không có kịch bản "người khác vừa sửa dữ liệu tôi đang xem" nên không cần; `EmployeeDetail.vue` (xem 1 nhân viên cụ thể — hợp đồng/tài liệu/điều chuyển của RIÊNG người đó) chưa nối vì mỗi tab con là 1 luồng dữ liệu khác nhau, cần tách nhỏ hơn resource `employees` hiện có nếu làm sau này. |
| Danh sách tệp cần sửa khi bảo trì | [`app/Events/AttendanceApprovalDecided.php`](app/Events/AttendanceApprovalDecided.php), [`app/Events/LeaveRequestChanged.php`](app/Events/LeaveRequestChanged.php), [`app/Events/ResourceChanged.php`](app/Events/ResourceChanged.php), [`app/Services/AttendanceService.php`](app/Services/AttendanceService.php), [`app/Services/LeaveRequestService.php`](app/Services/LeaveRequestService.php), [`app/Services/LeaveApprovalService.php`](app/Services/LeaveApprovalService.php), [`app/Services/DepartmentService.php`](app/Services/DepartmentService.php), [`app/Services/PositionService.php`](app/Services/PositionService.php), [`app/Services/RoleService.php`](app/Services/RoleService.php), [`app/Services/PermissionService.php`](app/Services/PermissionService.php), [`app/Services/PayrollService.php`](app/Services/PayrollService.php), [`app/Services/EmployeeService.php`](app/Services/EmployeeService.php), [`app/Services/WorkShiftService.php`](app/Services/WorkShiftService.php), [`app/Services/AttendanceLocationService.php`](app/Services/AttendanceLocationService.php), [`app/Services/AttendanceAdjustmentService.php`](app/Services/AttendanceAdjustmentService.php), [`routes/channels.php`](routes/channels.php), [`resources/js/stores/useAttendanceFeedStore.js`](resources/js/stores/useAttendanceFeedStore.js), [`resources/js/stores/useLeaveFeedStore.js`](resources/js/stores/useLeaveFeedStore.js), [`resources/js/stores/useResourceSyncStore.js`](resources/js/stores/useResourceSyncStore.js), [`resources/js/App.vue`](resources/js/App.vue), [`resources/js/views/Attendance/AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue), [`resources/js/views/Leave/LeaveManagement.vue`](resources/js/views/Leave/LeaveManagement.vue), [`resources/js/views/Department/Departments.vue`](resources/js/views/Department/Departments.vue), [`resources/js/views/Position/Positions.vue`](resources/js/views/Position/Positions.vue), [`resources/js/views/Role/Roles.vue`](resources/js/views/Role/Roles.vue), [`resources/js/views/Payroll/PayrollList.vue`](resources/js/views/Payroll/PayrollList.vue), [`resources/js/views/Employee/Employees.vue`](resources/js/views/Employee/Employees.vue), [`resources/js/views/WorkShift/WorkShifts.vue`](resources/js/views/WorkShift/WorkShifts.vue), [`resources/js/views/Settings/Settings.vue`](resources/js/views/Settings/Settings.vue), [`resources/js/views/AttendanceLocation/AttendanceLocations.vue`](resources/js/views/AttendanceLocation/AttendanceLocations.vue), [`resources/js/views/Attendance/AttendanceAdjustments.vue`](resources/js/views/Attendance/AttendanceAdjustments.vue) |

## 35. Dashboard cá nhân mở rộng (Employee — lịch làm việc/công tháng/lương/đơn của tôi) (2026-09-25)

> Theo yêu cầu người dùng, kèm 1 mockup cụ thể (chào hỏi + thẻ Check-in + Công tháng/Phép còn lại/Lương + Đơn của tôi). Bản trước đó (mục 5) chỉ có 3 thẻ số liệu + thông báo cho scope "personal" — mục này thay hẳn nội dung đó.

| Hạng mục | Chi tiết |
| --- | --- |
| Quyết định thiết kế — KHÔNG viết lại logic chấm công ở Dashboard | Thẻ "Hôm nay" tái dùng THẲNG `useCheckIn()` (composable đã có ở trang Chấm công tự phục vụ, xem mục 26/44) — không lặp lại logic GPS/IP/QR/duyệt riêng. `DashboardService::personal()` vì vậy KHÔNG cần trả "today_shifts" — chỉ còn `checked_in_today`/`checked_in_at`/`checked_out_at` (giữ lại, không dùng ở Frontend nữa nhưng không xóa, phòng khi cần). |
| Backend — [`DashboardService.php`](app/Services/DashboardService.php)::`personal()` | `employee_name` (cho câu chào — `/auth/me` KHÔNG có sẵn field này, cố tình không sửa endpoint dùng chung đó, chỉ thêm ở đây). `week_schedule` (T2-CN, tái dùng `AttendanceService::listActiveAssignmentsForDate()` — CHÍNH hàm `listTodayStatusForEmployee()`/`history()` đang dùng để suy ra "ca nào áp dụng đúng ngày", không viết công thức riêng). `monthly_work` + `recent_attendance` (DÙNG CHUNG 1 lần gọi `AttendanceService::history()` — `summary.total_work_days` là SỐ NGÀY CÔNG THẬT qua `WorkTimeCalculationService`, cùng công thức `PayrollService` dùng để trả lương, không tính lại riêng; mẫu số `standard_days` gọi `PayrollService::standardWorkDaysFor()`, đổi từ `private` sang `public` để dùng lại — không viết bản tính ngày công chuẩn thứ 2 dễ lệch số). `recent_requests` (gộp `LeaveRequest` + `AttendanceAdjustment` — 2 bảng riêng, KHÔNG có API chung, lấy 5 gần nhất MỖI loại rồi trộn lại lấy 5 để không bỏ sót loại nào). `latest_payslip` (tái dùng `PayrollService::listPayslipsForEmployee()` đã lọc closed/paid — kỳ "processing" hiện tại KHÔNG tính, vì HR có thể còn sửa số liệu). |
| Frontend | [`PersonalCheckInCard.vue`](resources/js/components/dashboard/PersonalCheckInCard.vue) (mới) — gọi `useCheckIn()`, chỉ cherry-pick `todayShifts`/`submit`/`submitting`/`submittingShiftId`/`submitError` (bỏ qua 5 dialog điều chỉnh/OT/bổ sung không cần trên Dashboard — có nút "Xem đầy đủ trang Chấm công" dẫn qua `/check-in` nếu cần), thêm đồng hồ sống (`setInterval` 30s). [`Dashboard.vue`](resources/js/views/Dashboard.vue) nhánh "personal" viết lại: câu chào + `PersonalCheckInCard` + `StatCards` (đổi "Trạng thái hôm nay" thành "Công tháng này", vì trạng thái chấm công giờ đã có trong thẻ Check-in) + thẻ "Lương gần nhất" (nút dẫn `{ name: 'my-profile', query: { tab: 'payslips' } }`) + "Lịch làm việc tuần này" (dải 7 ngày) + "Bảng công gần nhất"/"Đơn của tôi" (2 cột) + "Thông báo gần đây" (giữ nguyên). [`MyProfile.vue`](resources/js/views/Me/MyProfile.vue) thêm đọc `route.query.tab` lúc khởi tạo để hỗ trợ deep-link (trước đó `tab` luôn mặc định "info"). |
| Test | [`DashboardTest.php`](tests/Feature/DashboardTest.php) thêm 4 test: `week_schedule` đánh dấu đúng "hôm nay" + đúng ca đã gán, `monthly_work`/`recent_attendance` khớp đúng bản ghi đã duyệt (bẫy đã gặp lúc viết test: `history()` cũng dựng dòng từ BẢN GÁN CA như `dailyOverview()`, không phải trực tiếp từ `attendances` — test phải tự tạo `EmployeeShiftAssignment` mới "nhìn thấy" được bản ghi, cùng lớp bẫy với mục 24), `recent_requests` gộp đúng cả 2 loại, `latest_payslip` chỉ tính kỳ closed/paid. |
| Chưa làm / giới hạn đã biết | KHÔNG verify được bằng mắt qua trình duyệt (không có Playwright trong phiên này) — người dùng tự xem qua Vite dev server đang chạy sẵn (cổng 5173, HMR). `week_schedule`/`recent_attendance` gọi `history()`/`listActiveAssignmentsForDate()` — CÙNG lớp bẫy đã sửa ở `dailyOverview()` (mục 24 cập nhật 2026-09-25: bản ghi chấm công có thể "biến mất" nếu bản gán ca/Ca làm việc gốc bị xóa mềm SAU khi đã chấm công) — **CHƯA áp dụng "lưới an toàn" tương tự cho `history()`**, chỉ mới sửa cho `dailyOverview()`. Rủi ro thấp hơn (Dashboard cá nhân chỉ ảnh hưởng đúng 1 người, không phải toàn bộ HR nhìn nhầm), nhưng nên sửa nếu gặp báo lỗi tương tự ở "Lịch sử chấm công". |
| Danh sách tệp cần sửa khi bảo trì | [`app/Services/DashboardService.php`](app/Services/DashboardService.php), [`app/Services/PayrollService.php`](app/Services/PayrollService.php), [`resources/js/views/Dashboard.vue`](resources/js/views/Dashboard.vue), [`resources/js/components/dashboard/PersonalCheckInCard.vue`](resources/js/components/dashboard/PersonalCheckInCard.vue), [`resources/js/views/Me/MyProfile.vue`](resources/js/views/Me/MyProfile.vue) |

## 36. Thao tác hàng loạt (Bulk Actions) — Duyệt chấm công + Duyệt nghỉ phép (2026-09-25)

> Người dùng gửi 1 ảnh tham khảo (brainstorm UX từ ChatGPT) liệt kê nhiều nơi nên có bulk actions (Nhân viên/Chấm công/Nghỉ phép) — đã hỏi lại qua AskUserQuestion để chọn phạm vi làm trước, người dùng chọn **Chấm công + Nghỉ phép**, có thể chọn lẻ hoặc hàng loạt. Nhân viên (export/đổi trạng thái/chuyển phòng ban hàng loạt) CHƯA làm.

| Hạng mục | Chi tiết |
| --- | --- |
| Nền tảng dùng chung — [`DataTable.vue`](resources/js/components/common/DataTable.vue) | Thêm chọn nhiều dòng — TẮT MẶC ĐỊNH (`selectable: false`), không ảnh hưởng các trang khác đang dùng component này. Bật bằng `selectable` + khai `bulkActions` (CÙNG hình dạng với `actions` có sẵn, chỉ khác `onClick(selectedItems, { input })` nhận MẢNG item thay vì 1 item). Thêm `itemValue`/`itemSelectable` (String hoặc Function, đúng kiểu Vuetify's `item-value`/`item-selectable`) — cần thiết vì không phải trang nào cũng có field `id` phẳng (AttendanceOverview.vue mỗi dòng là `{employee, work_shift, attendance, status}`, phải tự truyền `item-value` là hàm `(item) => item.attendance?.id`). Thanh thao tác hàng loạt + hộp xác nhận RIÊNG (không dùng chung state với hộp xác nhận thao tác từng dòng, tránh lẫn lộn 2 luồng). Selection tự xóa khi `items` đổi (đổi trang/đổi bộ lọc) — tránh giữ lựa chọn "ma" trỏ nhầm dòng khác trùng giá trị item-value ở trang mới. |
| Backend — Chấm công | [`AttendanceService::bulkDecideApproval(array $attendanceIds, ...)`](app/Services/AttendanceService.php) — LẶP `decideApproval()` cho từng bản ghi, KHÔNG viết lại luật riêng. Không dừng cả loạt nếu 1 bản ghi lỗi (vd đã được duyệt từ trước bởi người khác) — trả `{succeeded: [...ids], failed: [{id, message}]}`. [`BulkDecideAttendanceApprovalRequest`](app/Http/Requests/Attendance/BulkDecideAttendanceApprovalRequest.php) (cùng luật `DecideAttendanceApprovalRequest` — nhân bản `attendance_ids`). Route `PUT /attendances/bulk-approval` (quyền `attendance.approve`, khai TRƯỚC `/{attendance}/approval`). |
| Backend — Nghỉ phép | [`LeaveApprovalService::bulkDecide(array $leaveRequestIds, ...)`](app/Services/LeaveApprovalService.php) — cùng khuôn, lặp `decide()` (tôn trọng ĐÚNG luật 2 cấp Manager/HR sẵn có, 1 đơn không đúng cấp/không phải cấp dưới thì rơi vào `failed`, không chặn các đơn khác). [`BulkDecideLeaveRequest`](app/Http/Requests/Leave/BulkDecideLeaveRequest.php). Route `PUT /leave-requests/bulk-decide` (quyền `leave.approve_manager,leave.approve_hr`, khai TRƯỚC `/{leaveRequest}/decide`). |
| Frontend | [`AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue): `selectable` CHỈ bật khi `canApprove` (đã có sẵn), dòng chọn được CHỈ khi có bản ghi chấm công (không chọn được "Vắng"/"Nghỉ phép" — không có gì để duyệt). [`LeaveManagement.vue`](resources/js/views/Leave/LeaveManagement.vue): chọn được CHỈ khi đơn đang `pending`/`manager_approved`, cùng điều kiện `hidden` của 2 nút duyệt từng dòng đã có. Cả 2 trang: bulk action KHÔNG throw khi có lỗi 1 phần (API luôn trả 200 kèm succeeded/failed) — tự báo qua `toast.warning()` thay vì coi thất bại 1 phần là lỗi cả thao tác. |
| Test | [`AttendanceBulkApprovalTest.php`](tests/Feature/Attendance/AttendanceBulkApprovalTest.php) (6 test — xác thực/phân quyền, duyệt nhiều bản ghi cùng lúc, "Từ chối" vẫn bắt buộc lý do khi ở dạng hàng loạt, 1 bản ghi lỗi không chặn các bản ghi khác, id không tồn tại bị chặn ở validation). [`LeaveBulkApprovalTest.php`](tests/Feature/Leave/LeaveBulkApprovalTest.php) (4 test — Manager duyệt nhiều đơn cấp dưới cùng lúc, đơn KHÔNG PHẢI cấp dưới rơi vào failed mà không chặn đơn khác, HR từ chối hàng loạt kèm 1 comment chung). |
| Chưa làm / giới hạn đã biết | Trang "Nhân viên" (export/đổi trạng thái/chuyển phòng ban hàng loạt — có trong ảnh tham khảo) CHƯA làm, người dùng chủ động chọn làm Chấm công + Nghỉ phép trước. KHÔNG verify được bằng mắt qua trình duyệt (không có Playwright trong phiên này). |
| Danh sách tệp cần sửa khi bảo trì | [`resources/js/components/common/DataTable.vue`](resources/js/components/common/DataTable.vue), [`app/Services/AttendanceService.php`](app/Services/AttendanceService.php), [`app/Services/LeaveApprovalService.php`](app/Services/LeaveApprovalService.php), [`app/Http/Controllers/Api/V1/AttendanceController.php`](app/Http/Controllers/Api/V1/AttendanceController.php), [`app/Http/Controllers/Api/V1/LeaveRequestController.php`](app/Http/Controllers/Api/V1/LeaveRequestController.php), [`app/Http/Requests/Attendance/BulkDecideAttendanceApprovalRequest.php`](app/Http/Requests/Attendance/BulkDecideAttendanceApprovalRequest.php), [`app/Http/Requests/Leave/BulkDecideLeaveRequest.php`](app/Http/Requests/Leave/BulkDecideLeaveRequest.php), [`routes/api/v1/attendances.php`](routes/api/v1/attendances.php), [`routes/api/v1/leave-requests.php`](routes/api/v1/leave-requests.php), [`resources/js/services/attendanceService.js`](resources/js/services/attendanceService.js), [`resources/js/services/leaveRequestService.js`](resources/js/services/leaveRequestService.js), [`resources/js/views/Attendance/AttendanceOverview.vue`](resources/js/views/Attendance/AttendanceOverview.vue), [`resources/js/views/Leave/LeaveManagement.vue`](resources/js/views/Leave/LeaveManagement.vue) |

---

**Quy ước cập nhật file này**: mỗi khi thêm 1 tính năng/module mới, thêm 1 mục mới theo đúng bảng 5 cột như trên (Giao diện Frontend / Xử lý Backend / Database & API / Test / Danh sách tệp cần sửa khi bảo trì) — không cần đủ cả 5 cột nếu module đó không có, nhưng phải cập nhật ngay trong ngày làm, không để dồn. Đường dẫn file viết dạng link Markdown `[tên](đường/dẫn)` để bấm mở trực tiếp. Cột "Ghi chú" chỉ ghi bẫy/quyết định thật sự ảnh hưởng tới việc bảo trì sau này, không chép lại toàn bộ quá trình thảo luận.
