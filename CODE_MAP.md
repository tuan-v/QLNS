# Bản đồ Mã nguồn dự án QLNS

Tài liệu tra cứu "tính năng nằm ở file nào" — bắt buộc theo [`docs/YEU_CAU_DU_AN_QLNS.html`](docs/YEU_CAU_DU_AN_QLNS.html) mục 6.1 và [`docs/CODING_STANDARDS.md`](docs/CODING_STANDARDS.md). Cập nhật ngay khi thêm/di chuyển một chức năng, không dồn tới cuối dự án.

## Quy trình thêm 1 chức năng mới

Thứ tự làm theo đúng khuôn các module đã có (`Position` là ví dụ gọn nhất, `Employee` là ví dụ đầy đủ nhất) — cứ theo thứ tự này thì hiếm khi bị vỡ ở giữa chừng.

**Backend:**

| Bước | Làm gì | Khi nào bỏ qua được | Ví dụ đã có sẵn |
|---|---|---|---|
| 1. Migration | Tạo bảng mới (nếu chức năng cần lưu dữ liệu mới) | Nếu dùng lại bảng đã có | [`create_provinces_table.php`](database/migrations/2026_09_07_000001_create_provinces_table.php) |
| 2. Model | Class ánh xạ bảng, khai `$fillable`, khai quan hệ (`belongsTo`/`hasMany`) | Không bao giờ bỏ qua nếu có bảng mới | [`Position.php`](app/Models/Position.php) |
| 3. Request (Store/Update) | Validate dữ liệu vào — field nào bắt buộc, định dạng gì | Nếu chức năng chỉ ĐỌC, không có form nhập | [`StorePositionRequest.php`](app/Http/Requests/Position/StorePositionRequest.php) |
| 4. Repository | Viết các hàm truy vấn DB (`paginate()`, `find()`...) | Gần như không bao giờ bỏ qua | [`PositionRepository.php`](app/Repositories/PositionRepository.php) |
| 5. Service | Logic nghiệp vụ (sinh mã, kiểm tra rule đặc biệt) | Nếu chỉ đọc/ghi thẳng không có logic gì thêm — vẫn nên có Service mỏng để nhất quán | [`PositionService.php`](app/Services/PositionService.php) |
| 6. Controller | Ghép nối: nhận request đã validate → gọi Service → trả response | Không bao giờ bỏ qua | [`PositionController.php`](app/Http/Controllers/Api/V1/PositionController.php) |
| 7. Route | Đăng ký URL + gắn quyền (`permission:xxx`) | Không bao giờ bỏ qua | [`routes/api/v1/positions.php`](routes/api/v1/positions.php) |
| 8. Resource | Định dạng lại JSON trả ra (ẩn field nhạy cảm nếu có) | Nếu trả thẳng Model không sao (không có field nhạy cảm) | [`EmployeeResource.php`](app/Http/Resources/EmployeeResource.php) — Position thì **không dùng**, trả thẳng Model |
| 9. Test | PHPUnit — quyền hạn, validate, ca chính | Không nên bỏ qua | [`PositionTest.php`](tests/Feature/Position/PositionTest.php) |

**Frontend (nếu chức năng cần giao diện):**

| Bước | Làm gì | Ví dụ |
|---|---|---|
| 1. Service (`xxxService.js`) | Hàm gọi API tương ứng | [`positionService.js`](resources/js/services/positionService.js) |
| 2. Store (Pinia) | Chỉ cần nếu nhiều trang cùng cần đọc/ghi 1 state chung | [`useEmployeeStore.js`](resources/js/stores/useEmployeeStore.js) |
| 3. View danh sách | Bảng, tìm kiếm, lọc — dùng lại [`DataTable.vue`](resources/js/components/common/DataTable.vue) | [`Employees.vue`](resources/js/views/Employee/Employees.vue) |
| 4. Form Thêm/Sửa | Dùng lại `FormDialog`/`FormSection`/`SearchSelect`/`InputDate`/`InputMoney` — xem mục 10 — gần như không phải viết CSS mới | [`EmployeeForm.vue`](resources/js/views/Employee/EmployeeForm.vue) |
| 5. Router | Thêm route mới trong [`router/index.js`](resources/js/router/index.js), đặt `meta.title` để breadcrumb tự hiện đúng | — |
| 6. Cập nhật `CODE_MAP.md` | Thêm 1 mục mới đúng khuôn 5 cột (xem "Quy ước cập nhật file này" ở cuối) | — |

**Chọn khi nào cần đủ các bước, khi nào rút gọn**: nhìn vào Position — bỏ qua Resource (không có field nhạy cảm) và chưa có Frontend (chưa tới lúc cần). Nhìn vào Employee (mục 8) — đủ cả vì có field nhạy cảm cần ẩn, form phức tạp, có logic nghiệp vụ (chống vòng lặp quản lý).

### Ví dụ 1 luồng chạy thật: bấm "Thêm nhân viên"

Đi từ lúc bấm nút tới lúc bảng hiện nhân viên mới — chia rõ theo phía Frontend/Backend giống 2 bảng ở trên. Chú ý **4 chỗ có thể dừng sớm** (validate/quyền sai thì dừng ngay tại đó, không đi tiếp xuống các bước dưới).

Nhìn nhanh đường đi qua từng file (chi tiết từng bước ở danh sách bên dưới):

```
Frontend (trước khi gửi)
Employees.vue → EmployeeForm.vue → useEmployeeStore.js → employeeService.js → (gửi HTTP)

Backend (xử lý)
routes/api/v1/employees.php → Middleware (auth:api, permission:employee.create)
    → StoreEmployeeRequest → EmployeeController → EmployeeService
    → EmployeeRepository → Employee (Model) → EmployeeResource → (trả JSON)

Frontend (sau khi nhận response)
useEmployeeStore.js → EmployeeForm.vue (toast + đóng modal) → Employees.vue (gọi lại GET, làm mới bảng)
```

**Phía Frontend — trước khi gửi đi:**

1. `Employees.vue` — bấm nút "Nhân viên" → `openCreate()` → mở `EmployeeForm.vue` (modal trống, `employee = null`).
2. Người dùng điền form, bấm "Thêm mới" → `submit()` trong `EmployeeForm.vue`.
3. **[Dừng sớm #1]** `await formRef.value.validate()` — chạy toàn bộ `:rules` phía client (`notEmpty`, `isEmail`...). Sai thì dừng ngay tại đây, **chưa gửi request nào lên server** — verify bằng cách mở tab Network, bấm Lưu với form rỗng, thấy 0 request.
4. Hợp lệ → `store.create(payload)` (`useEmployeeStore.js`) → `employeeService.create()` → `axios.post('/api/v1/employees', payload)`.
5. Request rời trình duyệt. `bootstrap.js`'s interceptor tự gắn header `Authorization: Bearer <token>` + bật `useLoadingStore()` (thanh loading trên cùng chạy).

**Phía Backend — nhận và xử lý request:**

6. Laravel nhận tại `routes/api/v1/employees.php` — khớp route `POST /` → `EmployeeController::store`.
7. **[Dừng sớm #2]** Middleware `auth:api` kiểm tra token hợp lệ chưa → sai thì trả 401, dừng, **không tới Controller**.
8. **[Dừng sớm #3]** Middleware `permission:employee.create` kiểm tra quyền → sai thì trả 403, dừng.
9. **[Dừng sớm #4]** `StoreEmployeeRequest` tự động chạy validate (Laravel tự gọi trước khi vào hàm Controller, vì nó được khai làm tham số của `store()`) → sai thì trả 422 kèm `{"errors": {...}}`, dừng, **không tới Controller**.
10. Vào `EmployeeController::store()` — data đã chắc chắn hợp lệ. Gọi `EmployeeService::create($request->validated())`.
11. `EmployeeService::create()` — tự sinh mã `NV00x` (`generateCode()`), mở `DB::transaction()`, gọi `EmployeeRepository::create()`.
12. `EmployeeRepository::create()` → `Employee::create($data)` — Eloquent chỉ ghi những field có trong `$fillable` của `Employee.php`, field lạ (nếu có) bị bỏ qua.
13. `$employee` (Model vừa tạo) trả ngược lên Controller.
14. Controller bọc `new EmployeeResource($employee)` — bên trong tự `loadMissing(['department','position','manager','province','commune'])` rồi build JSON, người tạo không phải cấp dưới của ai nên không field nào bị ẩn.
15. `response()->json(..., 201)` — response rời Backend, quay lại trình duyệt.

**Phía Frontend — sau khi nhận response:**

16. `bootstrap.js`'s interceptor tắt loading bar. `store.create()` (Pinia) nhận response, trả về.
17. `submit()` trong `EmployeeForm.vue` chạy tiếp: `toast.success(...)`, `emit('saved')`, đóng modal.
18. `Employees.vue` nghe được `@saved="fetchData"` → gọi lại `GET /employees` (một vòng Frontend→Backend→Frontend khác, ngắn hơn — chỉ qua Route → Middleware → Controller → Service → Repository → Resource, không có Request vì `index()` không nhận input cần validate) → bảng hiện nhân viên mới.

Nhìn vào đây sẽ thấy: **càng dừng sớm càng tốt** — validate phía client (bước 3) đỡ tốn 1 lượt round-trip mạng, middleware (bước 7-8) đỡ tốn công chạy Service/Repository cho request không hợp lệ, FormRequest (bước 9) đỡ Controller phải tự viết lại kiểm tra input.

## 1. Xác thực (JWT Authentication)

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | [`resources/js/views/Login.vue`](resources/js/views/Login.vue), [`resources/js/views/ForgotPassword.vue`](resources/js/views/ForgotPassword.vue) (nhập email → luôn hiện thông báo thành công dù email có tồn tại hay không, khớp với backend), [`resources/js/views/ResetPassword.vue`](resources/js/views/ResetPassword.vue) (đọc `token`/`email` từ query string trên URL — link trong email trỏ về `/reset-password?token=...&email=...`), [`resources/js/stores/authStore.js`](resources/js/stores/authStore.js), [`resources/js/services/authService.js`](resources/js/services/authService.js) (`login`, `refresh`, `me`, `logout`, `forgotPassword`, `resetPassword`) |
| Xử lý Backend | [`app/Http/Controllers/Api/V1/AuthController.php`](app/Http/Controllers/Api/V1/AuthController.php) (login/refresh/logout/me), [`app/Http/Controllers/Api/V1/PasswordResetController.php`](app/Http/Controllers/Api/V1/PasswordResetController.php) (quên/đặt lại mật khẩu), [`app/Services/AuthService.php`](app/Services/AuthService.php), [`app/Services/PasswordResetService.php`](app/Services/PasswordResetService.php), [`app/Services/Jwt/JwtService.php`](app/Services/Jwt/JwtService.php), [`app/Auth/JwtGuard.php`](app/Auth/JwtGuard.php), [`app/Repositories/UserRepository.php`](app/Repositories/UserRepository.php), [`app/Repositories/RefreshTokenRepository.php`](app/Repositories/RefreshTokenRepository.php) (thêm `revokeAllForUser()` — đăng xuất khỏi mọi thiết bị khi đặt lại mật khẩu), [`app/Repositories/PasswordResetRepository.php`](app/Repositories/PasswordResetRepository.php) |
| Validation | [`app/Http/Requests/Auth/LoginRequest.php`](app/Http/Requests/Auth/LoginRequest.php), [`app/Http/Requests/Auth/RefreshTokenRequest.php`](app/Http/Requests/Auth/RefreshTokenRequest.php), [`app/Http/Requests/Auth/ForgotPasswordRequest.php`](app/Http/Requests/Auth/ForgotPasswordRequest.php) — **cố tình không** dùng rule `exists:users,email` (tránh User Enumeration, xem Ghi chú), [`app/Http/Requests/Auth/ResetPasswordRequest.php`](app/Http/Requests/Auth/ResetPasswordRequest.php) (`password` dùng rule `confirmed`, cần thêm field `password_confirmation`) |
| Model | [`app/Models/PasswordResetToken.php`](app/Models/PasswordResetToken.php), [`app/Models/PasswordHistory.php`](app/Models/PasswordHistory.php) — cả 2 `$timestamps = false` vì bảng chỉ có 1 cột thời gian tự set qua `useCurrent()` ở DB, không có cặp `created_at`/`updated_at` chuẩn Eloquent |
| Email | [`app/Mail/PasswordResetMail.php`](app/Mail/PasswordResetMail.php) + view [`resources/views/emails/password-reset.blade.php`](resources/views/emails/password-reset.blade.php). Gửi **đồng bộ** (`Mail::send()`, không `queue()`) vì `docker-compose.yml` chưa có service nào chạy `queue:work`/Horizon — nếu queue mail lúc chưa có worker, mail nằm mãi trong Redis, không bao giờ gửi thật. Môi trường dev dùng `MAIL_MAILER=log` (mặc định của Laravel khi không set) — xem nội dung mail đã "gửi" tại `storage/logs/laravel.log` |
| Database & API | Bảng `users`, `refresh_tokens`, `password_reset_tokens` (có sẵn cả `token_hash` lẫn `otp_hash` từ migration ban đầu — hiện chỉ dùng nhánh Link/`token_hash`, chưa làm nhánh OTP), `password_histories`. Endpoint `POST /api/v1/auth/login`, `POST /api/v1/auth/refresh`, `GET /api/v1/auth/me`, `POST /api/v1/auth/logout`, `POST /api/v1/auth/forgot-password`, `POST /api/v1/auth/reset-password` — khai báo tại [`routes/api/v1/auth.php`](routes/api/v1/auth.php). 2 route quên/đặt lại mật khẩu **không** cần `auth:api` (đúng bản chất — người quên mật khẩu chưa đăng nhập được) |
| Test | [`tests/Feature/Auth/AuthTest.php`](tests/Feature/Auth/AuthTest.php), [`tests/Feature/Auth/LoginValidationTest.php`](tests/Feature/Auth/LoginValidationTest.php), [`tests/Feature/Auth/PasswordResetTest.php`](tests/Feature/Auth/PasswordResetTest.php) — dùng `Mail::fake()` + bắt lại `PasswordResetMail->resetUrl` để lấy token test thay vì đọc log; verify **không lộ email có tồn tại hay không** (cùng status/message cho cả 2 trường hợp), token dùng 1 lần rồi vô hiệu, request mới làm vô hiệu token cũ, đặt lại mật khẩu xong **thu hồi mọi refresh token đang có** của user đó |
| Ghi chú | **Quên mật khẩu**: token gốc (plain, 64 ký tự ngẫu nhiên) chỉ gửi qua email, DB chỉ lưu `hash('sha256', token)` — giống hệt cách `RefreshTokenRepository` đang hash refresh token, không tự nghĩ ra cách mới. `PasswordResetService::requestReset()` **luôn trả về thành công** kể cả khi email không tồn tại trong hệ thống (OWASP User Enumeration — tài liệu yêu cầu dự án có nhắc "Tuân thủ chuẩn OWASP Top-10" ở mục Bảo mật). Đặt lại mật khẩu thành công thì lưu 1 dòng vào `password_histories` (hash mật khẩu CŨ, trước khi đổi) và thu hồi (`revokeAllForUser()`) toàn bộ refresh token hiện có — access token JWT cũ (nếu còn hạn, tối đa 1h) vẫn dùng được tới khi tự hết hạn vì JWT không lưu trạng thái ở server, nhưng không refresh tiếp được nữa. Verify qua HTTP thật (không chỉ test giả lập): gọi `forgot-password` cho tài khoản admin thật → đọc link từ `storage/logs/laravel.log` → `reset-password` đổi mật khẩu tạm → xác nhận mật khẩu cũ hết dùng được, mật khẩu mới dùng được → **đổi lại đúng mật khẩu gốc** qua 1 vòng quên/đặt lại nữa (không để lại tác dụng phụ cho các bước test khác trong dự án vẫn dùng `admin@qlns.local`/`Admin@123`) |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên; đổi thời hạn token đăng nhập sửa [`config/config_jwt.php`](config/config_jwt.php) + `.env` (`JWT_ACCESS_TTL`, `JWT_REFRESH_TTL_DAYS`); đổi thời hạn link đặt lại mật khẩu (hiện 60 phút) sửa trực tiếp trong `PasswordResetService::requestReset()` |

## 2. Phân quyền (RBAC)

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | *(chưa có UI quản lý Role/Permission riêng — dự kiến làm sau)* |
| Xử lý Backend | [`app/Http/Middleware/EnsurePermission.php`](app/Http/Middleware/EnsurePermission.php) (alias `permission`, đăng ký tại [`bootstrap/app.php`](bootstrap/app.php)) |
| Model liên quan | [`app/Models/Role.php`](app/Models/Role.php), [`app/Models/Permission.php`](app/Models/Permission.php) (dùng [`app/Models/Concerns/Auditable.php`](app/Models/Concerns/Auditable.php)) |
| Database & API | Bảng `roles`, `permissions`, `role_permissions`, `user_roles`. Danh sách mã quyền: [`database/seeders/PermissionSeeder.php`](database/seeders/PermissionSeeder.php); phân quyền theo role: [`database/seeders/RolePermissionSeeder.php`](database/seeders/RolePermissionSeeder.php) |
| Test | [`tests/Feature/PermissionMiddlewareTest.php`](tests/Feature/PermissionMiddlewareTest.php) |
| Danh sách tệp cần sửa khi bảo trì | Thêm route cần giới hạn quyền: gắn `->middleware(['auth:api', 'permission:<mã-quyền>'])` trong file route module tương ứng ([`routes/api/v1/`](routes/api/v1)) |

## 3. Audit Log (Ghi vết thao tác)

| Hạng mục | Chi tiết |
|---|---|
| Xử lý Backend | [`app/Models/AuditLog.php`](app/Models/AuditLog.php), [`app/Observers/AuditObserver.php`](app/Observers/AuditObserver.php), [`app/Models/Concerns/Auditable.php`](app/Models/Concerns/Auditable.php) |
| Database | Bảng `audit_logs` |
| Danh sách tệp cần sửa khi bảo trì | Muốn Model nào được tự động ghi log created/updated/deleted: thêm `use Auditable;` vào Model đó (xem ví dụ [`app/Models/Role.php`](app/Models/Role.php)) |

## 4. Layout chính & Điều hướng

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | [`resources/js/App.vue`](resources/js/App.vue) (chọn layout theo `route.meta.layout`), [`resources/js/components/layout/AppLayout.vue`](resources/js/components/layout/AppLayout.vue) (giữ state `rail` — thu/mở sidebar — dùng chung giữa Header và Sidebar vì 2 component này ngang hàng nhau, không phải cha-con), [`AppHeader.vue`](resources/js/components/layout/AppHeader.vue) (nút hamburger emit `toggle-sidebar`), [`AppSidebar.vue`](resources/js/components/layout/AppSidebar.vue) (nhận `rail` qua prop, không tự giữ state nữa; **đã bỏ `expand-on-hover`** — không dùng chung được với nút bấm thủ công vì Vuetify tự ép `rail=true` mỗi khi chuột rời khỏi sidebar, đè lên trạng thái người dùng vừa bấm mở), [`AppBreadcrumbs.vue`](resources/js/components/layout/AppBreadcrumbs.vue) |
| Theme (Dark/Light) | [`resources/js/plugins/vuetify.js`](resources/js/plugins/vuetify.js) (theme `qlnsDark` / `qlnsLight`), đổi qua `useTheme()` trong [`AppHeader.vue`](resources/js/components/layout/AppHeader.vue) |
| Router | [`resources/js/router/index.js`](resources/js/router/index.js) — route nào không dùng layout chính khai `meta: { layout: 'blank' }` (ví dụ `/login`). Mỗi route còn khai **`meta.title` tiếng Việt** để [`AppBreadcrumbs.vue`](resources/js/components/layout/AppBreadcrumbs.vue) hiển thị — trước đó breadcrumb đọc thẳng `route.name` nên lòi mã tiếng Anh ra giao diện ("Trang chủ / departments"). Trang con khai thêm `meta.parent` là tên route cha để chèn cấp giữa (`/employees/:id` → "Trang chủ / Nhân viên / Chi tiết nhân viên"). Thêm route mới mà quên `meta.title` thì breadcrumb rơi về tên route — không vỡ, nhưng là dấu hiệu thiếu |
| Danh sách tệp cần sửa khi bảo trì | Thêm mục menu mới: [`AppSidebar.vue`](resources/js/components/layout/AppSidebar.vue). Thêm route trang mới: [`router/index.js`](resources/js/router/index.js) + tạo view trong [`resources/js/views/`](resources/js/views) |

## 5. Dashboard (tạm thời)

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | [`resources/js/views/Dashboard.vue`](resources/js/views/Dashboard.vue), [`resources/js/components/dashboard/StatCards.vue`](resources/js/components/dashboard/StatCards.vue) |
| Ghi chú | Đang dùng **dữ liệu minh họa** (hardcode), chưa nối API thật — Dashboard thật (biểu đồ, số liệu động) thuộc Phase Báo cáo & Thống kê (Ngày 51-60 theo kế hoạch) |

## 6. Phòng ban (Departments)

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | [`resources/js/views/Department/Departments.vue`](resources/js/views/Department/Departments.vue) (danh sách dạng cây thụt lề + tìm kiếm + lọc trạng thái + cột "Trưởng phòng"), [`resources/js/views/Department/DepartmentForm.vue`](resources/js/views/Department/DepartmentForm.vue) (modal Thêm/Sửa, dựng trên `FormDialog`/`FormSection` dùng chung — xem mục 10; ô Phòng ban cha và Trưởng phòng dùng `SearchSelect` — xem mục 10; danh sách nhân viên nạp qua `useEmployeeStore().fetchList({ per_page: 1000 })` lúc mount), [`resources/js/stores/useDepartmentStore.js`](resources/js/stores/useDepartmentStore.js), [`resources/js/services/departmentService.js`](resources/js/services/departmentService.js) |
| Xử lý Backend | [`app/Http/Controllers/Api/V1/DepartmentController.php`](app/Http/Controllers/Api/V1/DepartmentController.php) (`index()` trả `DepartmentResource::collection()` trực tiếp — Laravel tự bọc `{data,links,meta}`; `store()`/`update()` dùng `response()->json(new DepartmentResource(...))` — **cố tình không dùng `->response()`** để giữ JSON phẳng, không bọc `data`, tránh phá test/behaviour cũ đang đọc field ở top-level; `tree()` dùng `response()->json(DepartmentResource::collection(...))` — cùng lý do, phải giữ mảng phẳng vì frontend đọc `response.data` làm cây trực tiếp), [`app/Services/DepartmentService.php`](app/Services/DepartmentService.php) (gồm `generateCode()` tự sinh mã, xem Ghi chú; `syncHeadPosition()` — xem Ghi chú "Trưởng phòng ↔ Chức vụ" bên dưới), [`app/Repositories/DepartmentRepository.php`](app/Repositories/DepartmentRepository.php) (có `tree()`/`buildTree()` dựng cây và `wouldCreateCycle()` chống vòng lặp cha-con; `paginate()`/`find()`/`tree()` đều `with('manager')` để tránh N+1) |
| Model | [`app/Models/Department.php`](app/Models/Department.php) — tự tham chiếu `parent()`/`children()` (cây phân cấp), `manager()` trỏ tới `Employee` (khóa `manager_id`), dùng `Auditable` |
| Validation | [`app/Http/Requests/Department/StoreDepartmentRequest.php`](app/Http/Requests/Department/StoreDepartmentRequest.php), [`app/Http/Requests/Department/UpdateDepartmentRequest.php`](app/Http/Requests/Department/UpdateDepartmentRequest.php) — **không** có rule cho `code` (xem Ghi chú); `manager_id` là `nullable, exists:employees,id` |
| Output / Phân quyền hiển thị | [`app/Http/Resources/DepartmentResource.php`](app/Http/Resources/DepartmentResource.php) — field `manager` **bọc qua `EmployeeResource`** (không trả `Employee` Model trần), để tự động áp logic ẩn field nhạy cảm (CCCD, SĐT...) với người xem là cấp dưới của Trưởng phòng, giống hệt cơ chế field `manager` lồng trong `EmployeeResource` (mục 8) — nếu bỏ bước bọc này, bất kỳ ai có quyền `department.view` sẽ thấy nguyên vẹn thông tin cá nhân của Trưởng phòng bất kỳ phòng ban nào, kể cả khi không phải cấp dưới. Field `children` cũng tự bọc đệ quy qua chính `DepartmentResource` |
| Database & API | Bảng `departments` (migration từ Ngày 04, khóa ngoại `manager_id` → `employees` thêm ở migration riêng). Endpoint `GET/POST /api/v1/departments`, `GET /api/v1/departments/tree`, `PUT/DELETE /api/v1/departments/{department}` — khai báo tại [`routes/api/v1/departments.php`](routes/api/v1/departments.php), yêu cầu quyền `department.view`/`department.manage` |
| Test | [`tests/Feature/Department/DepartmentTest.php`](tests/Feature/Department/DepartmentTest.php) — quyền hạn (401/403), tạo (mã tự sinh đúng định dạng, bỏ qua `code` client gửi, không đọc nhầm mã cũ dạng `PB-01`, không tái dùng mã đã xóa mềm), sửa (bỏ qua `code`, chống vòng lặp cha-con — cả tự làm cha chính mình lẫn chuyển cha xuống dưới con), xóa (chặn khi còn con, xóa mềm khi không còn con), cây phân cấp lồng nhau, **gán Trưởng phòng** (`manager_id` hợp lệ → trả về đúng `manager` lồng bên trong; `manager_id` không tồn tại → 422), **ẩn field nhạy cảm của Trưởng phòng với người xem là cấp dưới** (qua endpoint `tree()`, cùng kiểu test với `EmployeeTest.php`) |
| Ghi chú | CRUD + cây phân cấp (dựng cây, chống vòng lặp) xong Ngày 16-17. UI đầy đủ (danh sách, modal Thêm/Sửa, xác nhận Xóa, tìm kiếm/lọc) xong Ngày 18-19. **Mã `code` do `DepartmentService::generateCode()` tự sinh** (`PB001`, `PB002`...), request Store/Update không nhận `code` từ client — sửa định dạng mã thì chỉ cần sửa hàm này. **Xóa phòng ban còn con nay bị chặn ở backend** (`DepartmentService::delete()` — 422 nếu còn con), không còn phụ thuộc riêng vào chặn phía UI. PHPUnit test xong Ngày 20. **Trưởng phòng (`manager_id`) hoàn tất sau đó**: cột đã có sẵn từ đầu (migration + fillable + relation Model) nhưng bị bỏ sót ở Request/Repository/Controller/UI suốt nhiều ngày — vá đủ cả validation, eager-load, Resource (kèm redaction), và UI chọn/hiển thị trong 1 lượt |
| Trưởng phòng ↔ Chức vụ (đồng bộ tự động) | Gán/đổi `manager_id` (qua `create()` lẫn `update()`) tự đồng bộ sang Chức vụ của người liên quan qua `DepartmentService::syncHeadPosition()`: người **mới** được gán nhận Chức vụ "Trưởng phòng" (`PositionService::ensureHeadPosition()` — find-or-create theo `department_id` + `type='head'`), người **cũ** (nếu có, khác người mới) bị hạ về Chức vụ "Nhân viên" mặc định (`PositionService::ensureDefaultPosition()` — `type='default'`). Không so khớp theo tên chuỗi "Trưởng phòng" (dễ vỡ khi đổi tên/dấu) — dùng cột `positions.type` (thêm ở [migration `add_type_to_positions_table`](database/migrations/2026_09_08_000001_add_type_to_positions_table.php), cùng kiểu cột chuỗi thường như `employment_status`, không phải DB enum). Chiều ngược lại — nhân viên đang là Trưởng phòng bị **luân chuyển** sang phòng ban khác — xử lý ở `EmployeeTransferService` (xem mục 8, không phải ở đây), vì đó không phải hành động gán/đổi Trưởng phòng trực tiếp. `update()` chỉ đồng bộ khi request thật sự có gửi `manager_id` (`array_key_exists`), và bỏ qua nếu giá trị cũ/mới giống nhau — tránh gọi `firstOrCreate` thừa mỗi lần sửa tên/mô tả phòng ban không đụng tới Trưởng phòng. Test: `test_setting_department_manager_assigns_head_position`, `test_replacing_department_manager_demotes_old_one_to_default_position` trong `DepartmentTest.php` |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 7. Chức vụ (Position)

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | [`resources/js/views/Position/Positions.vue`](resources/js/views/Position/Positions.vue) (danh sách + tìm kiếm + lọc theo phòng ban — copy khuôn `Departments.vue` nhưng không cây phân cấp; cột "Loại" hiện chip "Trưởng phòng — hệ thống"/"Mặc định — hệ thống" cho 2 bản ghi `type` khác `null`), [`resources/js/views/Position/PositionForm.vue`](resources/js/views/Position/PositionForm.vue) (modal Thêm/Sửa, dựng trên `FormDialog`/`FormSection`/`SearchSelect`/`InputMoney` dùng chung — xem mục 10). **Không dùng Pinia store** — chỉ 1 trang dùng (đúng nguyên tắc ở đầu file "Store chỉ cần nếu nhiều trang cùng cần đọc/ghi 1 state chung"), `Positions.vue`/`PositionForm.vue` tự giữ `loading`/`errors` cục bộ, gọi thẳng [`positionService.js`](resources/js/services/positionService.js) (nay đủ `list/create/update/remove`). Route `/positions` (tên `positions`) khai tại [`router/index.js`](resources/js/router/index.js), mục menu "Chức vụ" trong [`AppSidebar.vue`](resources/js/components/layout/AppSidebar.vue) (nhóm NHÂN SỰ, dưới "Phòng ban") |
| Xử lý Backend | [`app/Http/Controllers/Api/V1/PositionController.php`](app/Http/Controllers/Api/V1/PositionController.php) (`index`/`store`/`update`/`destroy`, không có `show()` riêng), [`app/Services/PositionService.php`](app/Services/PositionService.php) (`generateCode()` tự sinh mã `CV001`... — cùng cơ chế `preg_match` an toàn với `DepartmentService`/`EmployeeService`; `ensureHeadPosition()`/`ensureDefaultPosition()` — xem Ghi chú), [`app/Repositories/PositionRepository.php`](app/Repositories/PositionRepository.php) (`paginate()` lọc theo `department_id`, `with('department')`) |
| Model | [`app/Models/Position.php`](app/Models/Position.php) — `belongsTo(Department::class)`, dùng `Auditable` + `SoftDeletes` (đã có sẵn từ Ngày 21), thêm cột `type` (xem Ghi chú) |
| Validation | [`app/Http/Requests/Position/StorePositionRequest.php`](app/Http/Requests/Position/StorePositionRequest.php), [`app/Http/Requests/Position/UpdatePositionRequest.php`](app/Http/Requests/Position/UpdatePositionRequest.php) — `department_id` bắt buộc + phải tồn tại, `name` bắt buộc, `level`/`position_allowance` không bắt buộc; **không** có rule cho `code` (tự sinh) lẫn `type` (chỉ hệ thống ghi qua `PositionService::ensureHeadPosition()`/`ensureDefaultPosition()`, client không sửa được qua CRUD thường vì không nằm trong `validated()`) |
| Database & API | Bảng `positions` (migration Ngày 04, schema có sẵn từ trước; cột `type` thêm ở [migration `add_type_to_positions_table`](database/migrations/2026_09_08_000001_add_type_to_positions_table.php)). Endpoint `GET/POST /api/v1/positions`, `PUT/DELETE /api/v1/positions/{position}` — khai báo tại [`routes/api/v1/positions.php`](routes/api/v1/positions.php). **Không có mã quyền riêng** — cố tình dùng chung `department.view`/`department.manage` (Chức vụ luôn thuộc về 1 Phòng ban, không tách quyền quản lý riêng, giống cách tên quyền trong `PermissionSeeder.php` đã ghi rõ "Quản lý phòng ban / chức vụ") |
| Test | [`tests/Feature/Position/PositionTest.php`](tests/Feature/Position/PositionTest.php) — quyền hạn (401/403), tạo (mã tự sinh đúng định dạng `CV\d{3}`, bỏ qua `code` client gửi, bắt buộc `department_id` + `name`), sửa (bỏ qua `code`), xóa mềm, lọc theo `department_id`. Việc `type='head'`/`'default'` tự sinh đúng thì test ở `DepartmentTest.php`/`EmployeeTransferTest.php` (mục 6, mục 8) — nơi thật sự kích hoạt logic đó, không lặp lại ở đây |
| Ghi chú | Model `Position` có từ Ngày 21 nhưng **không có CRUD** (Controller/Service/Repository/Route) suốt nhiều ngày — phát hiện khi rà soát lại cùng lúc với việc thiếu Trưởng phòng ở Department (mục 6), làm cùng 1 lượt vì Ngày 28 (form Nhân viên) cần dropdown Chức vụ hoạt động thật, không thể hoãn thêm. **UI riêng** (`Positions.vue`/`PositionForm.vue`) làm sau đó, cùng lúc với cột `type`. **Cột `type`** (`null`/`'head'`/`'default'`, chuỗi thường như `employment_status` — không phải DB enum) đánh dấu 2 bản ghi Chức vụ hệ thống tự quản lý theo từng phòng ban: `ensureHeadPosition()`/`ensureDefaultPosition()` trong `PositionService` dùng `firstOrCreate(['department_id'=>..., 'type'=>'head'|'default'], [...])` — an toàn gọi lại nhiều lần, không tạo trùng. **Không so khớp theo tên chuỗi "Trưởng phòng"/"Nhân viên"** (dễ vỡ khi ai đó đổi tên hoặc gõ dấu khác) — đây là lý do chính cần thêm cột thay vì tái dùng `name`. `Positions.vue` khóa nút Sửa/Xóa (`disabled`) cho 2 bản ghi có `type` khác `null` — tránh người dùng chỉnh tay phá vỡ đồng bộ mà `DepartmentService`/`EmployeeTransferService` đang tự quản lý (xem mục 6, mục 8) |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 8. Nhân viên (Employee) — CRUD đầy đủ, có API + đang dựng Frontend

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | [`resources/js/views/Employee/Employees.vue`](resources/js/views/Employee/Employees.vue) (Ngày 26 — bảng danh sách, tìm kiếm, lọc theo phòng ban/trạng thái, phân trang **server-side** thật kể cả "Số dòng/trang" (`per_page` gửi lên API, đồng bộ qua `v-model:items-per-page` trên `DataTable`); cột "Thao tác" dùng `:actions` prop của `DataTable.vue` — "Xem chi tiết" điều hướng sang `EmployeeDetail.vue`, "Sửa" mở `EmployeeForm.vue`, gạt theo quyền `employee.create`/`employee.update`. **Chưa có Xóa** — cố tình để sau, không thuộc phạm vi Ngày 28), [`resources/js/views/Employee/EmployeeForm.vue`](resources/js/views/Employee/EmployeeForm.vue) (Ngày 28 — modal Thêm/Sửa dựng trên `FormDialog`/`FormSection` dùng chung, cùng pattern `DepartmentForm.vue`; lỗi 422 hiển thị qua `store.errors.<field>` — tên field khớp thẳng với key JSON Laravel trả về nên không cần ánh xạ tay. **2 điểm kỹ thuật đáng chú ý**: (1) dropdown "Chức vụ" tự lọc theo `department_id` đang chọn (tải hết vị trí 1 lần, lọc phía client — không gọi lại API mỗi lần đổi phòng ban), đổi phòng ban thì tự xóa chức vụ đang chọn **nhưng chỉ khi người dùng thật sự bấm đổi** (qua `@update:model-value` riêng, không phải `watch()` chung) — nếu dùng `watch()` sẽ vô tình xóa mất chức vụ vừa nạp lúc `fillForm()` tự gán `department_id` khi mở modal Sửa; (2) dropdown "Quản lý trực tiếp" **cố tình KHÔNG dùng `useEmployeeStore()`** để tải danh sách — vì đó là cùng 1 store đang giữ dữ liệu bảng chính của `Employees.vue` (cùng trang, mở modal cùng lúc), gọi `fetchList({per_page:1000})` trên store đó sẽ ghi đè mất dữ liệu bảng đang hiển thị; thay vào đó gọi thẳng `employeeService`/`positionService` vào ref cục bộ trong chính `EmployeeForm.vue`; (3) 2 dropdown "Tỉnh/Thành phố" + "Xã/Phường" (xem mục 9 — Địa chỉ hành chính) dùng cùng pattern cascading nhưng **tải Xã theo từng Tỉnh qua API** (`addressService.communes(provinceCode)`) thay vì tải hết 1 lần như Chức vụ — vì cả nước có hơn 3300 Xã, tải hết vừa chậm vừa phí, khác Chức vụ chỉ vài chục bản ghi. Không có `user_id` (liên kết tài khoản đăng nhập) và không có upload avatar trong form này — cố tình để riêng, chưa có UI/nhu cầu rõ ràng), [`resources/js/services/positionService.js`](resources/js/services/positionService.js) (nay đủ `list/create/update/remove` — UI CRUD riêng cho Position xem mục 7), [`resources/js/views/Employee/EmployeeDetail.vue`](resources/js/views/Employee/EmployeeDetail.vue) (Ngày 27 — trang chi tiết dạng `v-tabs`/`v-window` 3 tab: **Sơ yếu lý lịch** (gọi `employeeService.get(id)` lúc mount), **Hợp đồng** (gọi `employeeService.contracts(id)` — **lazy-load, chỉ gọi khi thật sự mở tab này** qua `watch(tab, ...)`, tránh gọi thừa API nếu người dùng chỉ xem tab 1; nút tải file **dùng `axios.get(download_url, {responseType:'blob'})` rồi tự tạo link tải qua `URL.createObjectURL`, không dùng thẻ `<a href>` trần** — vì route tải file yêu cầu `auth:api`, thẻ `<a>` không tự gắn được `Authorization` header nên sẽ bị 401), **Tài liệu** (cùng cơ chế lazy-load + tải file như tab Hợp đồng, thêm **nút "Tải lên"** mở `v-dialog` riêng (chọn Loại tài liệu qua `v-select` với 4 lựa chọn cố định `cccd`/`resume`/`certificate`/`other` — chỉ là gợi ý phía UI, backend nhận mọi chuỗi ≤ 50 ký tự; `v-file-input` cho tệp, gửi bằng `FormData` vì có file) và nút **Xóa** (mở `v-dialog` xác nhận riêng, không dùng `DataTable`'s actions vì bảng ở đây là `v-table` thường không phải `DataTable`) — khác Hợp đồng vì tài liệu cá nhân xóa/tải lại được, không phải văn bản pháp lý bất biến), **Luân chuyển** (bảng lịch sử + nút "Tạo luân chuyển" mở dialog riêng với 3 `SearchSelect` (Phòng ban mới/Chức vụ mới/Quản lý mới — Chức vụ lọc theo Phòng ban đang chọn, cùng pattern `onDepartmentChange` không dùng `watch()` chung như `EmployeeForm.vue`) + `InputDate` (Ngày hiệu lực) + `v-file-input` (Quyết định điều động, tùy chọn); **tự nạp `departmentService.tree()`/`positionService.list()`/`employeeService.list()` riêng, độc lập với `EmployeeForm.vue`** — 2 nơi không dùng chung state vì `EmployeeDetail.vue` và `EmployeeForm.vue` không cùng mount lúc nào; dropdown "Phòng ban mới" **tự loại phòng ban hiện tại của nhân viên** (`transferDepartmentOptions` lọc theo `employee.value?.department?.id`) — khớp với chặn 422 ở backend (mục Luân chuyển nhân sự bên dưới) nhưng đỡ 1 vòng round-trip cho người dùng; sau khi tạo xong gọi lại `loadEmployee()` để tab Sơ yếu lý lịch không hiện phòng ban/chức vụ cũ nếu người dùng quay lại xem), **Lương/Phép** (placeholder — chưa có backend, thuộc Phase 3 Ngày 36+ và Phase 4 Ngày 46+)), route `/employees/:id` (tên `employee-detail`, `props: true` truyền `id` từ URL) khai tại [`resources/js/router/index.js`](resources/js/router/index.js), [`resources/js/stores/useEmployeeStore.js`](resources/js/stores/useEmployeeStore.js) (đọc `response.data.{data,meta}` — **khác cấu trúc phẳng** của `useDepartmentStore.js`, vì `EmployeeResource::collection()` bọc theo chuẩn Laravel Resource, không phải paginator trần), [`resources/js/services/employeeService.js`](resources/js/services/employeeService.js) (`list()`, `get(id)`, `contracts(id)`, `create()`, `update(id)`, `documents(id)`, `uploadDocument(id, formData)`, `deleteDocument(id, documentId)`, `transfers(id)`, `createTransfer(id, formData)`). Dropdown lọc phòng ban dùng lại `useDepartmentStore().tree` + tự làm phẳng cây (`flattenDepartments()`, viết riêng trong `Employees.vue`, cùng logic với `flattenTree()` của `Departments.vue` nhưng không cần `depth`). Trạng thái (`employment_status`) hiển thị qua [`StatusChip.vue`](resources/js/components/common/StatusChip.vue) dùng chung — xem mục 10. **Thẻ thống kê đầu trang** (`Employees.vue`): tái dùng nguyên [`components/dashboard/StatCards.vue`](resources/js/components/dashboard/StatCards.vue) (component đã có sẵn cho Dashboard minh họa, nhận prop `stats` chung) — 4 thẻ Tổng nhân viên/Đang làm việc/Thử việc/Đã nghỉ việc lấy số thật từ `employeeService.stats()` (gọi lại sau mỗi lần Thêm/Sửa nhân viên qua `onEmployeeSaved()`, không chỉ lúc mount). **Cố tình không có thẻ "Đang nghỉ phép"**: đó là trạng thái tạm thời theo ngày (có đơn phép được duyệt trùng hôm nay), khác hẳn `employment_status` là trạng thái lâu dài — thuộc module Nghỉ phép (Ngày 36-40) chưa xây, thêm nhầm vào đây sẽ phải sửa lại cách tính sau này. **Avatar tròn màu + chữ cái đầu** trong cột "Họ tên": `initials()` lấy chữ đầu tên + họ, `avatarColor()` băm chuỗi tên ra 1 màu cố định trong bảng màu Vuetify có sẵn (`primary/success/info/warning/purple/teal/indigo/deep-orange`) — cùng 1 người luôn ra cùng 1 màu qua các lần render, không đổi ngẫu nhiên |
| Xử lý Backend | [`app/Http/Controllers/Api/V1/EmployeeController.php`](app/Http/Controllers/Api/V1/EmployeeController.php) (`index`/`show`/`store`/`update`/`destroy`/`uploadAvatar`/`stats`, `show` dùng Route Model Binding — **mọi endpoint trả 1 bản ghi đều dùng `(new EmployeeResource(...))->response()`** để JSON luôn bọc trong `{"data": {...}}`, đồng nhất với `index()`), [`app/Services/EmployeeService.php`](app/Services/EmployeeService.php) (`generateCode()` tự sinh mã `NV001`..., chặn vòng lặp quản lý qua `wouldCreateCycle()` khi đổi `manager_id`, `updateAvatar()` lưu file + tự xóa avatar cũ, `stats()` — đếm theo `employment_status` cho 4 thẻ đầu trang `Employees.vue`), [`app/Repositories/EmployeeRepository.php`](app/Repositories/EmployeeRepository.php) (`wouldCreateCycle()`, `isSuperiorOf()`, `ancestorIds()` — duyệt ngược chuỗi `manager`; `ancestorIds()` tách riêng để nơi gọi tự cache, tránh tính lại nhiều lần; `countByStatus()` — 1 query `groupBy('employment_status')` duy nhất thay vì 4 `count()` rời rạc). **Ngày 26**: `index()` nhận query `?search=&department_id=&employment_status=&per_page=&page=` (thêm vì frontend cần lọc/tìm kiếm/phân trang thật ở server — khác Department vốn tải hết dữ liệu rồi lọc phía client) — `search` khớp `full_name`/`code`/`company_email` kiểu `LIKE '%...%'`. **Lỗi đã gặp và vá**: quên nối `per_page` từ query xuống `EmployeeRepository::paginate()` khiến "Số dòng/trang" ở giao diện vô tác dụng (backend luôn dùng mặc định 15) — đã sửa `EmployeeController`→`EmployeeService::list()`→`EmployeeRepository::paginate()` nhận và dùng đúng giá trị |
| Hợp đồng lao động | [`app/Http/Controllers/Api/V1/EmployeeContractController.php`](app/Http/Controllers/Api/V1/EmployeeContractController.php) (`index`/`store`/`download`, route lồng `/employees/{employee}/contracts`), [`app/Services/EmployeeContractService.php`](app/Services/EmployeeContractService.php), [`app/Repositories/EmployeeContractRepository.php`](app/Repositories/EmployeeContractRepository.php), [`app/Http/Requests/EmployeeContract/StoreEmployeeContractRequest.php`](app/Http/Requests/EmployeeContract/StoreEmployeeContractRequest.php), [`app/Http/Resources/EmployeeContractResource.php`](app/Http/Resources/EmployeeContractResource.php) — **chỉ Tạo + Xem danh sách + Tải file**, không có Sửa/Xóa (hợp đồng là văn bản pháp lý đã ký, gia hạn = tạo hợp đồng mới, không sửa đè). `download()` **bắt buộc kiểm tra `$contract->employee_id === $employee->id`** trước khi trả file — thiếu dòng này sẽ là lỗi IDOR (tải nhầm hợp đồng người khác qua route lồng 2 tham số độc lập) |
| Tài liệu cá nhân | [`app/Http/Controllers/Api/V1/EmployeeDocumentController.php`](app/Http/Controllers/Api/V1/EmployeeDocumentController.php) (`index`/`store`/`destroy`/`download`, route lồng `/employees/{employee}/documents`), [`app/Services/EmployeeDocumentService.php`](app/Services/EmployeeDocumentService.php), [`app/Repositories/EmployeeDocumentRepository.php`](app/Repositories/EmployeeDocumentRepository.php), [`app/Http/Requests/EmployeeDocument/StoreEmployeeDocumentRequest.php`](app/Http/Requests/EmployeeDocument/StoreEmployeeDocumentRequest.php) (nhận PDF/JPG/PNG/**DOCX/XLSX**, khác hợp đồng chỉ nhận PDF — tài liệu cá nhân đa dạng hơn, vd CCCD chụp ảnh, sơ yếu lý lịch soạn bằng Word, bảng kê bằng Excel. **Cố ý KHÔNG nhận `.doc`/`.xls`** (Office 97-2003): `docx-preview` và `xlsx` (SheetJS) ở `FilePreviewDialog.vue` đọc gói OOXML (`.docx`/`.xlsx` thực chất là file ZIP), còn hai đuôi cũ là định dạng nhị phân — cho tải lên sẽ sinh ra tài liệu chỉ tải xuống được chứ không xem trước được ngay trên giao diện. Rule `mimes:...` dựa vào MIME do `finfo` đoán chứ không phải đuôi file người dùng đặt; đã kiểm chứng bằng file thật trong container `app`: `.docx` → `application/vnd.openxmlformats-officedocument.wordprocessingml.document`, `.xlsx` → `...spreadsheetml.sheet`, **không** rơi về `application/zip` như lo ngại thường gặp với OOXML), [`app/Http/Resources/EmployeeDocumentResource.php`](app/Http/Resources/EmployeeDocumentResource.php) — **khác Hợp đồng**: có `destroy()` (xóa mềm) vì tài liệu cá nhân không phải văn bản pháp lý bất biến như hợp đồng đã ký, upload nhầm thì xóa/tải lại được. `download()` **cũng bắt buộc kiểm tra `$document->employee_id === $employee->id`** — cùng dạng IDOR như hợp đồng, phải tự nhớ vá lại ở module mới, không tự động thừa hưởng từ Contract. **Tên tệp lúc tải xuống lấy từ [`EmployeeDocument::downloadFileName()`](app/Models/EmployeeDocument.php)**, không truyền thẳng `document_name`: `document_name` là tên người dùng gõ (vd "CCCD mặt trước") nên không kèm đuôi, tải về sẽ ra tệp không đuôi mà Windows không biết mở bằng gì — lỗi này không lộ khi chỉ có PDF/ảnh mở tạm được, nhưng thành rõ ngay khi nhận thêm Word/Excel. Đuôi thật nằm trong `file_path`; ghép ở **một chỗ duy nhất** trên Model để `EmployeeDocumentResource` (`file_name` gửi cho frontend chọn kiểu xem trước) và Controller (tên tệp tải xuống) không lệch nhau. Khác `EmployeeContractController::download()` — bên đó gắn cứng `.pdf` được vì hợp đồng chỉ nhận PDF. Lưu ở disk `local` (riêng tư) giống hợp đồng, không phải `public` |
| Luân chuyển nhân sự | [`app/Http/Controllers/Api/V1/EmployeeTransferController.php`](app/Http/Controllers/Api/V1/EmployeeTransferController.php) (`index`/`store`/`download`, route lồng `/employees/{employee}/transfers`), [`app/Services/EmployeeTransferService.php`](app/Services/EmployeeTransferService.php), [`app/Repositories/EmployeeTransferRepository.php`](app/Repositories/EmployeeTransferRepository.php), [`app/Http/Requests/EmployeeTransfer/StoreEmployeeTransferRequest.php`](app/Http/Requests/EmployeeTransfer/StoreEmployeeTransferRequest.php), [`app/Http/Resources/EmployeeTransferResource.php`](app/Http/Resources/EmployeeTransferResource.php). **Quyết định thiết kế quan trọng**: tạo 1 bản ghi luân chuyển thì **áp dụng ngay lập tức** vào `department_id`/`position_id`/`manager_id` của Employee trong cùng transaction — KHÔNG chờ tới `effective_date` mới áp dụng, vì dự án chưa có hàng chờ/lịch chạy nền (`queue:work`/Horizon, xem Ghi chú mục 1) để tự động thực thi đúng ngày trong tương lai; `effective_date` chỉ mang tính ghi nhận/báo cáo. `new_position_id`/`new_manager_id` là `nullable` = "giữ nguyên", không bắt buộc đổi cả 3 thứ cùng lúc với phòng ban mới. Có kiểm tra chống vòng lặp quản lý qua `EmployeeRepository::wouldCreateCycle()` (tái dùng, không viết lại) khi đặt `new_manager_id`. `approved_by`/`approved_at` tự set = người gọi API + thời điểm hiện tại (tạo bản ghi này qua quyền `employee.update` coi như đã là duyệt, không xây thêm luồng duyệt riêng — khác với "Duyệt phép" là module có luồng duyệt thật ở Phase 3). Không có Sửa/Xóa — lịch sử luân chuyển là bản ghi sự kiện đã xảy ra, giống triết lý Hợp đồng lao động. `download()` (file "Quyết định điều động", tùy chọn — nullable) cũng cùng dạng kiểm tra IDOR như Hợp đồng/Tài liệu. **Chặn chọn trùng phòng ban hiện tại**: `to_department_id === $employee->department_id` (ép `(int)` cả 2 vế vì request multipart gửi số dạng chuỗi) ném `ValidationException` ngay đầu `create()`, trước cả kiểm tra vòng lặp quản lý — "luân chuyển" về đúng phòng ban đang ở không có ý nghĩa nghiệp vụ. **Đang là Trưởng phòng mà bị điều đi phòng khác** (`$employee->position?->type === 'head'`, xem mục 7): tự để trống `Department.manager_id` của phòng cũ (không tự chọn người thay thế) và tự hạ Chức vụ về "Nhân viên" mặc định của phòng ban MỚI (`PositionService::ensureDefaultPosition()`) — trừ khi lượt luân chuyển này đã tự chọn `new_position_id`, khi đó tôn trọng lựa chọn đó thay vì ghi đè. Đây là **chiều ngược lại** của `DepartmentService::syncHeadPosition()` (mục 6): nơi đó xử lý khi Trưởng phòng bị đổi trực tiếp qua form Phòng ban, còn đây xử lý khi Trưởng phòng "biến mất" gián tiếp do bị luân chuyển đi nơi khác — 2 tình huống kích hoạt khác nhau nên đặt ở 2 Service khác nhau, không gộp chung 1 hàm. Test: `test_cannot_transfer_to_current_department`, `test_transferring_department_head_clears_old_department_manager_and_demotes_position` trong `EmployeeTransferTest.php` |
| Model | [`app/Models/Employee.php`](app/Models/Employee.php) — quan hệ `user()` (belongsTo User, đối ứng `User::employee()`), `department()`, `position()`, tự tham chiếu `manager()`/`subordinates()` (giống cặp `parent()`/`children()` của Department), `contracts()`/`bankAccounts()`/`documents()`/`transfers()`. [`app/Models/Position.php`](app/Models/Position.php) — `belongsTo` Department. [`app/Models/EmployeeContract.php`](app/Models/EmployeeContract.php), [`app/Models/EmployeeBankAccount.php`](app/Models/EmployeeBankAccount.php), [`app/Models/EmployeeDocument.php`](app/Models/EmployeeDocument.php) (thêm `uploader()` — `belongsTo(User::class, 'uploaded_by')`; và `downloadFileName()` ghép `document_name` + đuôi lấy từ `file_path` — dùng chung cho Resource lẫn Controller, xem mục Tài liệu cá nhân), [`app/Models/EmployeeTransfer.php`](app/Models/EmployeeTransfer.php) (nhiều quan hệ nhất: `fromDepartment()`/`toDepartment()`/`newManager()`/`oldPosition()`/`newPosition()`/`approver()`, **không** dùng `SoftDeletes` — khác 4 Model kia — vì bản ghi lịch sử không có khái niệm "xóa mềm rồi khôi phục") — đều `belongsTo` Employee |
| Validation | [`app/Http/Requests/Employee/StoreEmployeeRequest.php`](app/Http/Requests/Employee/StoreEmployeeRequest.php), [`app/Http/Requests/Employee/UpdateEmployeeRequest.php`](app/Http/Requests/Employee/UpdateEmployeeRequest.php) — **không** có rule cho `code` (tự sinh, xem Ghi chú); `avatar` validate riêng trong `EmployeeController::uploadAvatar()` (`$request->validate()` tại chỗ, không qua FormRequest riêng vì chỉ 1 field). Rule `unique` (`company_email`, `cccd`, `personal_tax_code`, `user_id`) ở bản Update đều loại trừ chính bản ghi đang sửa qua `$this->route('employee')->id`, giống `UpdateDepartmentRequest`. **Trường bắt buộc (chốt sau Ngày 28)**: hồ sơ chỉ được tạo sau khi nhân viên đã ký hợp đồng và đi làm, nên **13 trường đều `required`** — `full_name`, `company_email`, `hire_date`, `date_of_birth`, `gender`, `phone`, `personal_email`, `cccd`, `personal_tax_code`, `address_detail`, `province_code`, `commune_code`, `department_id`. Giữ `nullable` cho 5 trường: `position_id` (**đổi lại thành nullable** — chức vụ có thể chưa xếp ngay lúc tạo hồ sơ, chờ phân công sau; khác Phòng ban vẫn bắt buộc), `manager_id` (giám đốc không có cấp trên), `employment_status` (có default `probation` ở DB), `probation_end_date`, `termination_date`. Lưu ý DB vẫn để `NULL` được cho các trường này ở tầng schema — ràng buộc `required` nằm ở tầng Request. **`commune_code` dùng `Rule::exists('communes','code')->where('province_code', $this->input('province_code'))`** — không chỉ kiểm tra Xã có tồn tại, mà còn phải *thuộc đúng* Tỉnh đã chọn, tránh gửi lệch cặp Tỉnh-Xã không liên quan nhau (xem mục 9 — Địa chỉ hành chính) |
| Output / Phân quyền hiển thị | [`app/Http/Resources/EmployeeResource.php`](app/Http/Resources/EmployeeResource.php) — **ẩn field nhạy cảm** (`date_of_birth`, `gender`, `phone`, `personal_email`, `cccd`, `personal_tax_code`, `address_detail`, `province`, `commune`) khi người xem (`$request->user()->employee`) là cấp dưới (ở bất kỳ cấp nào) của nhân viên đang xem. Field `manager` lồng bên trong **phải bọc đệ quy qua `EmployeeResource`** (không trả Model trần) để logic ẩn field áp dụng cả cho quan hệ lồng. `province`/`commune` trả nguyên object (`code`, `name`) qua `whenLoaded()`, không cần Resource riêng vì đây chỉ là dữ liệu tham chiếu không nhạy cảm, chỉ nhạy cảm khi đi kèm địa chỉ chi tiết của 1 người cụ thể — nên vẫn ẩn theo `$hideSensitive` như các field khác. `loadMissing(['department','position','manager','province','commune'])` ở đầu `toArray()` vì `show()`/`store()`/`update()` không tự eager-load như `index()`. `avatar_url` trả URL đầy đủ qua `Storage::disk('public')->url(...)` — **không** dùng `Storage::url()` (disk mặc định trong `.env` là `local`, không phải `public`, sẽ ra URL sai/thiếu domain). **Ngày 25 (tối ưu N+1)**: tập ID cấp trên của người xem được cache tĩnh theo `viewer->id` (`self::$ancestorIdsCache`, xây trên `EmployeeRepository::ancestorIds()`) — tính đúng 1 lần cho cả danh sách thay vì lặp lại phép duyệt `manager` cho từng dòng (đo thật: 10 lần gọi cùng 1 viewer chỉ tốn 4 query, không phải ~40) |
| Database | Bảng `employees` (migration Ngày 04: `user_id`, `department_id`, `position_id`, `manager_id` tự tham chiếu, `code`, `full_name`, `date_of_birth`, `gender`, `phone`, `company_email`, `personal_email`, `cccd`, `address_detail` (đổi tên từ `addresses`, xem mục 9), `province_code`/`commune_code` (FK thêm sau, xem mục 9), `personal_tax_code`, `avatar`, `hire_date`, `probation_end_date`, `termination_date`, `employment_status`), `positions`, `employee_contracts`, `employee_bank_accounts`, `employee_documents`, `employee_transfers` — schema có sẵn, đã khớp với 5 Model trên. **Index tìm kiếm** (Ngày 25): [`2026_09_05_000001_add_search_indexes_to_employees_table.php`](database/migrations/2026_09_05_000001_add_search_indexes_to_employees_table.php) thêm index cho `full_name` và `employment_status` — lưu ý index thường (BTREE) chỉ tăng tốc so khớp chính xác/tiền tố (`LIKE 'x%'`), **không** tăng tốc tìm kiếm `LIKE '%x%'` (chứa ở giữa chuỗi); muốn tìm "chứa" nhanh thật sự cần FULLTEXT index hoặc search engine riêng, chưa làm ở ngày này |
| Database & API | Endpoint `GET/POST /api/v1/employees`, `GET /employees/stats`, `GET/PUT/DELETE /api/v1/employees/{employee}`, `POST /employees/{employee}/avatar`, `GET/POST /employees/{employee}/contracts`, `GET /employees/{employee}/contracts/{contract}/download` (route tên `employees.contracts.download`, dùng để sinh `download_url`) — khai báo tại [`routes/api/v1/employees.php`](routes/api/v1/employees.php). **`/stats` phải khai TRƯỚC `/{employee}`** trong file route — nếu để sau, Laravel khớp route theo thứ tự đăng ký nên `{employee}` sẽ nuốt mất chuỗi "stats", hiểu nhầm thành `id` (404 employee not found) thay vì tới đúng `stats()`. Khác Department: **4 mã quyền riêng** cho từng hành động (`employee.view`, `employee.create`, `employee.update`, `employee.delete`), không gộp chung 1 mã `manage`; avatar/hợp đồng/`stats` dùng lại `employee.view`/`employee.update`, không có mã quyền riêng |
| Lưu trữ file (Storage) | **Avatar** → disk `public` (`storage/app/public/`, công khai qua `/storage/...`, cần `php artisan storage:link`), file cũ **tự xóa** khi upload avatar mới (`EmployeeService::updateAvatar()`). **PDF hợp đồng** → disk `local` (`storage/app/private/`, riêng tư, **không có URL công khai**, chỉ tải qua route `download()` có kiểm tra quyền + kiểm tra đúng nhân viên). ⚠️ Lưu ý môi trường Docker: PHP-FPM xử lý request thật chạy bằng user `www-data`, còn `docker compose exec`/tinker chạy bằng `root` — nếu tạo thư mục con trong `storage/app/private/` bằng tinker/exec trước, thư mục sẽ thuộc về `root` và **request thật (www-data) sẽ bị "Permission denied" khi ghi file**, dù code hoàn toàn đúng. Gặp lỗi upload lạ, luôn nghi ngờ chỗ này trước |
| Test | [`tests/Feature/Employee/EmployeeTest.php`](tests/Feature/Employee/EmployeeTest.php) — quyền hạn, tạo (mã tự sinh, bỏ qua `code` client gửi), sửa (bỏ qua `code`, chống vòng lặp quản lý — tự làm quản lý chính mình lẫn chuyển lên trên cấp dưới), xóa mềm, **ẩn field nhạy cảm đúng người xem** (cấp dưới xem cấp trên bị ẩn, tự xem mình thì không, field `manager` lồng bên trong cũng bị ẩn đúng), avatar (upload, tự xóa avatar cũ, từ chối file không phải ảnh), tìm kiếm theo tên + lọc theo `employment_status`, **`/stats` đếm đúng theo `employment_status`** (yêu cầu quyền `employee.view`, trả đúng `total`/`active`/`probation`/`resigned`). [`tests/Feature/Employee/EmployeeContractTest.php`](tests/Feature/Employee/EmployeeContractTest.php) — quyền hạn, tạo kèm PDF, `contract_number` unique, `contract_file` bắt buộc, xem danh sách, tải file đúng, và **chặn IDOR** (tải hợp đồng qua cặp employee/contract không khớp). [`tests/Feature/Employee/EmployeeDocumentTest.php`](tests/Feature/Employee/EmployeeDocumentTest.php) — quyền hạn, upload (PDF/JPG/PNG/DOCX/XLSX được, `.doc`/`.xls` và định dạng lạ bị chặn; test Word/Excel còn khẳng định `file_name` giữ đúng đuôi `.docx`/`.xlsx` vì `FilePreviewDialog.vue` chọn kiểu xem trước bằng chính đuôi này), **tên tệp tải xuống giữ đuôi** (`assertDownload('CCCD mat truoc.pdf')` — chốt lại lỗi tên tệp không đuôi để không tái diễn), xem danh sách, tải file đúng, xóa mềm (file vật lý vẫn còn sau khi xóa mềm), **chặn IDOR cả ở download lẫn destroy**. [`tests/Feature/Employee/EmployeeTransferTest.php`](tests/Feature/Employee/EmployeeTransferTest.php) — quyền hạn, tạo luân chuyển **áp dụng ngay vào hồ sơ nhân viên** (verify bằng cách `refresh()` Model rồi so `department_id`/`position_id`), không đổi `new_position_id` thì giữ nguyên chức vụ cũ, chặn vòng lặp quản lý, xem lịch sử, upload/tải quyết định điều động, chặn IDOR khi tải, **chặn chọn trùng phòng ban hiện tại** (422 trên `to_department_id`), **điều chuyển 1 Trưởng phòng sang phòng khác** tự để trống `manager_id` phòng cũ + tự hạ Chức vụ về "Nhân viên" mặc định của phòng mới |
| Ghi chú | Ngày 21 Model, Ngày 22 Validation, Ngày 23 CRUD + Resource, Ngày 24 Upload avatar + Hợp đồng PDF, Ngày 25 tối ưu N+1 + Index + Test — tất cả xong, đã test qua HTTP thật (multipart thật, không chỉ giả lập). `code` **tự sinh** giống `PB001` của Department. `personal_email` không bắt `unique` (không có ràng buộc DB, chỉ là liên hệ phụ). Quyết định đã chốt: **không** chặn xóa nhân viên còn cấp dưới (khác Department) — cấp dưới chỉ đơn giản không xem được `manager` nữa sau khi cấp trên bị xóa mềm, do `SoftDeletingScope` ẩn bản ghi đã xóa, không dựa vào `nullOnDelete()` của DB (không kích hoạt với xóa mềm). **Phát hiện lúc viết test Ngày 25**: `show`/`store`/`update`/`uploadAvatar` trước đó KHÔNG bọc `{"data": {...}}` như `index()`, khiến chính bộ test đầu tiên viết ra bị dương tính giả (assert `data.cccd` là `null` — đúng bằng trùng hợp vì sai đường dẫn JSON, không phải vì ẩn field đúng) — đã sửa Controller cho nhất quán, đây là ví dụ thực tế vì sao 1 test "pass" chưa chắc đã đúng, phải tự hỏi "test này thật sự chứng minh được gì". **Ngày 27 (`EmployeeDetail.vue`)** đã xong, verify qua HTTP thật (login → tạo nhân viên → tạo hợp đồng kèm PDF thật → gọi đúng 3 endpoint mà trang dùng → tải file thành công → dọn dữ liệu test). `agreed_salary`/`insurance_salary` từ API trả về dạng **chuỗi** (cột `decimal` của Laravel luôn serialize thành string, không phải number) — `Intl.NumberFormat().format()` vẫn format đúng vì tự ép kiểu chuỗi số hợp lệ sang number, đã verify trực tiếp bằng Node chứ không chỉ đoán. **Ngày 28 (`EmployeeForm.vue`)** đã xong — không có `user_id`/avatar trong form (xem cột Frontend). Verify qua HTTP thật với payload đúng y hệt form gửi (kể cả `null` cho field trống — `submit()` tự đổi `""` thành `null` trước khi gửi vì `"nullable|date"` của Laravel không chấp nhận chuỗi rỗng là "chưa nhập"). **Bài học khi tự verify bằng curl**: lần đầu test 422 bằng curl trần bị Laravel trả về **302 redirect** thay vì JSON — vì thiếu header `X-Requested-With: XMLHttpRequest` mà `bootstrap.js` gắn mặc định cho MỌI request axios thật (`Request::expectsJson()` của Laravel dựa vào header này để quyết định trả JSON hay redirect kiểu form HTML truyền thống); thêm đúng header vào curl thì mới thấy đúng `{"errors": {...}}` — nhắc rằng test bằng curl trần phải tự mô phỏng đủ header, không chỉ tin request "gửi được" là đã mô phỏng đúng hành vi frontend thật. **Validation phía client**: `EmployeeForm.vue` bọc các `FormSection` trong `<v-form ref="formRef">`, mỗi ô bắt buộc có `:rules` phản chiếu đúng Form Request, và `submit()` gọi `await formRef.value.validate()` trước — sai thì **không gửi request nào** (đã verify: bấm Lưu với form rỗng hiện đúng 12 dòng lỗi, tab Network không có POST). Trước đó form chỉ có dấu `*` trang trí, backend `nullable`, nên người dùng bỏ trống vẫn lưu được — sửa rule ở backend thì **phải sửa cả `rules` trong `EmployeeForm.vue`**, hai nơi không tự đồng bộ |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên |

## 9. Địa chỉ hành chính (Tỉnh/Xã)

| Hạng mục | Chi tiết |
|---|---|
| Giao diện Frontend | [`resources/js/services/addressService.js`](resources/js/services/addressService.js) (`provinces()`, `communes(provinceCode)`) — dùng trong [`EmployeeForm.vue`](resources/js/views/Employee/EmployeeForm.vue) (xem mục 8). Chưa có UI quản lý riêng (không cần — dữ liệu chỉ đọc, nạp 1 lần qua Seeder) |
| Xử lý Backend | [`app/Http/Controllers/Api/V1/AddressController.php`](app/Http/Controllers/Api/V1/AddressController.php) — chỉ 2 hàm đọc `provinces()`/`communes()`, **không có Sửa/Xóa/Thêm** (dữ liệu hành chính cố định, Admin không tự quản lý qua API) |
| Model | [`app/Models/Province.php`](app/Models/Province.php) — `hasMany(Commune::class, 'province_code', 'code')`. [`app/Models/Commune.php`](app/Models/Commune.php) — `belongsTo(Province::class, 'province_code', 'code')`. Cả 2 dùng **khóa chính không tự tăng** (`code` — đúng mã hành chính GSO từ nguồn dữ liệu ngoài, không tự sinh `id` riêng để khỏi phải map ngược khi đối chiếu dữ liệu): `$primaryKey='code'; $incrementing=false; $keyType='int'`. Không dùng `Auditable`/`SoftDeletes` — đây là dữ liệu tham chiếu tĩnh, không phải nghiệp vụ cần audit |
| Database & API | Bảng `provinces` (`code` PK, `name`), `communes` (`code` PK, `name`, `province_code` FK) — migration [`2026_09_07_000001_create_provinces_table.php`](database/migrations/2026_09_07_000001_create_provinces_table.php), [`...000002_create_communes_table.php`](database/migrations/2026_09_07_000002_create_communes_table.php). Bảng `employees` thêm `province_code`/`commune_code` (nullable, FK `nullOnDelete`) và đổi tên `addresses` → `address_detail` qua [`...000003_add_province_commune_to_employees_table.php`](database/migrations/2026_09_07_000003_add_province_commune_to_employees_table.php) (dùng `renameColumn()` — Laravel 12 hỗ trợ thẳng, không cần `doctrine/dbal`). Endpoint `GET /api/v1/addresses/provinces`, `GET /api/v1/addresses/communes?province_code=` — chỉ cần `auth:api`, không có mã quyền riêng (ai đăng nhập cũng xem được, không phải dữ liệu nhạy cảm) |
| Nguồn dữ liệu | **Không tự gõ tay** — 34 Tỉnh + 3321 Xã/Phường tải nguyên trạng từ [`provinces.open-api.vn`](https://provinces.open-api.vn) (API công khai, đã cập nhật theo đợt sáp nhập hành chính 2025 — chỉ còn 2 cấp Tỉnh → Xã, **không còn Huyện**), lưu tĩnh tại [`database/seeders/data/provinces.json`](database/seeders/data/provinces.json) + [`.../communes.json`](database/seeders/data/communes.json), nạp vào DB qua [`ProvinceCommuneSeeder.php`](database/seeders/ProvinceCommuneSeeder.php) (`insertOrIgnore()` theo lô 500 dòng, gọi trong `DatabaseSeeder`). Ranh giới hành chính có thể đổi tiếp trong tương lai — muốn cập nhật thì tải lại 2 file JSON này, không cần sửa code Seeder |
| Validation | [`StoreEmployeeRequest`](app/Http/Requests/Employee/StoreEmployeeRequest.php)/[`UpdateEmployeeRequest`](app/Http/Requests/Employee/UpdateEmployeeRequest.php): `province_code` (`required, exists:provinces,code`), `commune_code` (`required`, `Rule::exists('communes','code')->where('province_code', $this->input('province_code'))` — **không chỉ kiểm tra Xã có tồn tại, còn phải đúng thuộc Tỉnh đã chọn**, chặn trường hợp gửi lệch cặp) |
| Test | [`EmployeeTest.php`](tests/Feature/Employee/EmployeeTest.php) — `addressIds()` helper lấy **Tỉnh/Xã thật đã seed** (không tự tạo bản ghi giả, vì bảng này chỉ đọc), dùng trong `validPayload()`. Verify thêm qua HTTP thật: tạo nhân viên với `province_code`/`commune_code` hợp lệ → nhận đúng object `province`/`commune` lồng trong response; gửi `commune_code` tồn tại nhưng thuộc Tỉnh khác → đúng 422 với thông báo "không thuộc Tỉnh/Thành phố đã chọn" |
| Danh sách tệp cần sửa khi bảo trì | Toàn bộ các file trên; muốn thêm quản lý Tỉnh/Xã riêng cho Admin (hiện chưa có, không cần) thì mới cần thêm Sửa/Xóa vào `AddressController` |

## 10. Hạ tầng chung (không thuộc 1 module cụ thể)

| Hạng mục | Chi tiết |
|---|---|
| Axios & Interceptor | [`resources/js/bootstrap.js`](resources/js/bootstrap.js) — tự gắn `Authorization` header, tự xử lý 401 (đá về `/login`, trừ chính request login) và 403 |
| Icon set | `@mdi/font`, import tại [`resources/js/plugins/vuetify.js`](resources/js/plugins/vuetify.js) |
| Component UI dùng chung | [`resources/js/components/common/`](resources/js/components/common): `FormDialog.vue` (khung dialog nhập liệu — banner, alert lỗi chung, nút Hủy/Lưu, slot `footer-note`; `<v-dialog>` có prop **`scrollable`** — thiếu prop này thì form nhiều field (vd `EmployeeForm.vue`, 3 khối field) sẽ bị cắt cụt khi cao hơn viewport và **không cuộn được**: `<v-card>` của dialog có class `overflow-hidden` (để bo góc banner gradient theo `rounded="xl"`) mang `overflow: hidden !important`, đè mất đúng luật `overflow-y: auto` Vuetify tự đặt cho card bên trong dialog — thêm `scrollable` thì Vuetify chuyển vùng cuộn xuống riêng `v-card-text` (class `.v-dialog--scrollable > ... > .v-card-text { overflow-y: auto }`), giữ banner + nút Hủy/Lưu cố định, chỉ phần giữa cuộn — đã verify bằng cách đọc thẳng CSS đã build của `node_modules/vuetify`, không đoán. **Lớp hiển thị** (giữ nguyên hệ màu/font/Glassmorphism theo mục 6 tài liệu, chỉ nâng độ mượt): ô icon vuông cạnh tiêu đề qua prop `icon` (mặc định `mdi-note-edit-outline`, đặt `icon=""` để bỏ) cho khối tiêu đề một điểm neo thị giác; vệt sáng `radial-gradient` mờ ở góc trên phải banner để dải gradient bớt phẳng; nút đóng chuyển sang dạng ghost — nút trắng đặc cũ hút mắt hơn cả tiêu đề trong khi đây chỉ là thao tác phụ; chân dialog tô chìm `rgba(--v-theme-on-surface, .03)` tách vùng thao tác khỏi vùng nhập; nút nhún `scale(.97)` khi bấm và đổ bóng theo màu primary khi hover — **chỉ áp trong dialog này qua `:deep()`**, không đụng nút của toàn app; alert lỗi chung bọc `<v-expand-transition>` vì alert bật đột ngột làm cả form giật xuống, người dùng mất dấu ô đang nhập. **Chuyển động mở** đặt bằng `@keyframes` ngay trên thẻ `v-card` (trượt lên 14px + `scale(.97)`, đường cong nảy nhẹ), lớp phủ dùng `fade-transition` có sẵn — cố ý KHÔNG tự viết transition riêng cho `<v-dialog>` vì lớp phủ do Vuetify dựng nằm ngoài template component nên phải viết CSS không scoped và lệ thuộc cơ chế gắn/gỡ lớp `*-enter-from` của Vue; để ở card thì đổi prop `transition` sau này cũng không ảnh hưởng. Toàn bộ có nhánh `prefers-reduced-motion: reduce` tắt hiệu ứng), `FormSection.vue` (khối nhóm field có tiêu đề), `DataTable.vue` (bọc `v-data-table`/`v-data-table-server` + prop `actions` khai báo thao tác dòng — xem Ghi chú Ngày 26 bên dưới về tương thích Vuetify 3 và mục "Thao tác dòng" về cách khai báo), `SearchField.vue` (ô tìm kiếm debounce), `PageHeader.vue` (tiêu đề trang + slot `actions`), `StatusChip.vue` (Ngày 26 — chip trạng thái dùng chung: nhận `status` thô từ API + `map` do từng trang tự định nghĩa nhãn/màu; so khớp qua `String(status)` nên nhận cả số nguyên `1/0` lẫn chuỗi enum. Đang dùng cho `is_active` của Department và `employment_status` của Employee), `InputDate.vue` + `InputMoney.vue` (xem mục "Ô nhập ngày & tiền" bên dưới). Module mới nên tái dùng các component này thay vì tự viết lại |
| **Xem trước tài liệu** | [`components/common/FilePreviewDialog.vue`](resources/js/components/common/FilePreviewDialog.vue) — dialog xem tệp ngay trên trang, đang dùng ở cả 3 bảng của [`EmployeeDetail.vue`](resources/js/views/Employee/EmployeeDetail.vue) (Hợp đồng, Tài liệu, Quyết định luân chuyển). Nhận `file-url` (là `download_url` API trả về) + `file-name`, **chọn cách hiển thị theo đuôi file**: `.pdf` → [`pdfjs-dist`](https://www.npmjs.com/package/pdfjs-dist) vẽ ra `<canvas>` kèm thanh công cụ của app (lật trang, phóng to/thu nhỏ 50-300%, "vừa khung"), `.jpg/.jpeg/.png` → `<img>` từ blob URL, `.docx` → [`docx-preview`](https://www.npmjs.com/package/docx-preview) tự vẽ DOM vào thẻ `div`, `.xlsx` → [`xlsx`](https://www.npmjs.com/package/xlsx) đọc workbook rồi **tự dựng bảng** (chip chọn sheet khi nhiều sheet, hàng tiêu đề dính khi cuộn dọc, cột STT dính khi cuộn ngang, số ≥ 1000 định dạng `Intl.NumberFormat("vi-VN")` và canh phải, ô ngày in `dd/mm/yyyy`). **PDF trước đây nhúng bằng `<iframe>`** — phải mượn trình xem của trình duyệt, mỗi trình duyệt một kiểu và lạc hẳn khỏi giao diện kính (mục 6 tài liệu yêu cầu Premium UX/UI); Excel thì không có hàng tiêu đề, chỉ xem được sheet đầu và số hiển thị thô. **Ba thư viện đều `import()` động, KHÔNG import tĩnh**: mục 2 tài liệu yêu cầu Lazy Loading, và số đo thực tế cho thấy tại sao — chunk `EmployeeDetail` từ **553 kB tụt còn 51,6 kB** (gzip 178 kB → 16 kB), phần nặng tách thành chunk riêng chỉ tải khi người dùng thật sự bấm xem tài liệu. **Hai thứ tuyệt đối không được đổi**: (1) không dùng `v-html` cho Word/Excel — nội dung tệp là dữ liệu người dùng tải lên, nhét thẳng vào `v-html` là mở đường XSS; `docx-preview` tự tạo node còn Excel render qua `{{ }}` nên đều được Vue escape; (2) tải tệp bằng `window.axios` chứ không phải `<iframe src="download_url">` trần — route tải file yêu cầu `auth:api`, thẻ trần không gắn được `Authorization` header nên sẽ 401 (cùng lý do với nút tải file ở `EmployeeDetail.vue`). **Sáu cái bẫy đã vấp và sửa, đều verify bằng trình duyệt thật**: (a) `pdfDoc` phải là **`shallowRef`** — `ref()` bọc đối tượng trong Proxy phản ứng sâu, mà `PDFDocumentProxy` của pdf.js dùng private field (`#`), gọi method qua Proxy sẽ ném `TypeError` vì `this` là Proxy chứ không phải đối tượng gốc; (b) hàm giải phóng là **`destroy()` của loading task**, `PDFDocumentProxy` chỉ có `cleanup()` — gọi nhầm `pdfDoc.destroy()` ném `"destroy is not a function"` và **hỏng luôn lần mở dialog kế tiếp** (lỗi này chỉ lộ khi đóng rồi mở lại, không lộ ở lần mở đầu); (c) phải `renderTask.cancel()` lượt vẽ cũ trước khi vẽ lượt mới, bấm lật trang/zoom nhanh mà không hủy thì pdf.js ném `"Cannot use the same canvas during multiple render() operations"`; `RenderingCancelledException` là chuyện bình thường, **không** được báo ra giao diện; (d) canvas phải có `background: #fff` — pdf.js vẽ nền trong suốt, thiếu dòng này thì lúc chưa vẽ xong nền tối lộ qua và trang giấy nhìn xám xịt; (e) vẽ theo `devicePixelRatio` rồi thu lại bằng CSS, nếu vẽ đúng kích thước CSS thì trên màn retina / Windows scale 125-150% chữ trong PDF bị mờ; (f) dọn state ở **`@after-leave`** chứ không phải ngay khi `open` thành false — dọn sớm thì người dùng nhìn thấy bảng/trang trắng trơn trong lúc dialog mờ dần, trông như tài liệu bị mất. Thẻ `<div ref="docxContainer">` nằm trong nhánh chỉ hiện khi `loading = false`, nên phải tắt `loading` **trước** rồi mới `await nextTick()` mới lấy được ref. Toàn bộ luồng tải bọc `try/catch` và hiện `v-alert` (401 hết phiên báo riêng) thay vì để Promise văng lỗi — tiêu chí nghiệm thu mục 8 yêu cầu "không xuất hiện lỗi Javascript ở console". Đuôi `.doc`/`.xls` cũ tuy không tải lên được vẫn có nhánh thông báo riêng, phòng dữ liệu cũ hoặc khi mở rộng validation sau này |
| **Thông báo ngắn (toast)** | [`stores/useToastStore.js`](resources/js/stores/useToastStore.js) giữ hàng đợi, [`components/common/AppToast.vue`](resources/js/components/common/AppToast.vue) hiển thị — gắn **một lần duy nhất** trong [`App.vue`](resources/js/App.vue). Dùng ở bất kỳ đâu: `useToastStore().success("Đã lưu.")`, có `success`/`info`/`warning`/`error`. **Cố ý KHÔNG dùng `<v-snackbar-queue>`** dù Vuetify có sẵn: ở bản 3.13.3 nó không bao giờ tự tắt. Nguyên nhân nằm ở `VSnackbarQueue.showNext()` — nó bật `isActive` trong `nextTick()`, mà `VSnackbar` chỉ lên hẹn giờ tắt ở `onMounted` (`if (isActive.value) startTimeout()`) hoặc khi `isActive` **đổi giá trị**; snackbar được mount sau khi `isActive` đã là true nên không nhánh nào chạy. Đã loại trừ khả năng trình duyệt bóp timer (đo `setTimeout` 2000ms → thực tế 2003ms) và dựng một `<v-snackbar>` trần cùng trang với `timeout: 2000` → tắt đúng hẹn. **Ba cái bẫy khi tự viết lại, đều đã vấp và sửa**: (1) để `visible`/`current` trong component rồi `watch` qua lại với store thì hai watcher tranh nhau, message bị lấy khỏi hàng đợi nhưng không hiện; (2) `model-value` về false chỉ ẩn nội dung chứ Vuetify **không tháo phần tử** — còn lại khung xám rỗng chỉ có nút "Đóng" nằm mãi ở góc màn hình, phải thêm `v-if="toast.current"`; (3) có `v-if` rồi thì phần tử mount lúc `model-value` đã là true nên hoạt ảnh vào bị bỏ qua và `opacity` kẹt ở 0 — phải mount ở trạng thái đóng rồi mở ở `nextTick`, và vì thế cờ "đang bận" trong `push()` phải là `current` chứ không phải `visible` (hai lần push liên tiếp cùng lượt chạy đều thấy `visible=false`, cái sau đè mất cái trước). Toàn bộ hẹn giờ do store giữ, component truyền `:timeout="-1"` để Vuetify không đụng vào. **Giao diện**: bỏ khối màu đặc mặc định của Vuetify (`color="transparent"` + `content-class="app-toast-slot"` xóa padding/nền của `.v-snackbar__content`) rồi tự dựng thẻ bên trong theo mục 6 tài liệu yêu cầu — nền `.glass-panel`, bo 14px, icon riêng cho từng loại trong ô bo góc, nút đóng dạng icon; **màu chỉ dùng để nhấn ở viền trái và ô icon**, không tô kín nền, nên đọc chữ dễ hơn trên nền tối. Style để **không scoped** vì `content-class` được Vuetify gắn lên phần tử của chính nó, không mang scope id của component. Toàn bộ màu lấy từ token `--v-theme-*` (giống mọi component khác) nên tự đổi theo Dark/Light. **Bẫy ở theme sáng**: `<v-snackbar>` vẫn tự tô nền `surface` + đổ bóng + đặt `min-width` ở lớp vỏ `.v-snackbar__wrapper` bất kể `color="transparent"` — theme tối trùng màu nền nên không ai thấy, theme SÁNG lộ ra một khối trắng to bọc quanh toast và đè lên nút của trang. Phải thêm `class="app-toast-root"` rồi dọn `.app-toast-root .v-snackbar__wrapper { background: transparent; box-shadow: none; min-width: 0 }`. Dọn xong vỏ vẫn chưa đủ, còn hai chỗ nữa chỉ lộ ở theme sáng: (a) **không dùng `.glass-panel`** (nền `surface` 72%) cho toast — trên nền trang gần trắng thì chữ phía sau lộ xuyên qua, đọc rất khó; thay bằng `rgba(var(--v-theme-surface), 0.92)` + `blur(14px)`, vẫn giữ cảm giác kính theo mục 6 nhưng tách hẳn khỏi nền; (b) dải màu bên trái phải là **`::before`** trong khối `overflow: hidden` chứ KHÔNG phải `border-left` — border khác màu thân bị `border-radius` cắt vát ở hai đầu, nhìn đúng như viền lỗi. Viền ngoài dùng `rgba(var(--v-border-color), 0.18)`: token này tự đổi đen/trắng theo theme nên một công thức chạy được cả hai. Đang gọi ở: lưu `EmployeeForm`, lưu `DepartmentForm`, xóa phòng ban trong `Departments.vue` |
| **Thanh loading toàn cục** | [`stores/useLoadingStore.js`](resources/js/stores/useLoadingStore.js) + [`components/common/AppLoadingBar.vue`](resources/js/components/common/AppLoadingBar.vue), cũng gắn một lần trong `App.vue`. Bật/tắt **tự động**, không phải gọi tay: interceptor axios trong [`bootstrap.js`](resources/js/bootstrap.js) (`start()` ở request, `stop()` ở cả nhánh thành công lẫn lỗi) và guard trong [`router/index.js`](resources/js/router/index.js) (`beforeEach`/`afterEach`, thêm `onError` vì tải chunk lỗi thì `afterEach` KHÔNG chạy và thanh loading sẽ quay mãi). Store **đếm số việc đang chạy** chứ không dùng cờ true/false — hai request chồng nhau mà cái nhanh xong trước sẽ tắt mất thanh loading của cái còn lại; `stop()` có chặn dưới 0 phòng khi bị gọi thừa. `z-index: 3000` để nằm trên lớp phủ `v-dialog` (Vuetify dùng 2400), nếu không thì lúc đang lưu trong modal sẽ bị che. **Đã đổi thứ tự trong [`app.js`](resources/js/app.js)**: `createPinia()` phải đăng ký TRƯỚC `router` vì guard của router gọi `useLoadingStore()`. Đây là loading CHUNG, không thay cho `loading` riêng của từng store (vẫn dùng để khóa nút Lưu, hiện skeleton bảng) |
| **Ô chọn có tìm kiếm (`SearchSelect.vue`)** | [`SearchSelect.vue`](resources/js/components/common/SearchSelect.vue) — bọc `v-autocomplete`, dùng cho mọi danh sách dài thay cho `v-select`. Giá trị thêm vào so với Vuetify gốc chỉ có 2 thứ: (1) **tìm kiếm không dấu** — `normalizeVietnamese()` (export sẵn để nơi khác dùng lại) tách dấu bằng `normalize("NFD")` rồi xóa, **riêng chữ `đ` phải đổi thủ công sang `d`** vì nó là ký tự độc lập U+0111 không tách ra được — nhờ vậy gõ `ha noi` ra "Thành phố Hà Nội", gõ `dong da` ra "Phường Đống Đa"; (2) bộ mặc định giao diện + chữ tiếng Việt (`no-data-text`, placeholder "Chưa chọn") để mọi ô chọn trông giống nhau. **Cố ý không khai báo prop `modelValue`** — mọi thuộc tính rơi thẳng xuống `v-autocomplete` qua `$attrs`, nên dùng được cả `v-model` lẫn `:model-value` + `@update:model-value` thủ công (Tỉnh và Phòng ban đang dùng kiểu thứ hai vì phải reset ô phụ thuộc). Đặt `v-bind="$attrs"` **cuối cùng** trong thẻ để nơi gọi ghi đè được mặc định. Đang dùng ở `EmployeeForm.vue` cho Tỉnh/Thành phố, Xã/Phường (3321 bản ghi — `v-autocomplete` cuộn ảo nên chỉ render ~45 dòng nhìn thấy), Phòng ban, Chức vụ, Quản lý trực tiếp. Ở [`DepartmentForm.vue`](resources/js/views/Department/DepartmentForm.vue) dùng cho Phòng ban cha và Trưởng phòng (ô Trưởng phòng nạp tới 1000 nhân viên qua `fetchList({ per_page: 1000 })`) — hai ô này **giữ `placeholder` riêng** ("Không có — đây là phòng ban gốc", "Chưa phân công") vì chữ đó mang nghĩa nghiệp vụ, khác mặc định "Chưa chọn". Giới tính và Trạng thái làm việc **vẫn để `v-select`** vì chỉ 4-5 lựa chọn, thêm ô tìm kiếm chỉ tổ vướng. **Hạn chế đã biết, chưa sửa**: ô đã có sẵn giá trị thì bấm vào gõ tiếp sẽ NỐI vào chữ cũ ("Phòng nhân sự" + "nhan su") nên không ra kết quả — người dùng phải bấm dấu × xóa trước. Đã thử 3 cách bôi đen sẵn khi focus (`@focus`, `@update:focused`, `@focusin` + listener native trên thẻ input) nhưng **không cách nào kiểm chứng được là chạy**, nên đã gỡ bỏ thay vì để lại code chưa chứng minh được |
| **Ô nhập ngày & tiền** | [`InputDate.vue`](resources/js/components/common/InputDate.vue) — bọc `VDateInput` của **`vuetify/labs`** (Vuetify 3 chưa có ở bản stable). Thay cho `<v-text-field type="date">`: ô ngày gốc của trình duyệt hiển thị theo locale máy người dùng (máy tiếng Anh ra `mm/dd/yyyy`) và không theo được giao diện Vuetify. Prop `modelValue` là **chuỗi `"YYYY-MM-DD"`** đúng dạng backend nhận, nhận luôn ISO datetime từ API rồi tự cắt phần giờ; hiển thị `dd/mm/yyyy`, lịch tiếng Việt, tuần bắt đầu Thứ 2. Date được dựng bằng `new Date(y, m-1, d)` theo giờ **địa phương** thay vì `new Date("YYYY-MM-DD")` (chuỗi đó bị hiểu là UTC nên ở múi giờ âm sẽ lùi mất 1 ngày). Đang dùng cho 4 ô ngày trong [`EmployeeForm.vue`](resources/js/views/Employee/EmployeeForm.vue). Ngoài component, file này còn **export 4 hàm dùng chung** cho ngày ISO: `parseIsoDate`, `toIsoDate`, `todayIso`, `shiftIsoDate` — đặt ở đây (block `<script>` thường bên cạnh `<script setup>`) thay vì tạo thư mục `utils/` vì [mục 2.1 tài liệu yêu cầu](docs/YEU_CAU_DU_AN_QLNS.html) chỉ liệt kê `views/ components/ stores/ services/ router/`. **Giới hạn ngày (`min`/`max`) phải phản chiếu đúng rule của Form Request**, vì rule backend so sánh *nghiêm ngặt* còn `min`/`max` của picker *bao gồm* hai đầu — quy đổi bằng `shiftIsoDate`: `before:today` → `max = todayIso() - 1`; `after:hire_date` → `min = hire_date + 1`. Đang áp ở `EmployeeForm.vue`: `date_of_birth` khóa từ hôm nay trở đi, `probation_end_date`/`termination_date` khóa mọi ngày ≤ ngày vào làm (cập nhật lại ngay khi đổi ngày vào làm). `hire_date` **cố ý không giới hạn** vì `StoreEmployeeRequest` chỉ có `required|date`. [`InputMoney.vue`](resources/js/components/common/InputMoney.vue) — gõ `1500000` hiện `1.500.000` (`Intl.NumberFormat("vi-VN")`) nhưng `modelValue` luôn là **số thuần** để gửi thẳng lên API; giữ đúng vị trí con trỏ khi sửa giữa chuỗi bằng cách đếm *số chữ số* trước con trỏ chứ không đếm vị trí ký tự (dấu chấm phân cách làm lệch vị trí ký tự). Dành cho lương/phụ cấp/khấu trừ ở module Tính lương (Ngày 46-50) — **chưa gắn vào form nào**, mới kiểm thử độc lập. Cả hai nhận `error-messages` để hiện lỗi 422 ngay dưới ô nhập theo [`CODING_STANDARDS.md`](docs/CODING_STANDARDS.md) |
| **Ngày 26 — Vuetify 3 (không phải 4)** | Dự án đúng theo spec phải dùng **Vuetify 3** ([YEU_CAU_DU_AN_QLNS.html mục 6](docs/YEU_CAU_DU_AN_QLNS.html#L612): *"Bộ giao diện chuẩn hóa (Vuetify 3)"*) — `package.json` từng bị lệch sang `^4.1.12` từ trước (không ai phát hiện vì vẫn build được), đã sửa lại đúng `^3.13.3`. Khi hạ về v3, `DataTable.vue` gãy 2 chỗ vì API khác v4: (1) slot `#bottom` của `v-data-table`/`v-data-table-server` trong v3 **không có `setPage`** (chỉ có ở v4) — sửa bằng cách bảng tự quản lý `page` qua `v-model:page` + ref nội bộ, không dựa vào hàm từ slot; (2) `v-data-table-server` trong v3 **không có `itemsLength`** trong slot `#bottom` — sửa bằng cách đọc thẳng prop `serverItemsLength` (mình tự truyền vào) thay vì lấy từ slot. Bài học: đổi major version của 1 thư viện UI phải test lại tương tác thật (bấm nút), không chỉ tin `npm run build` chạy được |
| **Thao tác dòng trong bảng (prop `actions`)** | Khai báo ở view, [`DataTable.vue`](resources/js/components/common/DataTable.vue) tự dựng cột "Thao tác" (không cần thêm vào `headers`), tự ẩn cả cột khi không dòng nào có thao tác hiển thị được, và tự mở hộp xác nhận. Mỗi thao tác: `icon`, `tooltip`, `label`, `color`, `hidden`, `disabled`, `confirm`, `onClick(item, { input })`. `hidden`/`disabled` và `confirm.title`/`message`/`warning`/`disabled` nhận cả giá trị tĩnh lẫn hàm `(item) => ...`. `confirm.input` bật ô nhập lý do trong hộp thoại (phục vụ yêu cầu "Duyệt/Từ chối **kèm lý do**" của màn Quản lý Duyệt phép — [YEU_CAU_DU_AN_QLNS.html mục 3.2](docs/YEU_CAU_DU_AN_QLNS.html), Ngày 40). `onClick` async thì nút tự hiện loading; nếu ném lỗi thì hộp thoại giữ nguyên và hiện thông báo lỗi lấy từ `error.response.data.message`, đồng thời phát event `action-error`. **Tương thích ngược:** view nào tự viết slot `#item.actions` thì slot đó vẫn được ưu tiên, bảng không chèn gì. Mẫu đầy đủ (kể cả cảnh báo + khóa nút xác nhận theo dòng): [`Departments.vue`](resources/js/views/Department/Departments.vue) |
| Hiệu ứng Glassmorphism | Class `.glass-panel` khai tại [`resources/css/app.css`](resources/css/app.css) — nền `rgba(var(--v-theme-surface), 0.72)` + `backdrop-filter: blur(12px)`, tự đổi theo theme. Theo yêu cầu thẩm mỹ mục 6, [YEU_CAU_DU_AN_QLNS.html](docs/YEU_CAU_DU_AN_QLNS.html#L987). Đang dùng ở `Login.vue`, `FormDialog.vue`, hộp xác nhận trong `DataTable.vue` và thanh lọc của `Departments.vue` |
| Docker / môi trường local | [`docker-compose.yml`](docker-compose.yml), [`docker/`](docker), chi tiết tại [`docs/LOCAL_DEVELOPMENT.md`](docs/LOCAL_DEVELOPMENT.md) |
| Tài liệu API (Swagger/OpenAPI) | Cấu hình: [`config/l5-swagger.php`](config/l5-swagger.php). Xem tại `/api/documentation`. Annotation dùng chung (`OA\Info`, `OA\SecurityScheme`) đặt tại [`app/Http/Controllers/Controller.php`](app/Http/Controllers/Controller.php); mỗi Controller tự khai `#[OA\Get\|Post(...)]` phía trên từng hàm (xem mẫu tại [`app/Http/Controllers/Api/V1/AuthController.php`](app/Http/Controllers/Api/V1/AuthController.php)). Sinh lại tài liệu bằng `php artisan l5-swagger:generate` sau khi thêm/sửa annotation |

---

**Quy ước cập nhật file này**: mỗi khi thêm 1 tính năng/module mới, thêm 1 mục mới theo đúng bảng 5 cột như trên (Frontend / Backend / Database & API / Test / Tệp cần sửa khi bảo trì) — không cần đủ cả 5 cột nếu module đó không có, nhưng phải cập nhật ngay trong ngày làm, không để dồn. Đường dẫn file viết dạng link Markdown `[tên](đường/dẫn)` để bấm mở trực tiếp.
